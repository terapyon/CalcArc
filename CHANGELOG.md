# 変更履歴

このファイルは利用者から見えた変更を記録する。開発の詳細は
[docs/superpowers/](docs/superpowers/) の設計書と計画書にある。

## 0.9.6 — 2026-09-23

**マニュアルを、電卓の画面の中で読めるようにしました。** iPhone でホーム画面に
追加したアプリから PDF を開くと、**電卓に戻れなくなっていた**ためです
（0.9.4 と 0.9.5 の直しは、どちらも効きませんでした）。

### マニュアルの画面

- **下部の「CalcArc <版数> について」→【マニュアル】で、マニュアルが画面の中で
  読めるようになりました。** 3 冊（簡易・詳細・英語）を上のボタンで切り替えます。
  **PDF を開かずに読めます**
- **【計算機に戻る】は画面のいちばん上に貼り付いていて、どこまで読んでも
  見えています**——**下まで読まないと戻れない、ということがありません**
- **インターネットにつながっていなくても読めます**（一度開いた版は端末に残ります）
- **画面写真は出していません**（写真は PDF の中だけにあります）。**画面写真つきの
  PDF は、この画面の下から開けます**（3 冊ぶん）
- **リンク集は 4 つに戻りました**: 【GitHub（@terapyon）】【マニュアル】
  【ライセンス】【検査結果】。**PDF 3 冊はマニュアルの画面へ移りました**
- **PDF は、また CalcArc のサイトから配ります**（0.9.5 は GitHub のリリースから
  でした）。**保存（ダウンロード）されるか、別の窓で開きます**——端末によります
- **★ iPhone の実機では、まだ何も確かめていません**——**マニュアルの画面が開くか・
  本文が読めるか・戻れるか**も、**PDF を開いたあと電卓に戻れるか**もです。
  手元で確かめたのは**パソコンのブラウザ（Chromium）を iPhone と同じ画面の大きさに
  したもの**までです。**画面の中のマニュアルは、PDF を開かずに読めます**

### 内部の変更

- **マニュアルの本文を、ビルド時に `docs/manual/*.md` から作って同梱するようにした**
  （PDF と同じ素材から作る。実行時には変換しない）。**本体とは別の塊**にしたので、
  **電卓を開くだけの人の読み込みは増えず**（gz で +0.9 kB）、**マニュアルを開いた人
  だけが本文を読み込む**（gz で 28.7 kB）。**マニュアルが変わらない版の更新では、
  読み直しも起きない**（いずれも実測）
- **`#manual` を「1.0 が約束する URL」の表に入れた**（13 → 14）。約束の検査は、
  **画面の種類まで見る**ようにした——見ないと、`#manual` が電卓に倒れても緑だった
- **暗い配色の検査を、写真ではなく機械が読む形にした**（盤面の空きキーの色）
- **複素除算の番人の床が、何を数えているかを書き直した**（0 と 0 を比べていた升が
  128 あった。**検出力の数ではない**）
- **文書の嘘を直した**: リリースの順（マニュアルを作るのは本番展開の前）・
  「最新は 0.7.0」・検証設計書の未決の書き残り・古い状態表の 4 件と、
  **0.9.5 に「サイト内の PDF 配信は 0.9.6 で撤去する」と書いた 6 か所**
  （**向きが逆になった。撤去しない**）
- **残りの作業の棚卸しを、台帳ではなく現物に当てて作り直した**

## 0.9.5 — 2026-09-19

**0.9.4 を出したあとに届いた指摘 2 件を直しました**（大きな複素数の割り算、
iPhone でマニュアルの PDF から戻れない）。**あわせて、マニュアルの誤りを
1 つ直しました。**

### 大きな複素数の割り算（0.9.4 で入った不具合）

- **0.9.4 の直しで、別の形が壊れていました。** 実部と虚部の桁が 308 桁ほど
  離れた複素数を割ると、**小さい方が 0 になっていました**（エラーは出ません）。
  `(10^308 + 10^-20 j) ÷ 1 − 10^308` が **0** でした（**正しくは 1e-20j**。
  0.9.3 では正しく出ていました）。`÷ (−1)`・`÷ 2`・`÷ j` でも同じでした
- **割る数の側でも、同じことが起きていました**（外部の報告とは別に、こちらで
  測って見つけました）。桁の離れた複素数で割ると、答えの小さい成分が 0 に
  なるか、桁を落としていました。**その一部は 0.9.4 より前から在ったもので、
  それも直りました**
- **0.9.4 で直したもの**（途中で範囲を超えて 0 になる・Math ERROR になる・
  とても小さい数の桁落ち・`tan`）は**直ったまま**です

### マニュアルの PDF

- **リンク集の PDF 3 冊は、GitHub にあるこの版のリリースから開くように
  しました。** 0.9.4 では、iPhone でホーム画面に追加したアプリから開くと、
  **電卓に戻れなくなっていました**——**0.9.4 で入れた「保存の扱いにする」
  直しは効いていませんでした**
- **この直しは iPhone の実機でまだ確かめていません**

### マニュアルの誤り

- **詳細マニュアルの「読み上げ」の章が、画面の名前を 14 個としていました。**
  「マニュアル」の画面は 0.9.4 で無くなっていたので、**13 個**です

### 内部の変更

- **複素除算を、途中の値を「仮数と指数の組」で持つ形に変えた。** 0.9.4 の
  「2 の冪で寄せてから割る」は、寄せる先の帯が 1 つしか無く、**桁の離れた
  2 つの成分を同時に入れられなかった**——**0.9.4 の項の「精度を足すのでも
  削るのでもなく」は、この形では成り立っていなかった**
- **複素除算の境界の golden を成分ごとの判定に変え、成分の桁が離れた行を
  足した。** 0.9.4 の表は大きい方の成分との比で判定していたので、**小さい
  成分が消えても許容の内に入っていた**
- **サイト内の PDF の配信（`/manual/`）は残した。アプリはもう指していない。**
  出荷前の夜にリリースの経路を動かさないためで、**撤去は 0.9.6**
- `docs/deploy.md` に、0.9.3 で入れ替える前の並び（「マニュアルが落ちても
  本番は出る」）が残っていたのを直した

## 0.9.4 — 2026-09-18

**0.9.3 を出したあとに見つかった不具合を直しました。** 利用者の実機で 2 件
（Convert の符号、iPhone でマニュアルの PDF を開くと戻れない）、外部の報告で
1 件（大きな複素数の割り算）です。**あわせて、マニュアルをリンク集から直接
開けるようにしました。**

### Convert の符号

- **答えが負のとき、続けて数字を打つと符号が残っていました。** `1` `−` `2` `=`
  の答え **−1** のあとに `5` を打つと **−5** になっていました（**正しくは 5**）。
  `.` なら `−0.5`、`000` でも同じで、**為替でも同じ**でした。**0.9.3 で入れた
  「答えのあとの数字は新しい値」の抜け**です——**数と符号は 1 つの値なのに、
  符号だけが前の答えのまま残っていました**
- **【DEL】で値を消し切ったあとも、同じことが起きていました。** `1` `−` `2` `=`
  `DEL` `5` が **−5** になっていました。**欄が空になったら符号も落とします**
  ——**見えないものは持ちません**。**数が残っているあいだは符号はそのまま**です
  （`−1.5` の【DEL】は `−1.`、もう一度で `−1`）

### 大きな複素数の割り算

- **答えが出せるのに、エラー無しで 0 になっていました。** `10^308 ÷ (10^308 +
  10^308 j)` は **0.5 − 0.5j** ですが、**0 が出ていました**——**計算の途中で
  範囲を超え、それが画面に出ないまま答えが潰れていました**。**逆に、答えが
  出せるのに Math ERROR にしていた形**もありました（`z ÷ z` が 1 にならない）
- **とても小さい数（非正規化数）でも、桁が落ちていました。** 割り算の途中で
  精度が失われ、**相対で 1 万分の 2 ほどずれていました**
- **`tan` も同じ経路を使っています。** 虚部の大きい `tan`（`tan(1 + 710.4j)`
  など）が Math ERROR になっていたのも直ります
- **範囲を本当に超える計算は、これまでどおりエラーです**——**エラーにする
  範囲は広げても狭めてもいません**

### マニュアルとリンク集

- **マニュアルの PDF が、下部のリンク集から直接開けるようになりました。**
  リンク集は **GitHub（@terapyon）／この版の PDF 3 冊／ライセンス／検査結果**
  の 6 つです。**0.9.3 にあった「マニュアル」の画面は無くなりました**——
  **その URL（`#manual`）を開くと、電卓が出ます**
- **★ iPhone でホーム画面に追加したアプリから PDF を開くと、電卓に戻れなく
  なっていました。** **PDF を「保存（ダウンロード）」の扱いに変えました**
  ——**アプリの窓はそのまま残るはず**です。**この直しは iPhone の実機で
  まだ確かめていません**（開発機に iOS が無いためです）
- **PDF の開き方が変わりました**: **保存（ダウンロード）されます**。端末に
  よっては別の窓で開きます。**GitHub・ライセンス・検査結果は、これまでどおり
  新しいタブ**です
- **下部のボタンに下線を引きました。** 0.9.3 でリンクからボタンに変えたときに、
  押せるものに見えなくなっていました

### 内部の変更

- **複素除算を、2 の冪で寄せてから割る形にした**（中間の値が範囲を超えたり
  桁を失ったりしないようにするため。**精度を足すのでも削るのでもなく、
  安全な帯へ移すだけ**）。**分母の形・符号・大きさを分けた 572 件の検査**を
  検査側に置き、**境界の golden を `testdata/` に足した**
- **本番のスモークを 1 つ強くした**——**配った PDF に保存の指示が付いている
  ことまで見る**（**付いていなければ、Pages Functions が拾われていない**）。
  **伝播待ちと本当の欠落を分けて出す**
- **`#manual` の画面と、その経路・画面名・検査を落とした**（画面名は 14 → 13）
- **hypot の註に範囲を付けた**（「溢れない」ではなく「**真の大きさが収まる限り
  溢れない**」）。**極形式の表示が `inf ∠ 45` にならない門は、その区別に
  懸かっている**

## 0.9.3 — 2026-09-18

**マニュアルが画面から開けるようになりました。** 下部のリンク集から
「マニュアル」を選ぶと、この版の PDF 3 冊が並びます。**あわせて、入力の
決まりを 3 つ直し、新しい版のお知らせを見落としにくくしました。**

### 下部のリンク集とマニュアルの画面

- **画面のいちばん下が「CalcArc 0.9.3 について」のボタンになりました。**
  押すとリンク集が開き、**GitHub（@terapyon）／マニュアル／ライセンス／
  検査結果**の 4 つが並びます。**これまで下部に出ていた `@terapyon` は、
  リンク集の GitHub の項目に入りました**
- **リンク集は【閉じる】・Escape キー・外側を押す、のどれでも閉じられます。**
  リンク集が開いているときの Escape は、リンク集を閉じるだけです（【AC】は
  働きません）
- **「マニュアル」を選ぶとマニュアルの画面になります。** この版の PDF が
  3 冊（簡易マニュアル・詳細マニュアル・Quick Guide）並び、新しいタブで
  開きます。**タブは 4 つのままです**——マニュアルはタブではありません
- **存在しないマニュアルの PDF の URL は、404 を返すようになりました。**
  これまでは電卓の画面が出ていて、利用者には「開けた」ように見えていました

### 入力の決まり

- **閉じた括弧のあとの【DEL】が、直前の【)】を取り消して組を開き直します。**
  【(】【(】【3】【)】【DEL】は【(】【(】【3】に戻ります。続けて閉じた【)】の
  ぶんだけ遡り、演算子や数字を挟むとそこで止まります。**これまでは外側の
  【(】が消えていました**
- **開いている【(】が無いときの【)】は、押せなくなりました。** 【3】【+】【4】の
  あとの【)】などです。**これまでは押せて Math ERROR になっていました**
  ——**必ず間違いになる打ち方なので、押せないほうが正しい**という判断です
  （0.9.2 で「数を黙って捨てるキーを拒む」ようにしたのと同じ線です）
- **Convert で、答えのあとに数字を打つと新しい値として始まります。**
  【1】【÷】【3】【=】のあとに【5】を打つと `5` です。**これまでは `1/35` に
  なっていました**。演算子なら、これまでどおり答えの続きです

### 更新のお知らせ

- **開いたままでも、6 時間ごとに新しい版を探すようになりました。**
  **これまでは、ページを読み込んだときにしか探していませんでした**——
  開きっぱなしのタブには、新しい版が何日も届かないことがありました。
  **新しい版が届くまで最大 6 時間かかります**（承知のうえでこの間隔にしています）
- **お知らせを閉じても、60 分後にもう一度出ます。** 新しい版があるあいだは
  何度でも出ます。**【閉じる】はそのままです**——消えないようにしたのでは
  なく、**もう一度知らせる**ようにしました

### マニュアル

- **簡易マニュアル（日本語・英語）と詳細マニュアル（日本語）を、0.9.3 の
  内容に合わせました。** リンク集とマニュアルの画面、上の入力の決まり 3 つ、
  更新のお知らせの 2 つ、Escape の行き先を書いています
- **PDF は画面からも開けるようになりました。** これまでどおり GitHub の
  Releases にも添付されます——**緊急の配り直しをした版だけは PDF を持たない**
  ので、そのときは Releases から取ってください

### リリースのしかたが変わりました

- **マニュアルの PDF が作れなければ、本番にも出しません。** これまでは
  本番に出したあとで PDF を作っていて、PDF が作れなくても配信は進んでいました。
  **PDF が画面から開くものになった以上、PDF の無い版を配ると、押せるリンクの
  先が 404 になります**——**マニュアルが最新であることはリリースの絶対的な
  条件である**、という方針に合わせました

### 内部の変更

- **リリースの順序を入れ替えた**（マニュアル → 本番展開 → 証拠）。
  **証拠が「落ちてもよい」と許すジョブは 0 になった**
- **マニュアルの数値の上限表を、製品の定数に縛る番人を置いた**
- **衝突マーカーの検査を、`docs/` だけでなく追跡下の全ファイルへ広げた**
- **年利の 2 つの規則（上限 100%・小数 4 桁）に名前を付け、文字列の経路と
  式の経路で同じ値であることを検査するようにした**（挙動は変えていない）
- **賞与の例と nCr の制限を、golden と検査で裏づけた**
- **WebKit の検査で、ページを開く操作が内部エラーで落ちたときだけ 1 度だけ
  やり直すようにした**（判定・しきい値・待ち時間は緩めていない）
- **リポジトリの根の `functions/` を lint の対象に入れた**

## 0.9.2 — 2026-09-17

**外部からの計算品質監査をきっかけに、計算と入力の欠陥をいくつか直しました。**
演算子を押し直したときの計算、ローンの月額の上限、Convert の `=`、履歴の呼び戻しが
対象です。**あわせて、画面の数を黙って捨てるキーを拒むようにし、マニュアルを
0.9.2 の内容に合わせました。**

### 数を黙って捨てないキー

- **画面に出ている数（打ちかけの数も、確定した値も）を黙って捨てるキーは、
  押せなくなりました。** たとえば `4` `√` で答え `2` が出たところへ続けて `5` を
  打つと、これまでは `2` を黙って捨てて `5` から新しい数が始まっていました。
  いまはそのキーは押せません——**盤面で押せなく見えるのは結果であって、直したのは
  「黙って捨てていた」ことです。** 続けられるのは演算子・`=`・`)` など、いまの値を
  そのまま使う打鍵です
- **`+/−` は数字を打ったあとに押します。** 数字より先に `+/−` を押すと
  （たとえば `AC` の直後）、その符号反転そのものが「いまの値」になるので、続けて
  数字を押すとその値を黙って捨ててしまいます——**そのキーは押せません**

### 演算子を押し直したときの計算

- **演算子を打ったあと、別の演算子で打ち直す（訂正する）と、最初からその演算子を
  打ったときと同じ答えになるようにしました。** これまでは先頭の演算子を差し替える
  だけで、下にある演算子との優先順位や、すでに畳んだ値が戻っていませんでした——
  `2` `+` `3` `+` `×` `4` `=` が **20** になっていました。**正しくは 14 です**
  （`2` `+` `3` `×` `4` `=` と同じ）
- **`(` を `DEL` で消すと、その前の演算子の直後に戻るようにしました。** `3` `×`
  `(` `DEL` `=` は、これまで **0** でしたが、`3` `×` `=` と同じ **9** になります
  （`(` の直後にすでに値がある——たとえば `√` の答え——ときは、その値に戻ります）

### ローンの月額の上限

- **ローンの月額（賞与額も同じです）が 10 億円を超える入力は、Math ERROR に
  するようにしました。** 10 億円を超える月額は、実際の計算が理論値から 1 円を
  超えてずれることがある範囲だったためです。**10 億円ちょうどまでは、これまでどおり
  答えが出ます**

### Convert

- **為替でも、単位換算と同じように式が打てるようになりました。** `100` `÷` `7` `=`
  のように、割り切れない式を打っても、`=` を押す前から答えの行に金額が出ます。
  **これまでは、`=` を押すまで答えの行に Math ERROR が出ていました。** `=` を押すと
  値が有効数字 10 桁に丸まり（`100` `÷` `7` なら `14.28571429`）、その丸めた値で
  答えが出ていました
- **`=` は式を値にまとめるだけで、桁を落とさなくなりました。** 割り切れない値は
  分数のまま（`1/3` など）値の欄に残り、続きの計算にもその正確な値を使います。
  **これまでは表示用に丸めた数で書き戻していた**ため、割り切れない計算のあとに
  `=` を押すと途中の桁が消え、また 100 億（1e10）以上の値は書き戻せず `=` が
  ずっと押せないことがありました

### 履歴

- **履歴から答えを入力に戻すと、違う数になることがありました。** マイナスの
  指数表記（`-1.5e12` など）を戻すと、`1e12` になっていました。**これは 0.9.2 より
  前からあった欠陥です。** このような行は、いまは「入力に戻す」ボタンが出ません
- **`(` を `DEL` で消したあと計算を終えると、履歴に残る式が実際に計算した内容と
  食い違うことがありました**（`2` `×` `(` `π` `DEL` `+` `1` `=` の答えは
  `7.283185307` なのに、記録された式は `2 × ( π + 1` でした、など）。いまは
  記録される式が、実際に計算した内容と一致します

### マニュアル

- **簡易マニュアル（日本語・英語）と詳細マニュアル（日本語）を、0.9.2 の内容に
  合わせました。** 入力の規則、Convert の `=`、為替、税のタイミング、ローンの
  最終回、賞与払い、上限の一覧表、既知の制限、画面幅の検査が実際に見ている範囲、
  を見直しています

### 内部の変更

- **`docs/` に衝突マーカーが紛れ込んでいないかを見張る検査を置いた**
- **利用者向けの変更があるリリースで、マニュアルが 1 行も変わっていなければ
  赤くなる検査を置いた**
- **重量級の検査を強めた**: 演算子の押し直しを、独立に評価した期待値と突き合わせる
  シャードを足した／ローンの月額が理論値の切り捨てから 1 円以内に収まることを、
  円境界ちょうどの golden で検査するようにした／判定名を「完全に正しい」から
  「採録ケースで一致」「採録ケースで表示精度内」に改めた（誤差の閾値は変えていない）

## 0.9.1 — 2026-09-12

**複利の期間に `月` を付けて打てるようになりました**（周期が月ごとのとき）。
**あわせて、iPhone の Safari と同じエンジン（WebKit）でも、毎回の検査で動きを
確かめるようにしました。**

### 複利の期間の単位

- **周期が月ごとのとき、期間に `月` を付けて打てます。** 1 か月が 1 期です
  （`1` `年` `6` `月` で 18 期）。**これまでは `月` のキーが出ていても、押すと
  Math ERROR になっていました**
- **周期が半年ごと・年ごとのときは `年` だけです。** `月` のキーは出ません
  （その場所は押せない空きになります）
- **`月` を付けて打った期間は、周期を月ごとから変えると空に戻ります。** 打った
  月が、新しい周期では 1 期にならないためです。`年` で打った期間は残ります
- ローンの期間は、これまでどおり `年` と `月` です

### 更新のお知らせ

- **新しい版のお知らせが出ているあいだ、ページの下にお知らせの高さぶんの
  空きができます。** これまでは、お知らせがいちばん下のキーに重なり、
  お知らせを閉じるまで押せないことがありました。**お知らせの場所と見た目は
  変わりません**

### マニュアル

- **簡易マニュアル（日本語・英語）と詳細マニュアル（日本語）を作りました。**
  0.9.1 から、リリースのたびに PDF にして GitHub の Releases に付ける仕組みにしました。
  PDF が作れなかったときは、そのことを Release に書きます（サイトの公開は
  止めません）
- **README の画面写真を、いまの画面で撮り直しました**

### 説明の直し

- README の「Finance と Data Scale の入力欄に打った式は、途中で丸めない」を
  「**Finance の入力欄に打った式は、途中で丸めない**」に直しました。Data Scale の
  欄には式を打てません（英語版も同じです）

### 内部の変更

- **WebKit（iPhone の Safari のエンジン）でも、E2E を毎回の CI で回すようにした。**
  オフラインの確かめ 1 本は、Playwright の WebKit ではネットワークを切れないので、
  理由を書いて WebKit だけ外した。WebKit の狭い画面は、実在する iPhone の寸法
  （375×812）で測る
- **低い画面（iPhone の Safari の見える高さ 4 つ）で、どの画面でも、押せるキーが
  すべてスクロールで見えて押せることを確かめる検査を足した**（お知らせが出ている
  場合も）
- **画面の URL 13 個が、ほかの画面に倒れず、その画面を開くことを見張る検査を足した**
- **E2E がこの端末の外へ出ようとしたら落ちるようにし、E2E では Service Worker を
  止めた。** WebKit で、為替の差し替えをすり抜けて本物のレートを取っていたため
- **E2E が落ちた日の報告書（画面写真つき）が CI に上がるようにした。** これまで
  報告書は 1 度も作られていなかった
- **マニュアルの PDF を CI とリリースで作るようにした。** リリースでは本番展開の
  あとで作り、失敗しても配信を止めない。マニュアルのキー名・画面写真・画面幅の
  文を、画面と E2E の寸法に突き合わせる検査を置いた
- **参照実装と期待値に、複利の期間の `月` の行を足した**（`12月` → 12 期、
  半年ごと・年ごとではエラー、など）
- **重量級の検査を強めた**（1.0 に向けた「独立検証の穴」）: 期首の積立の組み合わせの
  シャードを足した／Shift の奥のキーまで押せるかを確かめるようにした／キー列の綴りを、
  打った入力と表示の突き合わせでも見張るようにした
- **数値の決まりの文書（`docs/numerical-policy.md`）の古い記述を直した**
  ——「ENG はモード」「積立は期末」と書いていた（0.4.0 から ENG は覗くための
  キーで、0.9.0 から期首も選べる）

## 0.9.0 — 2026-09-10

**複利で、積立を期の終わりに入れるか初めに入れるかを選べるようになりました。**
金融計算の複利で `期末` と `期首` を切り替えられます。**既定は期末で、
これまでと同じ入力なら答えは変わりません。**

**あわせて、金融計算の盤面を見直しました。** 上の段は「何を求めるか」、
下の段は「そのために入れるもの」と、見て分かるようにしました。

### 積立の位置（期末 / 期首）

- **複利の 3 つの計算——複利残高・必要積立額・必要年数——のどれでも選べます**
- **選ぶのは下の段の `方式` から。** 押すと、周期の `月` / `半年` / `年` と
  並んで `期末` / `期首` が出ます
- **`期首` を選ぶと、その期に入れた積立にも、その期の利息が付きます。**
  一括で預けるだけ（積立額 0）のときは、どちらを選んでも答えは同じです
- **入力済みの欄には `方式 月ごと・期末` のように、周期と一緒に出ます**
- **選んだ位置は、次に開いたときも残ります**（周期・税の有無と同じです）

### 計算のしかたの説明

- **複利の画面に、計算のしかたを 2 行で常に出すようにしました。**

  > 各期の利息を 1 円未満切り捨て、積立は選んだ位置（期末／期首）に行います。
  > 税は国税と地方税を別々に切り捨てます。

  これまでは複利の画面にも、ローン向けの 1 文（返済額についての但し書き）が
  出ていました。**ローンの画面はこれまでどおり**「実際の返済額は金融機関の
  計算方法により異なります。」です

### 盤面

- **上の段（計算の種類）のキーの名前を、求めるものの名前に揃えました。**
  「月額」「期間」は下の段の入力キーと同じ名前で、**求めるのか入れるのかが
  見分けにくかった**ためです。**読み上げの名前は変わりません**

  | これまで | 0.9.0 |
  |---|---|
  | 月額 | 返済月額 |
  | 借入可能 | 借入可能額 |
  | 期間 | 返済期間 |
  | 複利 | 複利残高 |
  | 必要積立 | 必要積立額 |
  | 必要年数 | 必要年数（変わらず） |

- **下の段（入力する項目）のキーを低くしました。** 高さの違いで、上の段が主、
  下の段が補助と見て分かるようにしました。**文字の大きさは変えていません**
- **ローンの `ボーナス` のキーを `賞与` にしました**（低くしたキーに収めるため）。
  **読み上げの名前と、入力済みの欄の見出しは「ボーナス」のまま**です
- **複利の `周期` のキーを `方式` にしました。** 周期と積立の位置の 2 つを
  選ぶキーになったためです。入力済みの欄の見出しも「方式」です

### 内部の変更

- **参照実装と期待値に「積立の位置」を足した。** 期首の行を足し、
  **既存の行は 1 つも動いていない**（既定が期末のままであることの裏付け）
- **CI の全ジョブに時間制限を置いた**（実測した最悪の所要時間から決めた）
- **GitHub Actions の action 6 つを、Node 24 で動く版へ上げた。**
  `cloudflare/wrangler-action` と `jetli/wasm-pack-action` の 2 つは据え置き
  （前者は本番配信でしか走らず手前で確かめられないため、後者は新しい版が無いため）
- **`check:boundary` が、まだ `git add` していないファイルも読むようにした**

## 0.8.0 — 2026-09-05

**計算履歴が残るようになりました。** Scientific で `=` を押した計算が端末に
保存され、あとから見返して答えを入力に戻せます。**記録は既定で入**で、画面から
止められます。

**あわせて、画面ごとの名前が付きました。** タブに出る名前が画面ごとに変わり、
画面読み上げで使うときに現在地が伝わるようになりました。

### 履歴

- **`=` を押した計算が、式・答え・角度モードの 3 つで残ります。** 新しいものが
  上に積まれ、**50 件を超えると古いものから消えます**
- **開くのは `Shift` → `hist`。** Scientific のキーの 2 面目にあります
  （履歴があるのは Scientific だけです）
- **押すと、その答えが入力に入ります。** 手で打ち直したときと同じに扱われるので、
  そのまま続きの計算ができます。**入るのは答えだけ**で、式は入りません。
  **打ちかけていたものは消えます**（`AC` を押してから入れ直す動きです）
- **`AC` では履歴は消えません。** 消すのは履歴の画面から——1 件ずつの `×` か、
  一覧の下の `すべて消す`
- **記録を止められます。**「今後の計算を記録する」を切ると、それ以降は積まれません。
  **すでに記録した分は消えません**——**切ることと消すことは別です**
- **入力に戻せない計算もあります。** エラーで終わった計算と、答えをキーで
  打ち直せない計算（虚数・極形式・60 進、および桁数が入力の上限を超えるもの）は、
  一覧に出ますが押せません。
  **色が付くのはエラーのほうだけ**です——「計算が失敗した」と「入力に戻せない」は
  別の事実だからです

### 保存先

- **端末の `localStorage` に保存します。外部へは送りません。**
  ブラウザの保存データを消すと、履歴も消えます

### 画面の名前と読み上げ

**目で見える変化は、タブに出る名前だけです。** 画面の中——キーの位置、文字の
大きさ、計算結果——は 1 つも動いていません。**変わったのは、画面読み上げで
使うときに何が伝わるか**です。

- **タブの名前が、いま開いている画面の名前になりました。**
  `関数電卓 | CalcArc`、`長さの換算 | CalcArc`、`LLM のメモリ | CalcArc` の
  ように、**13 個の画面それぞれに名前が付きます**。ブックマークや閲覧履歴に
  残る名前も同じです
- **画面ごとの見出しが入りました。** 目には見えず、場所も 1px も取りませんが、
  読み上げでは見出しとして読まれます。**履歴の画面では「関数電卓」の下に
  「履歴」が入り**、画面とその中の面が階層になります
- **画面を切り替えると、切り替えたことを読み上げます。**
  「金融計算に切り替えました」のように伝えます。**読み込んだ直後は言いません**
  （まだ切り替えていないため）。読み上げ中の発話に割り込むことはありません
- **更新のお知らせが読み上げに届くようになりました。** 以前は新しい版が来ても
  読み上げが鳴らないことがありました——知らせる場所を、知らせる中身と同時に
  作っていたためです。**見た目と操作は今までどおり**です

## 0.7.0 — 2026-09-02

**空きのキーの見せ方を変えました。** 押せるキーの位置や数は変わりません。

### 画面

- **将来キーが入る空きに出ていた `—` を消し、薄い箱にしました。**
  記号が出ていると「そういうキーがある」ように見えるためです。
  **押せないキーの見せ方、キーの並び（5 × 5）、キーの数と位置は
  変わりません。**

### 画面読み上げ

- **押せない理由を言い分けるようになりました。** 「この電卓では使えません」
  （その計算では使わないキー）と、それ以外（いまは押せないが、項目や
  モードが変われば押せるキー）を区別します。**見た目は今までどおり
  1 通り**で、区別を持つのは読み上げだけです

## 0.6.0 — 2026-08-29

**内部の変更のみ。画面の動作に変化はありません。**

### 型と検査の強化

- **WASM 境界の結果型を判別可能 union にした（結果型 12 個、境界の関数 15 本）。**
  値とエラーが同時に存在する状態を、型が表現できなくなった。Rust 側は
  `CalcResult<T>` を潰さず、内側 tag の `Outcome<T>` として serde に渡す
- **`calcarc-wasm` に `deny(clippy::unwrap_used, clippy::expect_used)` を追加。**
  これまで `calcarc-core` にしか無かった
- **Python 参照実装に mypy を導入し、CI で実行**（`*_ref.py` の範囲）
- **`ruff` の規則を 8 群に拡張**（`E` `W` `F` `I` `UP` `B` `SIM` `PT`）
- **文字列を union にキャストしていた 13 か所を、検証つきの parse に置き換え**

### 実行時の検査を追加

- **`web/src/calc` が React と `ui/` を import していないことを CI で検査**
- **参照実装の各関数に、Rust と別手順である／別手順にできない旨を宣言**
  （複利・ローン・式評価の 28 関数）
- **重量級コーパスのハーネスが、境界の新しい形を開けることを検査。**
  tag を外して比較側へ渡す変換と、**知らない形（`kind` が無い／未知の `kind`
  ／`code` の無い `error`）が来たら例外で落ちること**を固定した

### 名前と仕様の整理

- `Buffer::push_sexagesimal_separator` → `try_push_sexagesimal_separator`
  （失敗しうる操作であることを名前で示す）
- `is_tan_pole` が複素数引数では極を判定しないことを、仕様と検査に明記
- `convert::convert` のスタッターを、`docs/api-style.md` に例外として明文化

### 依存とツール

- **`@biomejs/biome` を `2.5.8` に固定**（範囲指定をやめた）

## 0.5.0 — 2026-08-27

**押しても何も起きなかったキーが、押せない見た目になった。**

「LLM のメモリ」「データ量」「データ転送」の 3 つには
`(` `)` `÷` `×` `−` `+` `=` が並んでいた。**この 3 つは式を打つ作りに
なっていない**ので、押しても何も起きなかった。**淡く表示して、押せない
ことが見て分かる**ようにした。**キーの位置も大きさも変えていない**ので、
盤面の並びは今までどおりである。

**淡いキーには 2 つの意味がある。** `DEL` や `K` `M` `G` は「**いまは**
押せない」——打ち始めれば押せるようになる。上の 7 つは「**この電卓には
無い機能**」で、何をしても押せるようにはならない。

### 直したもの

- **為替で、通信が切れても画面が変わらなかった。** 為替を開いたまま
  機内モードにしたり電波の届かない場所に入ったりしても、`Offline` の
  表示が出なかった（開く前から切れていた場合だけ出ていた）。**切れたら出て、
  戻ったら消える**ようにした。**換算は今までどおり続く**——手元に保存された
  レートで計算できる。

### 内部の変更

**利用者から見える違いは無い。**

- 画面が 1 画面に収まるかを見る検査を、6 画面から **11 画面**に広げた
- キーのラベルが重ならないこと・器からはみ出さないことを、機械が見るようにした
- 為替の検査が本物の為替サービスに出ないよう、既定で塞いだ
- 開発用の道具を `tools/` にまとめ、時間のかかる検査を毎回の CI に載せた
- コードの註と公開文書にあった事実の誤りをいくつか直した

## 0.4.1 — 2026-08-25

**利用者から見える変更は無い。** 画面に出る版数が `0.4.1` になるだけである。

この版は**出荷の仕組みそのもの**を変えるために切った。これまで本番へは
main への push が届いていたが、いまは **`v*` タグだけが本番への扉**で、
タグを打つと版数の一致・CI 全部・重量級コーパス（数万件を独立実装と照合）を
通してから配信される。**通らなければ配信されない。**

配信のたびに、その走行が作った証拠が GitHub Release に残る——各検査の結論、
重量級の報告書、そして**実際に配った物**である。

## 0.4.0 — 2026-08-25

**画面の直しが 3 つ。** 計算そのものは 1 つも変わっていない。

### 変えたもの

- **フッタの字を大きくした。** 8px では読めなかったので **12px** にした
  （1.5 倍）。**字を大きくしたぶん、免責の文言を短くしている**
  （「計算結果は無保証です。重要な判断の根拠にしないでください。」→
  「計算結果は無保証です。」）。**無保証であることは変わらない**——
  重要な判断に使わないでほしいという但し書きは
  [README](README.md) と、Finance の画面内の注記にある
- **虚数の `j` を数の後ろに置くようにした。** `j2` → **`2j`**、
  `3+j4` → **`3+4j`**。打っている途中の表示も同じで、`3` `j` は `3j` と出る。
  **数は変わっていない。書き方だけである**
- **`ENG` が「覗くためのキー」になった。** 押すと今の表示が工学表記になり、
  **次に何かを押せば通常の表記に戻る**。以前は一度押すと以後の計算結果も
  ずっと工学表記だった。`°'"`（60 進表示）と同じ使い勝手になる
  - **`ENG` は憶えなくなった。** 再読み込みしても工学表記では始まらない
    （角度と Rectangular / Polar はこれまでどおり憶える）

## 0.3.1 — 2026-08-25

**為替換算を足した。** CalcArc が外部と通信する**唯一の機能**である。

### 足したもの

- **単位変換に「為替」が増えた。** 16 通貨（JPY / KRW / VND / USD / EUR / GBP /
  CHF / CNY / THB / SGD / HKD / TWD / AUD / CAD / INR / BRL）。桁は通貨ごとに
  決まっていて、**円・ウォン・ドンは小数点以下を出さない**
- **どのレートを使ったかが画面に出る。** レートの日付を換算結果の近くに
  常に表示する——**古いレートを黙って使わない**
- **レートが無くても電卓は壊れない。** 一度も取得できていなければ「レートが
  ありません」と出るだけで、他の 7 カテゴリはこれまでどおり動く

### 通信について

- **入力した金額は 1 バイトも外へ出ない。** 取ってくるのは**レート表 1 枚**
  だけで、掛け算はこの端末で行う
- **起動しただけでは通信しない。** 「為替」を開いたときに初めて取りに行く
- **取ってきたレートは端末に置くだけで、どこへも配らない。** 多くとも 1 日に
  1 回しか取りに行かない
- レートの提供元は [Exchange Rate API](https://www.exchangerate-api.com)。
  API キーは使っていない

詳しくは [docs/base-spec.md](docs/base-spec.md) の
「なぜ通信しても Privacy と両立するのか」にある。

## 0.3.0 — 2026-08-20

**計算機の並べ方を変え、電卓を 3 つ足した。** 既存の計算は 1 つも変わっていない。

### 足したもの

- **単位変換が使えるようになった。** `Convert` タブに **7 カテゴリ・63 単位**。
  長さ / 質量 / 温度 / 面積 / 体積 / 速さ / データ量。数字を打って単位を 2 つ
  選ぶだけで、**電卓と同じ打ち方**で換算できる
  - **温度は目盛りの向きも扱う。** `°C` と `°F` と `K` を正しく行き来する
  - **基準が 1 つに決まらない単位は、名前に基準を書いた。** `gal(US)` と
    `gal(Imp)`、`カップ(200mL)`、`畳(1.62m²)`——**裸の `gal` や `畳` は出さない**
  - **データ量は 10 進と 2 進を区別する。** `GB`（10⁹）と `GiB`（2³⁰）は別の単位
- **`Scale` に「LLM のメモリ」が増えた。** パラメータ数と量子化のビット数から、
  モデルを載せるのに要るメモリ量を出す
- **`Scale` に「データ転送」が増えた。** 帯域幅と時間から転送量を出す。
  **`kbps` の `k` は 1000** である（1024 ではない）

### 変えたもの

- **タブが 4 つになった。** `Scientific` / `Convert` / `Scale` / `Finance`
- **Data Scale が `Scale` の下に入った。** 計算の中身は同じ
- **`#data-scale` のブックマークは効かなくなった。** 開くと Scientific が
  出る。新しい場所は `#scale/data-scale`
- **カテゴリ名に英語を併記した。** `長さ Length` のように出る。
  `Convert` の `データ量 Data Size`（単位どうしの換算）と `Scale` の
  `データ量 Data Scale`（規模の計算）は**日本語が同じ**で、英語が無いと
  見分けられなかった
- **カテゴリを選ぶ欄の見た目を整え、`Scale` の盤面が画面幅いっぱいに戻った。**
  幅の狭い端末で盤面が縮んでいた

## 0.2.1 — 2026-08-17

表示の修正 1 件と、機能の追加 1 件。計算は 1 つも変わっていない。

### 直したもの

- **画面幅 360px で関数列のボタンが横にはみ出していた。** Android に多い幅で、
  横スクロールが出ていた。キーの大きさ（44px）は保ったまま、キーとキーの隙間を
  詰めて収めた。320px は対象外のままで、はみ出しは消えるが隙間が無くなる

### 足したもの

- **設定を憶えるようになった。** リロードしても、タブを移って戻っても、選んだ
  設定がそのままになる。憶えるのは次の 8 つだけ:
  - Scientific: 角度の単位（DEG/RAD）・極形式・ENG 表記
  - Data Scale: データ型・主に表示する単位系
  - Finance: 計算の種類・年あたりの期数・源泉徴収の有無
- **打った式・途中の数字・答は憶えない。** リロードすれば今までどおり消える。
  端末に保存されるのは上の 8 項目だけで、計算の内容は 1 バイトも保存しない

## 0.2.0 — 2026-08-16

最初のベータ。まだ広くは告知していない。

### Scientific

- **Shift キーと第 2 面を設けた。** 以降に足した関数の多くはここに乗っている
- **入力を増やした** — 後置 `j`（数字を打ったあとに `j` で実部と虚部を切り替える）、
  `Exp`（指数入力）、`000`（3 桁まとめてゼロ）、そして**確定前の式をそのまま出す
  エコー行**
- **関数を実数に閉じた。** 実数の答が一意に決まらない入力（`sqrt(-4)`、`ln(0)`、
  `asin(2)` など）はエラーを返す。複素数は入力・四則・表示の機能として残る
- `1/x` `xʸ` `eˣ` `ln` `log` `asin` `acos` `atan` を追加した。`xʸ` は右結合
  （`2^3^2 = 512`）で、このエンジンで右結合はこれだけである
- **`n!` `nPr` `nCr` を追加した。** 非負整数の上でのみ定義する。優先順位は
  `× ÷` より先、`xʸ` より後で、左結合
- **60 進の入出力（`°'"`）を追加した。** 経過時間と角度の両方に使える。
  値は 10 進の実数のままで、**四則演算は 1 つも足していない**——`1:30 + 2:45` は
  `1.5 + 2.75` である。度分秒で入れた角度をそのまま `sin` に渡せる
- **ENG（工学表記）と 3 桁カンマを追加した**

### Finance

- **複利を追加した。** 一括預入と毎月積立を 1 つのモードで扱う。税（源泉分離
  課税）の有無を選べる
- **複利の逆算を追加した。** 目標額から必要な積立額、または必要な年数を求める。
  **手取りは期数について単調でない**ため、必要年数は二分探索ではなく前進で解く
- タブ名を Loan から **Finance** に変えた（ハッシュも `#finance`）。ローンは
  Finance の中の 1 機能になった
- **全項目で四則演算が打てる**ようになった

### 画面

- **Data Scale と Finance の入力を、Scientific と同じ盤面に統一した。** それまでは
  フォーム入力で、電卓としての操作感が 3 タブでばらばらだった
- **新しい版が配られたことをトーストで知らせるようにした。** 押したときだけ
  切り替わる——見ている画面の下で勝手に版が入れ替わることはない
- **表示エリアの高さを固定した。** 入力・確定で盤面が上下しなくなった
- **3 タブの高さを揃えた。** タブを切り替えても縦が動かない
- タブのラベルが 2 段になるのを直した
- Finance の 4 文字ラベル（「借入可能」など）がボタンから溢れるのを直した
- **`n!` `nPr` `nCr` の置き場所を `(` `)` `+/−` の第 2 面に移した。** 以前は
  数字 `7` `8` `9` の裏にあり、Shift 中に数字が打てなかった
- **版数・リポジトリへのリンク・免責を全タブの下部に出した**

### 開発

- 版数の出所を `web/package.json` に一本化し、`Cargo.toml` との一致を CI が検査する
- デプロイ後スモークの向け先を公開 URL（`calc.terapyon.net`）にした
- Issue テンプレート（不具合の報告 / 質問・要望）を追加した

## 0.1.0 — 2026-08-13

**タグは打っていない。** [base-spec §50](docs/base-spec.md) の Definition of Done
14 項目を満たした時点で、記録は
[docs/definition-of-done.md](docs/definition-of-done.md) にある。

Scientific（四則・複素数・極形式・角度）、Data Scale（要素数 × 次元 × データ型の
メモリ量）、Loan（元利均等の月額・借入可能額・返済期間の逆算・ボーナス併用）の
3 モジュールが動き、PWA としてインストールでき、オフラインで計算できる状態。
計算コアは Rust/WASM、期待値は Python の独立実装が生成している。

**この時点では Data Scale と Loan の入力はフォームで、電卓の盤面に統一されるのは
0.2.0 である。** 区切りは DoD の 14 項目が満たされた日に置いた。
