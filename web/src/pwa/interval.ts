/**
 * 更新を探す間隔。**`virtual:pwa-register` を引き込まない**ので、検査から読める。
 *
 * **`index.ts` に置かない理由がこれである**——あちらは `registerSW` を import
 * するため、**jsdom の検査からは読めない**(仮想モジュールは vite が作る)。
 * **値を固定する番人を置けるようにするため、定数だけをここに出した。**
 */

/**
 * **新しい版を探す間隔。6 時間**(利用者の裁定 2026-09-17、0.9.3 設計書 §3.2)。
 *
 * **新しい版が最大 6 時間遅れて届く形を、承知で選んでいる。**
 *
 * **これが無いと、アプリは新しい版を一度も探しにいかない。** `registerSW` は
 * 登録するだけで、**待機している SW を見つけるのはブラウザ側の更新確認**である
 * ——**ハッシュだけで画面が変わるこの作りでは、開きっぱなしのタブに
 * ナビゲーションが起きない**。0.9.2 まで `setInterval` も `registration.update()` も
 * `web/src` に 0 件だった。
 *
 * **番人は `interval.test.ts`。** **「間隔が在る」ではなく「6 時間である」を見る**
 * ——**緩めれば緑になる数字にしないため。**
 */
export const UPDATE_CHECK_INTERVAL_MS = 6 * 60 * 60 * 1000;
