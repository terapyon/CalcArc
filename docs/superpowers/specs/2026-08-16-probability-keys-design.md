# S-3 階乗・順列・組合せ — 設計

日付: 2026-08-16
対象: `crates/calcarc-core/src/scientific/`、`engine/{key,state,mod}.rs`、golden、盤面。
前提: **main（`0fd4842`）から**（複利の逆算と E2E がマージされた後）。S-1 の `DomainError` に依存する。
**状態: 実装済み（`feature/probability-keys`、2026-08-16）。**
計画は `docs/superpowers/plans/2026-08-16-probability-keys.md`。
実装順と依存は `2026-08-16-eng-notation-design.md` §0.1 が正である。

**§4 の主張を 1 つ訂正した**（実装計画の起草時に実測）。§4 を参照。

## §0 位置づけ

ユーザー発意（2026-08-16）:「**確率計算用の階乗や P / C なども入れたい。**」

base-spec §9.2 の MVP には無い**追加機能**である。4 本のうち 3 本目。

## §1 形が 2 種類ある — ここが設計の入口

エンジンはキーを 2 通りに捌いている:

| | 仕組み | 例 |
|---|---|---|
| **単項** | `apply_unary(state, f)`。数を打ってから押すと、その場で現在値に適用 | `sin` `√` `x²` |
| **二項** | `push_binop(state, op)`。優先順位（`precedence()`）で括る | `+` `−` `×` `÷` |

**`n!` は単項である**——`5` `n!` で `120`。`apply_unary` にそのまま乗り、
エンジンの構造は 1 行も変わらない。

**`nPr` / `nCr` は二項演算子である**——`5` `nPr` `2` `=` で `20`。
**`BinOp` に 2 つ足し、優先順位を決める必要がある。** ここがこの spec の主題。

## §2 優先順位

いま `BinOp::precedence()` は `Add/Sub = 1`、`Mul/Div = 2`。

**`nPr` / `nCr` は `3`（`×` `÷` より先）にする。**

```
5 × 4 nCr 2 =   →  5 × (4 nCr 2)  =  5 × 6  =  30
```

理由: 一般的な関数電卓（Casio）がそうであり、`nCr` は「1 つの数」を作る演算として
読まれる。`5 × 4` を先に計算して `20 nCr 2` にする読み方は、書いた人の意図から遠い。

**同じ優先順位どうしは左結合**（既存の `>=` 比較がそうなっている）。
`5 nCr 3 nCr 2` は `(5 nCr 3) nCr 2` = `10 nCr 2` = `45`。

## §3 定義域

3 つとも**非負整数**の上でしか定義しない。

| | 定義域 | 外れたとき |
|---|---|---|
| `n!` | `n` が非負整数 | `DomainError` |
| `nPr` | `n`, `r` が非負整数、`r ≤ n` | `DomainError` |
| `nCr` | 同上 | `DomainError` |

- **非整数は `DomainError`。** `2.5!` はガンマ関数だが、S-1 の裁定（関数は実数に
  閉じる、面倒な拡張はしない）と同じ精神で入れない
- **負は `DomainError`**
- **`0! = 1`**、**`nP0 = 1`**、**`nC0 = 1`**、**`nCn = 1`**——境界は全部 1

**判定は `x.fract() == 0.0 && x >= 0.0 && x.is_finite()`。** `Value` が複素数のときは
`is_real()` が偽なので `DomainError`（S-1 の線引きと同じ）。

## §4 精度 — f64 で計算してよいのか

`Value` は `f64` である。**整数値を返す演算を f64 でやることの是非**を、
数値方針に書く必要がある。

### 事実

- `20!` = 2,432,902,008,176,640,000 は `2^53` を超えており、**f64 では既に厳密でない**
- `170!` ≈ 7.26e306 が f64 の上限。**`171!` は `Overflow`**
- しかし**表示は有効数字 10 桁**（`DISPLAY_DIGITS`）である。`20!` は
  `2.432902008e18` と出る

### 誤差の見積り

`n!` を素直に `n` 回掛けると、1 回の乗算の相対誤差は `2^-53 ≈ 1.1e-16`。
`n` 回で高々 `n × 1.1e-16`。**`n = 170` でも `1.9e-14`** であり、
**表示の 10 桁（`1e-10` 相当）より 4 桁良い。**

**したがって、表示される 10 桁はすべて正しい。** これを spec の主張とし、
golden で固定する。

### `nCr` は素直に書くと途中であふれる

`nCr = n! / (r!(n−r)!)` をそのまま計算すると、`n = 200` あたりで分子が
`Overflow` する——**答が f64 に収まるのに、途中であふれる。**

**乗除を交互にする形で書く。順序は「割ってから掛ける」である**:

```
C(n, r) =  各段で  acc ← acc / (i + 1) × (n − i)      （i = 0 .. r−1）
```

`r` を `min(r, n−r)` に置き換えて反復回数も減らす。

`nPr = Π[i=0..r-1] (n − i)` は素直な積でよい（答より大きい途中値が出ない）。

### 【訂正 2026-08-16、実装計画の起草時に実測】

**当初この節は `acc ← acc × (n − i) / (i + 1)`（掛けてから割る）と書き、
「各段の途中値は常に `C(n, i+1)` そのものなので、答が収まるなら途中も収まる」
と主張していた。後半は偽である。**

`C(n, i+1)` は**その段を終えた後**の値であって、**段の中のピーク**ではない。
ピークは割る前の `acc × (n − i)` で、これは `C(n, i+1) × (i+1)` に等しく、
**答の最大 `r` 倍**まで膨らむ。

実測した反例（中心二項係数）:

| `n` | `C(n, n/2)` | 掛けてから割る | 割ってから掛ける |
|---|---|---|---|
| 1020 | 2.8063e305 | 2.8063e305 | 2.8063e305 |
| **1022** | **1.1214e306** | **Overflow** | 1.1214e306 |
| **1024** | **4.4813e306** | **Overflow** | 4.4813e306 |
| **1028** | **7.1561e307** | **Overflow** | 7.1561e307 |

`C(1022, 511) × 511 ≈ 5.7e308` が f64 の上限 1.798e308 を超える。
**答が f64 に収まるのに、段の中で落ちる。**

**割ってから掛ける形にすると、この帯が消える。** 精度はほぼ変わらない
——`n = 0..1028` の格子（各 `n` について `r ∈ {0, 1, 2, n/4, n/3, n/2, n}`）で
最悪相対誤差を測ると:

| 式 | 最悪相対誤差 |
|---|---|
| 掛けてから割る | 3.7e-15 |
| **割ってから掛ける** | **4.5e-15** |

**どちらも表示の 10 桁（1e-10 相当）より 4 桁以上良い。**

**この 2 行は測り直した値である。** 先に無作為 4,000 組で測ったときは
どちらも 3.5e-15 級と出たが、**中心二項係数の近くを取りこぼしていた**
（実際 `C(200,100)` は割ってから掛けると 7.4e-16、掛けてから割ると 0）。
**最悪値を主張するなら、悪くなる場所を狙って測ること。** 精度を捨てずに定義域が
広がるので、割ってから掛ける形を採る。

**`acc / (i+1)` は整数にならない段がある**（`C(5,2)` は途中で 2.5 を通る）。
それでよい——f64 はもともと `20!` の時点で厳密ではなく、測るべきは**表示の
10 桁が正しいか**だけである。golden がそれを固定する。

**この誤りの型を残す**: 途中値の主張は、**不変点（段の切れ目）ではなく
ピーク点（段の中）で検算する。** 「各段の値は C(n, i+1) である」は正しく、
そこから「途中も収まる」を導いたのが誤りだった。

**この「途中であふれない書き方」は spec の主張なので、golden の必須ケースに
入れて固定する**——`200 nCr 100`（≈ 9.05e58。素直な `n!/(r!(n−r)!)` はここで
落ちる）と、**`1022 nCr 511`（掛けてから割る形が落ちる帯の証拠）**。

## §5 Python 参照の独立軸

**ここは独立性が素直に取れる。** Python は `math.factorial` / `math.comb` /
`math.perm` で**厳密な整数**を出せる。

- Rust は f64 の逐次乗除、Python は**任意精度の整数** —— アルゴリズムが同型でない
- golden の期待値は**Python の厳密整数を 10 桁に丸めたもの**にする
- **`tolerance` は `scientific.json` の既存のものを使う**（相対誤差）。
  §4 の誤差見積り（`1.9e-14`）が tolerance に収まることを、生成時に確かめる

**`n!` が f64 の上限を超える境界**（`170!` は成功、`171!` は `Overflow`）も
Python 側で判定できる（`math.factorial(171)` の桁数を見る）。

## §6 盤面

**3 キー要る**（`n!` `nPr` `nCr`）。

**2026-08-16 時点の盤面**（S-2 で関数列が 2 段になった。S-1 §7 が正）:

```
関数列 1 段目: [Shift] [sin] [cos] [tan] [√]  [x²]  [DEG/RAD]
関数列 2 段目: [ENG]   [ln]  [log] [1/x] [eˣ] [xʸ]  [°'"]
```

**第 1 面はこれで埋まっている。** 第 2 面は S-1 が 4 つ（逆三角 3 + `e`）、
`EXP` の裏に `π`（既存）。**空いているのは数字キーと `√` `x²` `DEG/RAD` などの裏。**

**裁定: 数字キーの Shift を使う（§7）。**

| 第 1 面 | Shift |
|---|---|
| `7` | `n!` |
| `8` | `nPr` |
| `9` | `nCr` |

理由: 3 つは**同じ仲間**なので隣り合わせに置きたい。関数列の裏は S-1 が意味の対応
（`√`→`ln` など）で埋めており、そこに割り込むと対応が壊れる。数字の裏は空いていて、
**3 つ並べられる場所は他に無い**。

**懸念**: 数字キーに Shift が付くのはこのリポジトリで初めてである
（いま Shift を持つのは `sin`/`cos`/`tan`/`√`/`x²`/`EXP` だけ）。
「数字の裏に機能がある」ことを利用者が見つけられるか。§7 の裁定待ちにする。

## §7 裁定（ユーザー、2026-08-16）

| # | 論点 | 裁定 |
|---|---|---|
| 1 | `nPr` / `nCr` の優先順位 | **`×` `÷` より先（3）。** `5 × 4 nCr 2` は `5 × 6 = 30` |
| 2 | 3 キーの置き場 | **数字 `7` `8` `9` の Shift。** 3 つを隣り合わせに置ける唯一の場所（関数列 2 段目は ENG + S-1 の 5 つ + `°'"` で埋まっている）。**数字キーに Shift が付くのはこのリポジトリで初めて**なので、**発見性を E2E で確かめる**こと |
| 3 | 非整数の階乗 | **`DomainError`。** ガンマ関数は「実数に閉じる」の精神から外れる |
| 4 | 表示の桁 | **10 桁のまま。** `20!` は `2.432902008e18`。19 桁は画面に入らない |

**裁定 2 に伴う実装上の注意**: 数字キーの Shift は前例が無いので、**Shift を押したときに
数字の面が変わることが見て分かるか**をスクリーンショットで確かめる。テストでは
分からない。

**【訂正 2026-08-16】** **3 つは `(` `)` `+/−` の第 2 面へ移した**
（0.2.0 設計書 §9）。「隣り合わせに置ける場所が他に無い」は誤りで、
括弧と符号の 3 つが空いていた。**発見性を色で担保する仕掛けも外した**
——あれは数字が消えることの埋め合わせであり、数字を消すのをやめた以上
不要である。面が変わったことは、ラベルが `(` から `n!` に変わることで
直接見える。

## §8 検証

段ゲート: 数値方針（§4 の誤差の主張）→ Python 参照 + golden → Rust → WASM → 盤面。

**必須ケース（案）**: `0!` `1!` `20!` `170!` `171!`(Overflow) `2.5!`(DomainError)
`(-1)!`(DomainError) / `5 nPr 2` `5 nCr 2` `nC0` `nCn` `5 nCr 6`(DomainError)
`200 nCr 100`（途中であふれない証拠）、**`1022 nCr 511`（掛けてから割る形が
落ちる帯の証拠。§4 の訂正）**、`1000 nCr 500`（f64 の上限近く）/
優先順位 `5 × 4 nCr 2`。

**赤確認は 3 段階にする**（§4 の訂正に伴い、当初の 1 段階から拡張）。
**3 つの書き方で定義域が 3 段階に違うこと自体が主張である**:

| 書き方 | 落ちる最初の必須ケース |
|---|---|
| 素直な `n!/(r!(n−r)!)` | `200 nCr 100`（`200!` が溢れる） |
| 掛けてから割る | `1022 nCr 511`（段の中のピークが溢れる） |
| **割ってから掛ける（採用）** | どちらも通る |

**上 2 つに書き換えて、落ちる範囲が実測表と一致することを見る。**
1 段階だけだと「素直な式より良い」しか言えず、**採用した順序でなければ
ならない理由**が主張されない。

## §8.1 他セッションとの約束（2026-08-16 合意）

- **`KEY_TOKENS` は足すだけ**（`n_fact` `n_p_r` `n_c_r` の 3 つ）。既存の綴りを
  変えない・消さない
- **`corpus_expr.py` の `UNARY_FNS` / `BINARY_OPS` に触らない**——`nPr` / `nCr` を
  あそこに足すと、同じ種でも重量級コーパス 4,000 件が総入れ替えになる
- **`engine_table.rs` は行を足すだけ**（`5 nCr 2` `5 × 4 nCr 2` の優先順位ほか）。
  既存の行を書き換えない。相手の直列化はあの表を仕様として従う

## §9 スコープ外

- ガンマ関数、ベータ関数、二項分布などの統計
- 乱数
- 重複順列・重複組合せ
- 大きな整数の厳密表示（S-3 は f64 の 10 桁で閉じる）
