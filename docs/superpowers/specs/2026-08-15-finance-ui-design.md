# Finance の盤面（A）— 設計

日付: 2026-08-15
対象: Finance タブに複利を出す。あわせて**実運用で見つかった表示・入力の欠陥**を直す。
前提: B（`loan` の `finance` 下への移動）の上に積む。計算コアは F1 で完成済みで、
**この spec はコアを 1 行も変えない**。
**状態: 未承認のドラフト。** 未裁定 10 点（§15）はユーザーの選択を待つ。

## §0 位置づけ — 3 つの主題

1. **複利をタブの中に出す。** F1 が計算コアと WASM 境界まで作り、
   `web/src/finance/` のラッパーは**呼び出し元が無いまま**置いてある。ここに
   盤面を繋ぐ。
2. **入力が読めない・切り替えると計算根拠が消える**（実運用で報告された欠陥 1。
   §1）。「いま何で計算しているのか分からなくなる」——**答だけが見えて根拠が
   見えない**状態が、項目を切り替えるたびに起きている。
3. **`000` が効かない**（実運用で報告された欠陥 2。§2）。
4. **期間を打つのに頭で 12 を掛けている**——35 年のローンを `420` と入れている。
   一方で `万`・`億` のセルは金額以外では死んでいる。**単位キーを項目に従わせる**
   （§5）。
5. **`AC`・`DEL` の位置とキーの寸法が Scientific とズレている**（利用者の要望。
   §6）。タブを行き来すると `AC` の場所が変わる。
6. **四則演算が Data Scale と Finance に無い**（利用者の要望。§7）。
   **これはコアの追加になるので、A には載せずに別 spec へ送る**のが推奨
   （§15 Q10）。

2 と 3 は F1 の範囲外だったが、**Finance の盤面を触るこの spec が正しい置き場**
である。どちらも「盤面の入力体験」の問題で、複利の項目を足すときに同じコードを
触る。**しかも 2 は複利で悪化する**——項目が 6 つあり、周期と税まで含めると、
根拠が見えないまま答だけが出る画面になる。

---

## §1 欠陥 1: 入力が読めない・切り替えると計算根拠が消える

**2 つの症状は同じ原因から出ている。** どちらも「入力の見せ方」の設計が
足りていない。

### 症状 1: いま打っている数字が小さすぎる（実測）

`web/src/ui/Readout/Readout.module.css` の `.echo` は

```css
  color: var(--display-status-fg);
  font-size: var(--display-size-status);   /* 0.75rem = 12px */
```

**status 行と同じ扱い**になっている。

原因は**役割の取り違え**である。**Scientific では echo は副次表示**だ——答が
`main` に出ていて、echo は「いま何を打ったか」の控え。小さくて薄くてよい。

**Loan と Data Scale では echo が入力の主表示**である。`main` に出るのは
**答**（月額・データ量）で、**利用者がいま打っている数字は echo にしかない**。
金額を打っているあいだ、画面で一番大きいのは無関係な答か空欄で、打鍵の結果は
12px の薄い字で右上に出る。

サイズが合っていないのではなく、**役割が違うものを同じ見た目にしている**。

### 症状 2: 項目を切り替えると、前に入れた値が画面から消える

**echo はアクティブな項目しか出していない。**

```
借入額を選んで 3000万 と打つ  → echo: 借入額 3000万
年利に切り替える              → echo: 年利          ← 3000万 が消える
```

3,000 万・1.5%・420 か月を入れ終えた時点で、画面に残っているのは
**答と、最後に触った項目だけ**である。「この 91,855 円は何から出たのか」を
確かめる方法が、**項目タブを 1 つずつ押して回ること**しかない。

これは Loan で 6 項目、複利で 6 項目あるので、項目が増えるほど悪化する。
**答だけがあって根拠が見えない画面**は、金融計算では特に危うい——桁を 1 つ
間違えたまま答を読んでも気付けない。

### 直し方: echo を「名前つきの入力の列」にする

1 行の文字列ではなく、**入力の一覧**を見せる。いま打っている項目は大きく
はっきり、**打ち終わった項目は小さく副次的に、しかし画面に残す。**

```
┌──────────────────────────────────────┐
│                          年利 1.5     │  ← アクティブ（大きく・濃く）
│  借入額 3000万 · 期間 420か月          │  ← 入力済み（小さく・薄く）
│  月額を求める            年利を入力中   │  ← status（既存）
│                            91,855     │  ← main（答）
└──────────────────────────────────────┘
```

- **未入力の項目は出さない**（空の `残価` や `ボーナス` で埋めない）。
- **アクティブが未入力なら、ラベルだけを大きく出す**（いまの挙動と同じ）。
- 並び順は**項目行の並び**に合わせる（盤面と目の動きを揃える）。

### 機構（§15 Q1 で裁定）

- **(a) `Readout` の echo を構造化する**（推奨）:

  ```ts
  export interface ReadoutEntry {
    /** 項目名。Scientific のように名前が無い入力では空文字。 */
    label: string;
    /** 表示用に整形済みの値。**Readout は整形しない**（既存の規律）。 */
    value: string;
    active?: boolean;
  }
  ```

  `echo: string` を `entries: ReadoutEntry[]` に置き換える。
  **Scientific は「名前なしの 1 件」を渡す**（`[{ label: "", value: "sin 30 +",
  active: true }]`）ので、見た目も意味も変わらない——アクティブな 1 件しか
  無いから、いままでと同じ 1 行になる。

  この形なら **`echoRole` のような見た目の指定が要らない**。「アクティブか
  どうか」は入力の事実であって、装飾の指定ではない。

- **(b) `echo: string` のまま、パネル側で `借入額 3000万 · 年利 1.5` と組み立てる**:
  `Readout` を触らずに済むが、**どれがアクティブかを文字列で表せない**ので
  強弱が付けられない。症状 2 だけを直して症状 1 を残す形になる。

- **(c) 入力一覧を別の区画にする**（`Readout` の外）: 強弱は付けられるが、
  **表示が 2 か所に割れる**——`main` と入力が離れると視線が往復する。

**推奨 (a)。** ただし **Scientific の見え方を変えないこと**が条件である
（S2 のエコーは設計どおりに動いている）。`entries` が 1 件で `label` が空なら、
いまの `.echo` と同じ描画に落ちる、という形で守る。

**大きさの具体値は plan で決める。** 目安はアクティブが `--key-font-size`
（1.375rem）級・色は `--display-fg`、入力済みが `--display-size-status`
（0.75rem）級・色は `--display-status-fg`。**いまの echo の見た目が「入力済み」
側に降りる**と読むと分かりやすい。

### 縦の余地（§15 Q7）

入力済みの行が増えると `Readout` が縦に伸びる。390×844 で、Loan の
月額モード（借入額・年利・期間・残価・ボーナスまで入りうる）が最悪で、
**入力済み行が 2 行に折り返す**可能性がある。

- **(a) 折り返して伸ばす**（推奨）: 値が見えることが目的なので、隠さない。
- (b) 1 行に収め、溢れは横スクロール: 見えない値ができる。目的に反する。
- (c) 最新の n 件だけ出す: どれが消えたか分からない。

**キーパッドの 44px を割ってはならない**ので、伸びる余地があるかは
**実機で撮って確かめる**（§11 の完了条件）。足りなければ、入力済み行の
文字を詰めるか、`Readout` の縦 padding を削る——**キーの寸法は最後に削る**。

---

## §2 欠陥 2: `000` が 1 個しか入らない

### 実測（2026-08-15、jsdom で再現）

`月々の返済額` モードで「借入額」を選び、`3` → `000` と押した結果:

| 項目 | 打鍵 | 出た値 | 期待 |
|---|---|---|---|
| 借入額（金額） | `3` `000` | **`30円`** | `3000円` |
| 返済期間 | `1` `000` | `1000か月` | `1000か月` ✓ |
| 年利 | — | キーが無効 | 無効（設計どおり） |

**金額の項目だけが壊れている。**

### 原因

`LoanPanel.tsx` の `zeros3` は 1 打鍵を 3 回の数字入力に展開する:

```ts
      case "zeros3":
        for (const _ of [0, 1, 2]) pressDigit("0");
```

`pressDigit` は項目の種類で分岐する:

```ts
    if (MONEY_FIELDS.includes(active)) {
      setEntry(active, pushDigit(entryOf(active), digit));   // ← 値を先に計算する
      return;
    }
    if (active === "rate") {
      setRate((previous) => ...);                            // ← 関数型更新
    }
    setMonths((previous) => ...);                            // ← 関数型更新
```

**金額だけが「いまの値」をレンダー時のクロージャから読む。** `entryOf(active)` が
返すのは、この描画時点の `amounts` である。同じイベントの中で 3 回呼んでも 3 回
とも同じ値を読み、React がまとめて反映するとき**最後の 1 回だけが残る**。

年利と期間が無事なのは、関数型更新（`(previous) => ...`）が**直前の結果**を
受け取るので 3 回が積み上がるからである。

**Data Scale は正しい。** 同じ `000` を、ローカルで畳んでから 1 回だけ書く:

```ts
      case "zeros3":
        if (numberField) {
          let next = entry;
          for (const _ of [0, 1, 2]) next = pushDigit(next, "0");
          setEntry(next);
        }
```

### 検査の穴（記録）

**L には「`000` を押した結果」を見る検査が 1 つも無い。**

```bash
$ grep -rn "3桁のゼロ" web/src/ui/Loan/LoanPanel.test.tsx web/tests/e2e/loan*.spec.ts
LoanPanel.test.tsx:152:  expect(...{ name: "3桁のゼロ" })).toBeDisabled();     # 年利で無効
LoanPanel.test.tsx:157:  expect(...{ name: "3桁のゼロ" })).toBeEnabled();      # 金額で有効
loan-keypad.spec.ts:62:   const zeros = ...{ name: "3桁のゼロ" };              # 可否のみ
```

**押せるかどうかしか見ていない。** 押した結果を誰も見ていないので、キーが
何もしなくても全部緑になる。**Data Scale にも同じ穴がある**（`000` の押下結果を
見る検査は 0 件。実装は正しいが、正しさは検査に支えられていない）。

**実装側の自己レビューでも、読みの完了レビューでも捕まらなかった。**
可否表（disabled の条件）を丁寧に作ったことが、かえって「このキーは検査済み」
という錯覚を生んだ——**可否は入口の検査であって、出口の検査ではない。**
`000` は D でも「押した結果」が無検査なので、これは 1 つのキーの話ではなく
**「状態を持たないキーは検査を忘れる」という型**である。

### 裁定（§15 Q2）

- **(a) `000` を廃止する**（推奨・ユーザーの傾き）: 金額には `万`・`億` があり、
  `3000万` は `3` `000` `万` より `3000` `万` の方が短い。**`000` が短縮になるのは
  単位キーが無いときだけ**である。廃止すれば壊れた経路も消える。
- **(b) 直して残す**: Data Scale と同じローカル畳み込みにする。修正は 4 行。

**どちらを採っても、検査の穴は塞ぐ**——(a) なら「`000` のキーが存在しない」を、
(b) なら「金額で `3` `000` が `3000` になる」を検査にする。

**(a) を採った場合の空きセル**（§15 Q3）: `000` が抜けた 4×4 の 1 マスを
(i) 恒久の空きにする（D の型面と同じ。何も描かない） (ii) 予約スロットにする
（S1 の「ここに何か来る」。無効なボタンを描く） (iii) 他のキーを寄せる。
**推奨 (i)**——「ここに何か来る」予定が無いので、予約スロットは嘘になる。

**Scientific の `000` は廃止しない。** Scientific には単位キーが無いので
`000` は本当に短縮であり、しかも**Rust の engine 側で処理されていて壊れていない**
（`engine_table.rs` が仕様として持っている）。

---

## §3 複利を 1 つのモードにする

F1 のコアは `grow(principal, deposit, rate, periods_per_year, periods, tax)` の
**1 本**で、一括預入は `deposit = 0`、毎月積立は `principal = 0` の退化である。

**盤面もその形をそのまま写す。** 「一括」と「積立」を別モードにしない——
**モードが違うのに計算が同じ**という状態を作らないためで、利用者から見ても
「最初にまとまった額を入れて、毎月も積む」が自然に書ける。

### モード行（§15 Q4）

いまの区画は `求めるもの`（3 キー、半高）で、`月々の返済額` / `借入可能額` /
`返済期間`。ここに `複利` が入ると **区画名が嘘になる**（複利は「求めるもの」
ではなく「計算の種類」）。

- **(A1) 1 段 4 キーにして区画名を `計算の種類` に変える**（推奨）:
  390px で 4 列だと 1 キー ≈ 87px。**`月々の返済額` は確実に折り返す**ので
  ラベルを詰める（`月額` / `借入可能` / `期間` / `複利`）。読み上げ名は
  いまのまま（`月々の返済額を求める` など）残す。
- **(A2) 2 段にする**: 上段 `計算の種類`（`ローン` / `複利`）、下段 `求めるもの`
  （ローンのときだけ 3 キー）。ラベルは詰めなくてよいが、**半高の行が 3 本**に
  なり、キーパッドの 44px を守る余地が減る。

**推奨 (A1)。** 縦の余地の方が希少である（44px はキーの誤爆に直結する）。
ただし**折り返しの有無は実機で撮って確かめる**——L で「ボーナス」が 6 列で
折り返した前例がある。

### 項目行（複利のとき）

| 項目 | 種類 | 備考 |
|---|---|---|
| 元本 | 金額 | 一括預入ぶん。空なら 0 |
| 積立額 | 金額 | 毎期の積立。空なら 0 |
| 年利 | 数（小数可） | ローンと同じ入口 |
| 期間 | 数 | **単位は「期」であって月ではない**（§4） |
| 周期 | 選択 | 月 / 半年 / 年 |
| 税 | 選択 | なし / あり（20.315%） |

6 項目で、ローンと同じ列数（6）に収まる。

**可否表**（L の `fieldEnabledIn` と同じ方式）:

- 複利モードでは**すべての項目が有効**。ローン固有の項目（残価・ボーナス・月額）は
  出さない。
- **モードを切り替えたら、アクティブ項目を新しいモードで有効な先頭へ移す。**
  L の完了レビューで見つかった状態整合バグ（無効になった項目に居座り、
  「無効表示なのに入力先」になる）の再演を防ぐ。**ローン ⇄ 複利の切り替えは
  L の実装時に存在しなかった経路**なので、`fieldEnabledIn(field, forMode)` の
  ようにモードを引数で受ける形——切り替えの瞬間、まだ state に入っていない
  次のモードで判定する必要がある——をここでも守る。
- **元本と積立額が両方とも空**のときは計算しない（コアが `SyntaxError` を返す。
  空欄は「まだ入力していない」であってエラーではないので、**画面は中立のまま**
  にする——D の「両方の項目が埋まっているときだけ計算する」と同じ規律）。

---

## §4 周期と税をどう置くか

どちらも数字ではなく選択である。既存の前例が 2 つある:

| 前例 | 置き方 | 性質 |
|---|---|---|
| Data Scale の**データ型** | 項目タブを押すと**盤面の面が入れ替わる** | 計算に入る |
| Data Scale の**主表示トグル** | 盤面の外のトグル | **表示の読み方だけ** |

**規律を 1 つ決める: 計算に入るものは盤面の中、表示の読み方だけが盤面の外。**

周期も税も**計算に入る**（周期は分母を変え、税は出力を増やす）。したがって
**どちらも「面が入れ替わる項目」にする**——データ型と同じ機構で、周期は 3 キー
（`月` / `半年` / `年`）、税は 2 キー（`なし` / `あり`）。

**面の高さは 4 行ぶんを保つ**（D で確立した機構。外すと面の入れ替えで 93.5px
ずれて押す位置が動く）。周期面も税面も同じ枠に載る。

### 期間の単位は「期」である

**ここが誤解を生みやすい。** 周期が半年なら「期間 10」は 10 半期 = 5 年である。
月と決め打ちできない。

- echo は `期間 10期` のように**単位を出す**。**§5 の単位キーを入れると、
  利用者は「10年」と打てるようになり、期数への換算は盤面が引き受ける**——
  この節の面倒の大半はそこで消える。
- 状態行に**周期を常時出す**（`半年複利` など）。D が主表示の系を status に
  出しているのと同じ。
- 上限は 1200 期（コアの `MAX_PERIODS`）。
- **周期を変えても期間の値は保持する。** L で立てた「打った値は消さない」の
  規律をそのまま引く。**保持すると意味が変わる**——`10` のまま月から半年へ
  移れば 10 か月が 5 年になる——が、それを引き受けるのが echo の「期」単位と
  status の周期常時表示である。**黙って空に戻す方が事故が大きい**（打ち直しに
  気付かないまま別の答を読む）。

---

## §5 単位キーは項目に従う（`万`/`億` の 2 セルを使い回す）

### いま、右の 2 セルは半分の時間だけ働いている

Loan の数字面は 4×4 で、右列に `万` と `億` が居る。

```
7   8   9   DEL
4   5   6   AC
1   2   3   万
0  000  .   億
```

**この 2 つが有効なのは金額の項目だけ**である（`money && canPushUnit(...)`）。
期間を打っているあいだも年利を打っているあいだも、盤面の右下 2 マスは
**押せないボタンとして場所を占めている**。

一方で、**期間には単位が要る**。いまは「か月」固定なので、35 年のローンは
`420` と打つ——**利用者が頭の中で 12 を掛けている**。

### 直し方: 項目に応じて単位キーを差し替える

```
借入額を打っているとき     期間を打っているとき      年利を打っているとき
     ... 万                    ... 年                    ... （空き）
     ... 億                    ... 月                    ... （空き）
```

**`units/entry.ts` の文法がそのまま使える。** この機構は
「確定済みセグメント列 + 入力中の数字」で、値は
`Σ セグメントの数字 × scale + 末尾の数字` である。**10 の冪を前提にしていない**
——`年 = { label: "年", scale: 12n }`、`月 = { label: "月", scale: 1n }` を渡せば

```
3 5 年      → 35×12        = 420 か月
3 年 6      → 3×12 + 6     = 42 か月
3 年 6 月   → 3×12 + 6×1   = 42 か月
```

が、**万/億 と同じ規則で**動く。「単位は下る向きにしか置けない」（年のあとに
年は無い）、「DEL は 1 段ずつ解く」、「数字が無いと単位を置けない」——
すべて共有される。**新しい文法を書かない。**

`units/entry.ts` の冒頭には「小数や単位を持たない値（年利、期間、次元数）は
ここを通らない」と書いてある。**期間が通るようになるので、この行は訂正する。**

### 桁の上限は「合成後の値」に効かせる（L の教訓）

L は期間に `MAX_MONTHS_LEN = 4` を入れた。理由は**桁数ではなく定義域**である
——10 桁を超える月数は WASM 境界の `u32` 変換で折り返し、コアの 1200 上限を
**すり抜けてもっともらしい誤答を出す**（4,294,968,496 か月が 1,200 か月として
計算される）。L の完了レビューで見つかった穴で、そのとき入れたガードである。

**セグメント入力にすると、この上限は打鍵の桁数では効かない。** `9999年9999`
は打鍵としては 4 桁ずつでも、合成すると 129,987 か月になる。したがって:

- `pushDigit` に渡す `maxDigits` は**入力中の数字**にしか効かない。
  期間には小さめの値（4 程度）を渡す。
- **それとは別に、合成後の値（`digits(entry)`）が定義域に収まるかを見る。**
  期間は `u32` かつコアの上限 1200——**盤面が `年` キーを閉じる**か、
  **`u32` に収まらない値をエラーにする**かは plan の判断だが、
  **どちらも実装しないという選択は無い**。
- 複利の期間も同じである（`MAX_PERIODS = 1200`、`u32`）。

**この段落を書いておくのは、L で 1 度踏んだ穴だからである**——セグメント文法に
移すときに「桁数の上限」だけを移植すると、守っていたものが落ちる。

### 複利の期間は「期」なので、単位も周期に従う（§4 の続き）

複利では期間の単位が**期**である。周期が半年なら「10」は 5 年になる——
§4 で「echo の単位と status の周期表示で引き受ける」と書いた点だが、
**単位キーがあればもっと素直に解ける**。

| 周期 | 上の単位 | 下の単位 |
|---|---|---|
| 月次 | `年`（scale 12） | `月`（scale 1） |
| 半年 | `年`（scale 2） | `半期`（scale 1） |
| 年 | `年`（scale 1） | —（1 期 = 1 年なので下位が無い） |

**上の単位はいつでも `年` で、scale が「1 年あたりの期数」になる。**
どの周期でも**割り切れる**ので、端数の期が生まれない。利用者は
「10 年間」と打てばよく、期数への換算は盤面がやる。

**周期を変えたら scale が変わるので、同じ `10年` が別の期数になる**
（月次 120 期 / 半年 20 期 / 年 10 期）。これは正しい——期間は 10 年のままで、
複利の刻みだけが変わっているからである。§4 の「周期を変えても期間の値は
保持する」がここでも効く。

### 年利の 2 セルはどうするか

年利には単位が無い（`.` は別のセルに居る）。**空きにする**——恒久の空き
（何も描かない。D の型面と同じ）。無効なボタンを描くと「押せそうに見えるのに
押せない」が残る。

### この節が Q2 と絡む点

`000` を廃止する（§3 Q2=a）と、数字面はこうなる:

```
7   8   9   DEL          7   8   9   DEL
4   5   6   AC     →     4   5   6   AC
1   2   3   万           1   2   3   年
0  ―   .   億           0   ―   .   月
```

**`000` の空きと単位キーの入れ替えは独立に決まる**が、同じ 4×4 の絵の上で
起きるので、**plan では 1 つのタスクで扱う**（別々に触ると絵が 2 回変わる）。

---

## §6 盤面を Scientific に揃える

**利用者の要望（2026-08-15）**: `AC` と `DEL` を Scientific と同じ位置に。
数字キーの大きさも揃える。

### いまズレている（実測）

| | Scientific | Loan / Data Scale |
|---|---|---|
| 列数 | **5** | 4 |
| キーの寸法（390px 幅） | **約 67px 角** | 約 86px 角 |
| `DEL` の位置 | **最上段の右から 2 番目** | 右上（1 行目・4 列目） |
| `AC` の位置 | **最上段の右端**（`DEL` の隣） | `DEL` の**下**（2 行目・4 列目） |

```
Scientific（5×5）                Loan（4×4）
(   )   +/−  DEL  AC             7   8   9   DEL
7   8   9    ÷    j              4   5   6   AC
4   5   6    ×    ▸∠             1   2   3   万
1   2   3    −    Exp            0  000  .   億
0  000  .    +    =
```

**タブを行き来すると、`AC` が「右上の 1 つ下」と「右上」を往復する。**
消去は取り返しの効くキーだが、**`AC` を押すつもりで別のキーを押す**のは
入力のやり直しに直結する。

### 直し方: Loan / Data Scale も 5 列にする

**列数を揃えると、位置も寸法も同時に揃う。**

```
Loan（5×5、案）                  Data Scale（5×5、案）
(   )   +/−  DEL  AC             ―   ―   ―   DEL  AC
7   8   9    ÷    万              7   8   9   ÷    K
4   5   6    ×    億              4   5   6   ×    M
1   2   3    −    ―              1   2   3   −    G
0  000  .    +    =              0  000  ―   +    =
```

- **`DEL` と `AC` が最上段に並ぶ**（Scientific と同じ）。
- **キーは約 67px 角**になり、3 つのタブで同じ寸法になる。44px は余裕で守る。
- **5 列目がモジュール固有の列**になる（Scientific は `j`・`▸∠`・`Exp`・`=`、
  Loan は単位と `=`、Data Scale は `K`・`M`・`G` と `=`）。
- 4 列目が**演算の列**になる（§7 が入る場合）。

**キーが小さくなることは後退ではない。** 44px の線を割らず、
**位置の一貫性の方が誤爆を減らす**——3 つのタブで同じ場所に同じキーがある
状態は、1 つのタブだけ大きいことより価値がある。

### 空きセルの扱い

`―` は**恒久の空き**（何も描かない。D の型面と同じ）。
`(` `)` `+/−` は Loan では意味を持たない——§7 で四則演算が入るなら括弧が
生きるが、入らないなら空きである。**Q9 の裁定に従う。**

### 波及（見落としやすい）

- **L と D の vitest・E2E がキーの位置を見ている**箇所は書き換わる
  （`loan-keypad.spec.ts` の 44px 検査、`data-scale-keypad.spec.ts` の
  「面を入れ替えても DEL の位置が動かない」）。
- **D の型面も 5 列になる**（数字面と同じ枠に載る、が D の不変条件）。
  9 つの型 + `DEL` + `AC` を 5×5 に置き直す。
- **`000` を廃止する（§2 Q2=a）なら、5 列化と同じタスクで絵を決める**——
  4×4 の絵を 2 回変えない。

---

## §7 四則演算を Data Scale と Finance にも

**利用者の要望（2026-08-15）**: 四則演算の機能を Data Scale にも Finance にも。

### いまは無い（意図的に落とした経緯）

D の設計時に「式入力（四則）を入れるか」を再判断し、**入れない**と決めた。
理由は「演算キーは優先順位・保留・括弧の状態機械を持ち込み、engine 変更ゼロと
衝突する」——そのときのスコープを守る判断だった。

**要望が来た以上、これは覆る。** ただし**入れ方に制約がある。**

### 制約: 計算は web に書けない

CLAUDE.md の 2 つの規律が効く。

- **計算ロジックは `calcarc-core` に置く。`web` に計算を書かない。**
- **丸めた表示値を入力に還流させない**（base-spec §26）。

つまり `3000*12` を TypeScript で `eval` することはできない。**Rust に整数の
式評価が要る。**

### Scientific の engine は流用できない

Scientific の engine は複素数の f64（`Value`）で計算する。**金額と件数は
厳密整数**である（Loan は u64 円、Data Scale は u128）。

- Data Scale の件数は 10 進 39 桁まで受ける。**f64 は 2^53 で精度を失う**ので、
  39 桁の値に `×2` を掛けた瞬間に答が壊れる。
- Loan の金額も u64 域で同じ問題を持つ。

**したがって「engine をそのまま呼ぶ」では済まない。** 必要なのは
**整数の式評価**（u128 または u64 の定義域、オーバーフローは `Overflow`）で、
これは**新しい計算コア**である。

### だから A には載せない（§15 Q10）

- **(a) 別 spec（C）に分ける**（推奨）: A は**盤面と表示の spec**で、
  完了条件に「`crates/` の差分ゼロ」を掲げている（§10）。整数式の評価器は
  コアの追加であり、**Python 参照実装と golden が要る**（base-spec §49 の
  ゲートが金融計算に掛かる）。A に混ぜると、盤面の変更とコアの新機能が
  同じ diff に入って**レビューできない大きさ**になる。
- (b) A に含める: 1 本のブランチで盤面・表示・複利・整数式評価まで載る。

**推奨 (a)。** ただし**A で場所は用意する**——§6 の 5 列化で 4 列目が
演算の列になるので、C はキーを置くだけで済む。**A の時点では 4 列目を
恒久の空きにし、「ここに演算が来る」とは書かない**（S1 の予約スロットの
教訓: 来ると書いたものは来るまで嘘になる）。

### C に送る論点（spec C を書くときの下敷き）

- **定義域**: Data Scale は u128、Loan は u64。**同じ評価器を 2 つの定義域で
  使うか、モジュールごとに持つか。**
- **除算**: 整数除算の切り捨てか、割り切れないときにエラーか。
  「件数 ÷ 3」は現実に要る（分割数）。**丸めの向きを仕様として固定する**
  （base-spec §37 と同じ扱い）。
- **単位キーとの同居**: `100万 + 50万` は打てるべきか。位取り文法
  （`units/entry.ts`）と式の文法が同じ入力欄で共存する。
- **括弧と優先順位**: Scientific の engine が持つ状態機械を、整数側でも
  作るのか、それとも**式を持たず「打った順に適用する」電卓流**にするのか
  （後者なら状態機械が要らない）。
- **`=` の意味**: 項目の入力を確定するのか、式を評価するのか。
- **golden**: 整数式は `testdata/` に新しい op が要る。Python 参照は
  `int` の式評価（Python の `int` は多倍長なので u128 を自然に持てる）。

---

## §8 結果の出し方

`main` = **満期の元利合計**（税ありなら手取り。§15 Q5）。

結果領域（D の `datascale-result` と同型）に併記する:

```
元本合計      7,200,000 円
運用収益      2,648,906 円
――― 税あり のとき ―――
国税            405,679 円
地方税          132,445 円
手取り        9,310,782 円
```

**エラー**: コアが返すのは `SyntaxError`（定義域外）と `Overflow`（u64 超え）。
`main` に `Math ERROR` を出し、`data-error` に種類を入れる（L・D と同じ）。

**免責と税の注記**（F1 spec §13）:

- 既存の免責（「実際の返済額は金融機関の計算方法により異なります」）は
  **複利にも掛かる文言に広げる**か、モードで出し分ける（plan の判断）。
- **税を出すときは、税務上の助言ではない旨を添える。** これは新規の文言で、
  「20.315% を引いた額」を見た人が申告の根拠にしないための最低限である。

---

## §9 区画の名前（E2E のセレクタになる）

| 区画 | 名前 |
|---|---|
| モード行 | `計算の種類`（**`求めるもの` から変わる**。Q4=A1 の場合） |
| 項目行 | `入力する項目`（据え置き） |
| 数字面 | `数字と単位のキー`（据え置き） |
| 周期面 | `複利の周期のキー` |
| 税面 | `税のキー` |

**`求めるもの` → `計算の種類` の改名は、L が書いた E2E のセレクタを動かす**
（`loan.spec.ts` と `loan-keypad.spec.ts`）。機械的な置換だが、**見落とすと
E2E が赤になる**ので plan の最初のタスクで扱う。

E2E は**パネル起点で引く**（`region` 名 `金融計算`）——`入力する項目` と
`数字と単位のキー` は Data Scale と同名である。

---

## §10 コアと golden を動かさないための条件

1. `crates/` の差分ゼロ。`testdata/finance.json` の 53 件も無変更。
2. **コアへ渡すのは素の数字列**（`万`・`億` は UI 層の展開。base-spec §26）。
3. WASM の `compound_grow` のシグネチャを変えない（F1 で確定済み）。
4. `web/src/finance/index.ts` は**型変換と初期化だけ**を持つ（計算を書かない）。

---

## §11 検証段（tiering、ci.yml 導出）

web 層だけを触るので **web 段**:

```bash
cd web && pnpm typecheck && pnpm lint && pnpm test && pnpm e2e
```

- `cargo` / `reference` は回さない。**`git diff --stat <分岐点> -- crates/ testdata/ reference/`
  が空であることを完了報告に書く**。
- **スクリーンショットは必須**（390×844）。見るもの:
  - モード行 4 キーの**折り返し**（§3 の A1 の唯一の弱点）
  - **いま打っている項目が読める大きさか**（§1 の主題そのもの。数値では測れない）
  - **入力を 3〜5 項目まで入れた状態**で、入力済みが副次的に読め、
    **キーパッドが 44px を割っていない**こと（§1 の縦の余地。Q7）
  - 項目を切り替えたときに**前の値が画面に残っている**こと
  - **項目を切り替えたとき、右の 2 セルが `万`/`億` ↔ `年`/`月` に入れ替わり、
    他のキーが動かないこと**（§5。数字の位置がずれると打ち間違える）
  - **3 つのタブを並べて、`AC`・`DEL` が同じ位置・数字が同じ寸法か**（§6）
  - 周期面・税面に入れ替えても**枠の高さと DEL/AC の位置が動かないか**
  - Scientific の echo が**変わっていない**こと（波及していない証明）
  - Data Scale の入力一覧も同じ形になっていること（`Readout` を共有するため）

**`Readout` の入口が変わると 3 つのパネル全部が呼び出し側になる**（§1 の (a)）。
Scientific と Data Scale の書き換えは機械的だが、**Scientific は「名前なし 1 件」
に落ちることを検査で固定する**——ここが崩れると S2 のエコーが壊れる。
- 撮ったら preview を落とし、`ss -ltn | grep 4173` で解放を確かめる
  （`pkill` では落ちない。`fuser -k 4173/tcp` まで）。

### 塞ぐべき検査の穴（§2 の記録から）

- **`000` を押した結果**の検査（廃止なら「キーが無い」、維持なら「`3000` になる」）。
- **Data Scale 側の `000`** にも同じ検査を足す（実装は正しいが無検査）。
- **金額項目の連続打鍵**——同じイベントで複数回状態を更新する経路は、
  `000` だけではない可能性がある。plan で `LoanPanel` の `press` を走査する。

---

## §12 一貫性の論点（§15 Q6）

**`000` を「単位キーがあるから不要」として廃止するなら、同じ理屈が Data Scale
にも当たる**——`K` が `000` を包含する（`100K` = `100000`）。

- **(a) A では触らない**（推奨）: D の `000` は**正しく動いている**。変える理由が
  一貫性だけなら、動いているものを同じ PR で触る利は薄い。**別 PR に送る**
  （一貫性を捨てるのではなく、順序の話である）。
- (b) A で一緒に廃止する。

---

## §13 スコープ外

- **複利の逆算**（目標額から必要積立額・必要年数）。F1 spec §15 でスコープ外。
- **シミュレータ方式**（途中丸めなし）と**実効換算**。F1 の裁定と【訂正 1】。
- 期首積立、途中引き出し、インフレ調整、資産推移表の画面表示。
- `web/src/loan/` の `finance/` 配下への移動（B spec §10）。
- **Scientific の `000` と echo**——触らない。

---

## §14 完了条件

1. Finance タブで複利が計算できる（一括・積立・両方）。`main` に元利合計、
   結果領域に元本合計と運用収益、税ありなら国税・地方税・手取り。
2. **入力の見せ方が §1 のとおりになっている**: いま打っている項目が大きく
   はっきり出て、**入力済みの項目が副次的に、しかし画面に残る**。
   項目を切り替えても**計算根拠が画面から消えない**。
   **Scientific の echo は変わっていない**（スクリーンショットで確認）。
3. `000` が Q2 の裁定どおりになっており、**押した結果を見る検査がある**
   （L と D の両方）。
4. **期間が `年`/`月` で打てる**（Q8=a のとき）。`3年6` = 42 か月、
   `35年` = 420 か月。複利では周期に応じて scale が変わり、**どの周期でも
   割り切れる**。`units/entry.ts` の冒頭の「期間はここを通らない」の記述が
   訂正されている。**合成後の値が定義域（`u32`・1200）に収まることを
   検査している**（§5 の桁上限）。
5. 周期・税の面の入れ替えでも、単位キーの差し替えでも、**枠の高さと
   DEL/AC・数字の位置が動かない**（E2E）。
6. **`AC`・`DEL` が 3 つのタブで同じ位置にあり、数字キーが同じ寸法**
   （Q9=a のとき。§6）。44px を割っていないことを実機で確認。
7. `git diff --stat <分岐点> -- crates/ testdata/ reference/` が空
   ——**四則演算を A に載せない**（Q10=a）ことの証明でもある。
8. web 段のフルスイープが緑。390×844 のスクリーンショットを目視。
9. 免責が複利にも掛かり、**税を出すときは税務助言ではない旨がある**。

---

## §15 未裁定（ユーザーの選択待ち）

| | 論点 | 選択肢 | 推奨 |
|---|---|---|---|
| Q1 | 入力の見せ方 | (a) `Readout` の echo を `ReadoutEntry[]` に構造化 (b) 文字列のまま並べる (c) 別区画に出す | **(a)**（§1） |
| Q2 | `000` | (a) 廃止 (b) 直して残す | **(a)**（§2。ユーザーの傾き） |
| Q3 | 空きセル | (i) 恒久の空き (ii) 予約スロット (iii) 詰める | **(i)**（Q2=a のとき） |
| Q4 | モード行 | (A1) 1 段 4 キー + ラベル短縮 (A2) 2 段 | **(A1)**（§3） |
| Q5 | 税ありのとき `main` に出す値 | (a) 手取り (b) 税引前の元利合計 | **(a)**（税を出す意味がそこにある） |
| Q6 | Data Scale の `000` | (a) 触らない (b) 一緒に廃止 | **(a)**（§12） |
| Q7 | 入力一覧が縦に伸びるとき | (a) 折り返して伸ばす (b) 横スクロール (c) 最新 n 件だけ | **(a)**（§1。ただし 44px を割らないことを実機で確認） |
| Q8 | 単位キーを項目に従わせるか | (a) 期間に `年`/`月`（複利は周期に従う） (b) 金額のときだけ単位、他は空き (c) いまのまま（期間は「か月」直接） | **(a)**（§5） |
| Q9 | 盤面を Scientific に揃えるか | (a) Loan/DS を 5 列にし `AC`・`DEL` を最上段へ（寸法も揃う） (b) 位置だけ揃えて 4 列のまま (c) いまのまま | **(a)**（§6。列数を揃えると位置と寸法が同時に揃う） |
| Q10 | 四則演算 | (a) 別 spec（C）に分ける (b) A に含める | **(a)**（§7。整数式の評価器はコアの追加で、Python 参照と golden が要る） |

**Q2 と Q6 は連動して見えるが、独立に決められる。** Q2=(a) かつ Q6=(a) は
「Loan は壊れているから消す、Data Scale は動いているから今は触らない」であり、
一貫していないのではなく**順序を分けている**。
