# 独立検証の穴 — 設計（**裁定済み 2026-09-10。§7 の 5 点すべて推しどおり**）

1.0 の門の 3 つ目。**実装は利用者の承認のあと**である。この文書は範囲・直し方・
番人・受け入れの測り方を決め、**裁定が要る点を §7 に並べる**。

土台は `origin/main` = `e266a6d`（v0.9.0 の準備まで入った main）。行番号は
**その日の座標**である。門の 1 と 2（WebKit、保存データと URL）は別の設計書が
持ち、ここでは触らない。

---

## §0 この設計書が守ること

- **数字は現物から取る。** 取れないものには **（実測前）** と印を付け、
  §2.6・§4.8 で**実装時に測る受け入れ条件**へ回す。
- **「穴を埋めた」と「その種類の欠陥を捕まえられる」を混ぜない。**
  §2.2 は後者を、欠陥ごとに見積もる。
- **写しを増やさない。** この族の欠陥は、7 回とも「engine の振る舞いを
  別の場所に手で写したもの」から出た（§1.2）。直し方がまた写しになる案は採らない。

## §0.1 先に訂正すること（2026-09-09 の棚卸しの誤り）

私（重量級担当）の 09-09 の棚卸しに誤りが 2 つあり、この門の範囲はそこから
作られている。**範囲を読む前に直しておく。**

1. **「`spell()` に独立実装が無い。CLAUDE.md の規律から外れている唯一の core
   関数」は誤りだった。** `spell` は **`独立: 不可能` を 3 か所で宣言している**
   ——`crates/calcarc-core/src/engine/spell.rs` の module docstring、
   `crates/calcarc-core/tests/spell_table.rs` の冒頭、履歴の設計書
   `2026-09-03-history-design.md:189-192`。番人（`spell_table.rs` の表）も名指し
   されている。**私は宣言の有無を確かめずに「無い」と書いた。**
   **本当の穴は 2 つ**である: (a) その宣言は **「実測前——参照実装を書く段で
   確定させる」**と書かれたまま**確定していない**（同 `:191`、`:480-481` の §13-5）。
   (b) 代わりの守りは **手で並べた 13 本の表**（`spell_table.rs`、`#[test]` 13 本・
   乱択 0）で、**この族の欠陥は毎回、誰も並べなかった角から来た**（§1.2）。
2. **「履歴の欠陥 7 回のうち 6 回は `spell` の中」は数え方が 1 件ずれる。**
   `docs/superpowers/sdd/history-HANDOFF.md` §0 の一覧と、直したコミットが触った
   ファイルを突き合わせると、**`spell` の中は 5 回、web は 2 回**である
   （表は §1.2）。6 回目の「`=` が持ち越しを読むこと」は
   `web/src/ui/ScientificPanel.tsx` に在った（`2a38e32`・`4183e44`）。
3. 期首の「0 件」は**重量級コーパスについてだけ真**である。**golden には
   期首のケースが在る**（`reference/src/calcarc_reference/cases.py:747-836`、
   `testdata/finance.json` へ入る）。

---

## §1 現物（`e266a6d`）

### §1.1 3 つの穴の現状

| 範囲 | 在るもの | 無いもの |
|---|---|---|
| `spell()` | `独立: 不可能` の宣言（**実測前のまま**）／`spell_table.rs` 13 本（入力中の数を engine の `render(...).main` と突き合わせる）／`label_parity.rs`（固定字形 26）／`carried_value_parity.rs`（持ち越しの集合） | **乱択・網羅の検査**。表に無い列は 1 本も見ていない |
| 到達性 | `heavy/tests/ui/reachability.spec.ts` の 4 本（`:29` `:39` `:51` `:88`）。`hist` は web の E2E（`web/tests/e2e/history.spec.ts`）が到達を見ている | 重量級の「すべてのボタン」が `token: null` の面を見ない（`heavy/tests/ui/keys.ts` の `build()` が `token !== null` の面だけを表に入れる）。関数電卓の `token: null` は **Shift（`scientific.ts:18`）と `hist`（`:214`）の 2 つ** |
| 期首 | golden の期首ケース（`cases.py:747-836`）／`compound_ref` の `timing` 引数（`grow` `closed_form` `deposit_for` `periods_for`）／閉形式 `closed_form` は `独立: 別手順` で期首も持つ | 重量級コーパスの期首ケース（`finance-000.json` 3,500 件に 0 件）。harness は `"end"` を 3 か所で固定（`heavy/harness/heavy-harness.ts:161-190`）。**境界証明書は timing を 1 度も読まない**（`heavy/tests/corpus/certificates.ts` に `timing` 0 件）。`heavy:ui` の金融の面も期首の chip を押さない |

### §1.2 履歴の欠陥 7 回——どこに在り、何が捕まえたか

出どころは `history-HANDOFF.md` §0 と各コミットの本文。

| # | 欠陥 | 在った場所 | その版 | 直した版 | 捕まえたもの |
|---|---|---|---|---|---|
| 1 | `zeros3` の `del`（`1 000 DEL` を `1` と綴る。engine は `100`） | `spell` | `8d94c57` | `b89a093` | レビューの探針 |
| 2 | 演算子の `del`（`3 + DEL 4` を `34` と綴る。engine は `7`） | `spell` | `8d94c57` | `5d6b9ba` | レビューの探針 |
| 3 | `dms`（`1 dms 3 0` を `130` と綴る。engine は `1°30`） | `spell` | `8d94c57` | `5d6b9ba` | レビューの探針 |
| 4 | `j` / `Exp` の `del` | `spell` | `5d6b9ba` | `a8a20e9` | レビューの探針 |
| 5 | 字数上限・指数の符号ほか（`Buffer` の 16 の振る舞いのうち 13 が未写し） | `spell` | `a8a20e9` | `58995ec`（本物の `Buffer` を持つ） | 数え直し（`state.rs` を読んだ） |
| 6 | `=` が持ち越しを読むこと | **web**（`ScientificPanel.tsx`） | — | `2a38e32` / `4183e44` | レビューの探針 |
| 7 | エラー中の呼び戻し（effect で写した ref の時間差。H-5） | **web**（`ScientificPanel.tsx`） | — | `99d0310` | 再レビューの jsdom 探針 |

**7 回とも、その時点のゲートは全部緑だった**（同 §0「全ゲートが緑のまま、この族を
1 件も捕まえなかった」）。**捕まえたのは毎回、`spell`（または web）と engine を
直接突き合わせた探針**である。4 の欄の対応（`5d6b9ba` が `j`/`Exp` の `del` を
持つ）は **§2.6 で実測して確かめた**（2026-09-10。`5d6b9ba` は `3 Exp DEL` を
`3 Exp`、`3 j DEL DEL 3` を `3 j 3` と綴り、`a8a20e9` で両方直る）。**2 と 3 は
`8d94c57` にも在った**（同じ列を同じく `34`・`130` と綴る。表の「その版」は
それに合わせて `8d94c57` に直した——初版はここを `b89a093` と誤って書いていた）。

---

## §2 `spell()` — 独立は「不可能」で確定し、守りを「写しの残り」へ向ける

### §2.1 裁定案 1: `独立: 不可能` を確定させる（「実測前」を外す）

理由は 3 つある。

1. **綴りは取り決めである。** 演算子・関数・括弧の字面は「盤面のキーに何と
   書いてあるか」で、数学的な事実ではない。別手順で同じ文字列に着く道が無い
   （`spell.rs` の宣言のとおり）。
2. **数の部分は、設計として engine そのものである。** `58995ec` 以降、`spell` は
   本物の `Buffer` を歩かせ、値を `Buffer::text()` で綴る。Python に独立実装を書く
   とは **`Buffer` をもう 1 つ手で写すこと**であり、それは**この族を 5 回生んだ
   作業そのもの**である（写しは 16 の振る舞いのうち 13 を取りこぼしていた）。
3. **この族の欠陥は「`spell` が値を間違える」ではなく「`spell` が engine から
   ずれる」である。** 前者の oracle は独立実装だが、**後者の oracle は engine
   自身である。** 独立な Python と照合しても、ずれたのが Rust の写しなのか Python
   の写しなのかが言えない。

**`独立:` の宣言は 3 か所とも「不可能」のまま据え置き、「実測前」の但し書きを
外して、代わりに何が守るか（§2.4）を書き足す。** CONTRIBUTING の「宣言は 2 か所」
（設計書の節と公開関数の docstring）はこの節と `spell.rs` で満たす。

### §2.2 独立な golden は、7 回を捕まえられたか——正直な見積もり

**捕まえられなかった、と見立てる。** 確実に捕まえたと言えるものは **0 件**。

| # | 独立な golden（Python が期待値を作る） | 理由 |
|---|---|---|
| 1–5 | **運しだい。確実なものは 0 件** | golden は**誰かが並べた列**しか見ない。**5 件とも、並べる人が思いつかなかった角**だった——`spell` の作者も、表を書いた人も踏まず、探針が初めて踏んだ。**実装を独立にしても、列の選び方は独立にならない。** さらに Python 側で `Buffer` を写すことになるので、同じ角を同じように取りこぼす見込みが高い |
| 6, 7 | **捕まえられない（構造上）** | core の golden は web のコードを 1 行も走らせない |

**裏付けが 1 つある。** `58995ec` の前に `Buffer` を読んで振る舞いを全部数え、
表の行に起こしたところ、**13 行のうち 11 行が実際にずれていた**
（`history-HANDOFF.md` §0）。**行を並べさえすれば、表は捕まえた。**
足りなかったのは oracle の独立性ではなく、**列を並べ尽くすこと**である。

**だから守りは「独立な期待値」ではなく「列を人に並べさせない突き合わせ」にする。**
探針がやっていたこと（`spell` と engine を直接比べる）を、**人が選んだ列ではなく、
網羅と乱択が作る列**に掛ける。これが §2.4 である。

（この節は**見立て**である。書かれなかった golden は測れない。測れるのは §2.4 の
守りのほうで、§2.6 で過去の欠陥版に当てる。）

### §2.3 残っている写し——キーの行き先の振り分け

`58995ec` で数の綴りは本物になったが、**写しは 1 つ残っている。** `spell` の
`match` の各アームは、キーが「バッファを開く／伸ばす」「確定する」「捨てる」の
どれかを、**`engine/mod.rs` の `apply()` を読んで手で振り分けたもの**である
（`spell.rs` の docstring が「`apply()` をそのまま辿った」と書いている）。

**次にこの族が入るならここである**——engine にキーが増えた、あるいは `apply()` の
振り分けが変わった日に、`spell` の `match` は追随しない（既定のアーム `_ =>` は
「確定して字形を足す」に倒れる）。

### §2.4 代わりの守り A——入力中は、綴りの末尾が engine の表示と一致する

**不変条件 A.** 打鍵列のどの prefix でも、**engine が入力中の数を持っている
（バッファが開いている）とき、`spell(prefix)` の最後の語は engine の
`render(...).main` と一致する。**

- **逆写像を要らない。** 綴りを打鍵に戻す関数（§2.5 の案 B）を書かないので、
  **新しい写しが増えない。**
- **振り分けのずれを捕まえる。** `spell` が「確定」に振ったキーを engine が
  「伸ばす」に振れば（逆も）、次の数字で `spell` は新しい語を始め、engine は
  書き足す——**最後の語と表示が食い違う。**
- **偽の赤が出にくい。** 比べるのは engine 自身の表示だけで、`000` と `0` 3 回の
  等価性（上限を跨ぐと等価でない）のような解釈を持ち込まない。

**どこで回すか——網羅と乱択の 2 本。どちらも core のテストに置く。**

1. **網羅の歩き**（`crates/calcarc-core/tests/engine_robustness.rs` の
   `walk_from_the_start` と同じ形）。**ただし同値類に `Zeros3` と `Neg` を足す。**
   いまの `ALL_CLASSES`（`:482`、17 個）には**この 2 つが無い**——欠陥 1（`000` の
   `del`）と 5（指数の符号）の鍵である。`Exp` と `Dms` は在る。
2. **入力に寄せた乱択**（proptest）。**既存の `weighted_key`（`:683` 付近）は
   そのまま使えない**——`Zeros3`・`Exp`・`Dms` を 1 つも引かない。字数上限
   （欠陥 5）には長い数が要るので、数字・`000`・`.`・`Exp`・`Neg`・`j`・`dms`・
   `del` に寄せた生成器を別に持つ。

**置き場を重量級でなく core にする理由。** core のテストは**毎回の PR**で走る
（`Rust core`、制限 10 分、実績最大 93 秒）。重量級はリリースと手動起動でしか
走らない。**この族は PR の中で入る**ので、PR で止める。WASM 境界は `spell` を
素通しで出すだけで、字形は `label_parity.rs` が見ている——**重量級で同じことを
もう 1 度回す理由が無い。**

**A が捕まえないもの（書いておく）。**

- **字形の側のずれのうち、最後の語に響かないもの**——欠陥 2 の、`DEL` の次が
  数字でない形。`b89a093` は `3 + DEL ( 4` を `3 ( 4`、`3 + DEL √` を `3 √` と
  綴る（`+` が消える）が、次の数字は新しい語を始めるので最後の語は表示と一致し、
  A は黙る（2026-09-10 実測）。
  **欠陥 2 の元の列 `3 + DEL 4` は A が捕まえる**——落ちた `+` のせいで `4` が
  前の `3` にくっつき、綴りは `34`、表示は `4` になる（§2.6 で実測）。**初版の
  この項は「`DEL` の直後は開いたバッファが無いので A は何も言わず、次の `4` では
  両方が `4` を示す」と書いていたが、誤りだった**——`4` の時点でバッファは開いて
  いて、綴りの最後の語は `4` ではなく `34` である。元の列は
  **`spell_table.rs:211` の既存の行**（`spell_of(&["3", "add", "del", "4"]) == "3 + 4"`。
  `5d6b9ba` が足したもの）も持ち続ける。
- **途中に取り残された語**——欠陥 4 の `j` の半分。`5d6b9ba` は `3 j DEL DEL 3` を
  `3 j 3` と綴る（engine は `3`）。`DEL` 2 回で engine はバッファを捨てるが綴りには
  `3 j` が残り、次の `3` は新しい語を始める——**最後の語は表示と一致し、A は黙る**
  （2026-09-10 実測。この列で A が鳴るのは `3 j` の時点だけで、それは 4 版に共通の
  `j` の位置の違いである。§2.6）。**A は最後の語しか見ないので、それより前の語の
  ずれには構造的に黙る。** `j` の `del` を持つ表の行は `spell_table.rs:227-228`
  （`j 4 DEL DEL` の形）に在る。
- **web の 2 件**（欠陥 6・7）。core のテストは web を走らせない。守りは既存の
  `carried_value_parity.rs` と web の単体・E2E（`history-HANDOFF.md` §0 の番人の表）。
- **捨てるべき数を確定した**形（`3 (` の `3` を綴りに残す）。表示にも答にも
  現れない（`(` がバッファを捨てるので）。**綴りの見た目だけの違い**で、族の
  定義（式が自分の答を生まない）には当たらない。

### §2.5 採らなかった案 B——答の往復

**B.** `engine(再生(spell(keys))) == engine(keys)`。欠陥 2 も捕まえる。

**採らない。** 理由は 3 つ。

1. **「再生」は `spell` の逆写像を手で書くことである。** 字形→キー、数の文字列→
   キー列——**新しい写し**であり、この族を生んだ形そのものである。
2. **偽の赤が構造的に出る。** `000` を `0` 3 回で再生すると字数上限の手前で振る舞いが
   変わる（`Buffer` の「`000` の上限跨ぎ」）。受け入れ済みの残差
   **`3 × ( DEL =`**（綴りが答 `0` を説明しない。`history-HANDOFF.md` §0「残した
   論点」）も赤になる。どれも除外の一覧を持つことになり、**その一覧がまた腐る。**
3. 欠陥 2 は**既に表の行が持っている**。B の追加分は「表に無い字形のずれ」だけで、
   それを取りに行く代価が 1 と 2 である。

（裁定で B も欲しいとなれば、§7 の 2 番。）

### §2.6 受け入れ——過去の欠陥版で赤くなることを実測する

**A を名指しする前に、A が本当に捕まえることを測る。** 欠陥入りの版は git に在る。

1. 隔離作業木で、**`spell.rs` だけを各欠陥版に戻し**、A（網羅と乱択の両方）を回す:
   `8d94c57`（欠陥 1）、`b89a093`（2・3）、`5d6b9ba`（4）、`a8a20e9`（5）。
2. **赤くなった版ごとに、proptest が縮めた反例（または網羅が止まった列）を記録し、
   §1.2 のどの欠陥かに対応させる。** 「版が赤くなった」は「その版の欠陥を全部
   捕まえた」ではない——`b89a093` は欠陥 2 と 3 を持つが、A が捕まえるのは 3 だけの
   見立てである（**実測で誤り。2 も捕まえた**——下の 2 つ目の表）。
3. 古い `spell.rs` がいまの engine に対してコンパイルできない場合は、**逆に A の
   テストをその時点の木へ持っていって**回す（A が使うのは公開の `spell` /
   `reduce` / `render` / `Key` だけである）。

**実測（2026-09-10）。** 隔離作業木で `spell.rs` だけを各版に差し替え、網羅
（`every_sequence_…`）と乱択（`random_entry_…`）を**別々に**回した。**4 版とも
コンパイルでき（手順 3 は要らなかった）、4 版とも両方が赤。**

| 欠陥版 | 網羅 | 網羅が止まった列（表示 / 綴り） | 乱択 | 乱択が縮めた反例（表示 / 綴り） |
|---|---|---|---|---|
| `8d94c57` | 赤 | `3 3 3 3 j`（`3333j` / `3333 j`） | 赤 | `000`（`0` / `000`） |
| `b89a093` | 赤 | 同じ | 赤 | 同じ |
| `5d6b9ba` | 赤 | 同じ | 赤 | 同じ |
| `a8a20e9` | 赤 | 同じ | 赤 | 同じ |

**4 版とも、止まった列はその版の欠陥ではなかった。** 網羅は深さ優先で最初の
1 件で止まり、乱択は最小の反例へ縮める——どちらも「その版に固有の欠陥」を選ばない。
`000` は欠陥 5 の 1 つ（先頭の 0 の置き換え。`58995ec` が数えた未写しの 13 の中に在る）、
`j` の位置は `58995ec` の本文が「見える帰結」として変えた綴りで、**§1.2 の 7 回の
どれでもない**。どちらも `58995ec` の前の 4 版すべてに在る。**だから「版が赤い」は、
その版の欠陥を捕まえた証拠にならない。**

**欠陥ごとの対応は、再現列を各版に直接当てて測った**（使い捨てのテストで、各
prefix に A と同じ条件を掛けた。コミットしていない）。いまの `spell.rs` ではどの列
でも A は鳴らない（対照）。網羅の深さ 5 の全 prefix 2,218,307 個のうち A が鳴る数は
`8d94c57` 513,558／`b89a093` 514,318／`5d6b9ba` 499,548／`a8a20e9` 490,457／
いまの版 0。

| # | 再現列（網羅の類で書いた形・深さ） | 欠陥版の表示 / 綴り | A | 直した版 |
|---|---|---|---|---|
| 1 | `1 000 DEL`（`3 000 DEL`・3） | `8d94c57`: `100` / `1` | **鳴る** | `b89a093` で黙る |
| 2 | `3 + DEL 4`（`3 − DEL 3`・4） | `b89a093`: `4` / `34` | **鳴る**（見立ては「捕まえない」で、誤り） | `5d6b9ba` で黙る |
| 2 の別形 | `3 + DEL ( 4`、`3 + DEL √` | `b89a093`: 綴り `3 ( 4`、`3 √`（`+` が消える） | **黙る**（§2.4） | — |
| 3 | `1 dms 3 0`（`3 dms`・2） | `b89a093`: `1 dms` の `1°` / `1` から `1°30` / `130` まで | **鳴る** | `5d6b9ba` で黙る |
| 4（`Exp`） | `3 Exp DEL`（3） | `5d6b9ba`: `3` / `3 Exp` | **鳴る**。この prefix は他の 3 版では黙る——**4 に固有** | `a8a20e9` で黙る |
| 4（`j`） | `3 j DEL DEL 3`（5） | `5d6b9ba`: `3` / `3 j 3` | **最後で黙る**（§2.4）。この列で鳴るのは `3 j` の時点（`j` の位置）だけ | `a8a20e9` で綴り `3` |
| 5（字数上限） | `3 000 000 000 000`（5） | 4 版とも: `300000000000` / `3000000000000` | **鳴る**。その前の prefix は黙る——**上限に固有** | いまの版で黙る |
| 5（指数の符号） | `3 Exp 3 +/−`（4） | `a8a20e9`: `3e-3` / `3 Exp 3` | 鳴るが、**`3 Exp` の時点で既に鳴る**（`a8a20e9` は `Exp` を別の語に綴る）。符号だけを切り出せない | いまの版で黙る |

再現列はどれも網羅の類と深さ 5 の中に在るので、網羅は**止まらずに歩き続ければ**
これらの prefix を 1 つずつ確かめる。

**見立てと食い違ったこと（書き戻した）。**

1. **止まった列は 4 版とも、その版の欠陥でなく共通の形だった**（上の 1 つ目の表）。
   見立ては版ごとに、自分の欠陥の列を反例に挙げていた。
2. **欠陥 2 は A が捕まえる。** 見立ては「2 は捕まえない」で、§2.4 も同じ誤りを
   書いていた（§2.4 を直した）。捕まえないのは `DEL` の次が数字でない形である。
3. **欠陥 4 の中身は見立てどおり**（`5d6b9ba` は `Exp` と `j` の両方の `del` を持ち、
   `a8a20e9` が直した）。**ただし A が固有に捕まえるのは `Exp` の半分だけ**で、
   `j` の半分は取り残された `3 j` が途中の語になって黙る（§2.4 に足した）。
4. 欠陥 5 の見立ての列 `1 . 5 Exp 3 +/−` は、`a8a20e9` では `1.5 Exp` の時点で既に
   鳴る。**符号を固有に捕まえた証拠は無い。** 字数上限は固有に鳴る。

**赤くならなかった版は無い。**

**受け入れ条件: 4 版とも赤。欠陥ごとの対応が上の見立てと食い違ったら、その食い
違いを設計書に書き戻してから名指しする。** 網羅の歩きと乱択の所要時間も測り、
`Rust core` の 10 分に対して何秒かを記録する——**網羅 3.88 秒・乱択（2000 件）
1.47 秒**（2026-09-10 実測、いまの `spell.rs`、debug ビルド、手元。再測で
3.88 秒・1.37 秒）。**10 分の 1% に満たない。** CI の機械では測っていない。

---

## §3 到達性——「すべてのボタン」を本当にする

### §3.1 現物

- `heavy/tests/ui/keys.ts` の `build()` は、`SCIENTIFIC_SECTIONS` の面を
  **`token !== null` のものだけ** `BUTTON_FOR` に入れる（第 1 面・第 2 面とも）。
- `reachability.spec.ts:51` の **`every button the keypad claims can actually be
  pressed`** は `BUTTON_FOR` を回す。**名前は「すべてのボタン」だが、実装は
  「トークンを持つボタン」**である。
- 関数電卓の `token: null` は 2 つ——**Shift**（`scientific.ts:18`、`kind: "shift"`。
  `keys.ts` の `SHIFT_ARIA_LABEL` が別に持つ）と **`hist`**（`:214`、
  `action: "history"`、`j` の第 2 面）。予約の空き枠は関数電卓には無い
  （金融の盤面には在る——`FinancePanel.tsx` の `"空き"`）。
- `hist` の到達は **web の E2E が見ている**（`history.spec.ts`「the history key is
  reachable behind Shift…」）。**守られていないのは `hist` ではなく、重量級の
  テスト名の主張**である。

### §3.2 裁定案 2: 盤面の面を 3 種に分類し、分類できない面で落ちる

**`keys.ts` が、`SCIENTIFIC_SECTIONS` のすべての面（第 1 面・第 2 面）を次の
どれか 1 つに分ける。** どれにも当たらない面を見つけたら**例外で落ちる。**

| 種 | 条件 | 行き先 |
|---|---|---|
| トークン | `token !== null` | `BUTTON_FOR`（今のまま） |
| 操作 | `action !== undefined`（`token` は null） | **新設 `ACTION_BUTTONS: ReadonlyMap<KeyAction, ButtonFor>`** |
| Shift | `kind === "shift"` | 今のまま（`SHIFT_ARIA_LABEL`） |

**これが構造の穴を閉じる。** 今日の実害は `hist` 1 つだが、**形としては
「トークンを持たないキーという種類が製品に増えたのに、重量級のモデルが表現できない」**
である。分類が網羅であることを要求すれば、**次に種類が増えた日に、この表が赤くなって
決めさせる**（予約の空き枠が関数電卓に来た日も同じ）。

**`reachability.spec.ts` の変更:**

- `:51` は第 1 面の**トークン ∪ 操作**を回す（今日、第 1 面の操作は 0 個——`hist` は
  第 2 面なので次の項へ）。名前はそのまま、**主張を名前に合わせる**。
- `:88` の「Shift の裏のキーは Shift を押してから現れる」に**第 2 面の操作を足す**。
  `hist` は Shift のあと**ちょうど 1 つ在り、有効である**ことを見る。
- **`hist` は押さない。** 押すと履歴の画面へ移り、以降のテストの盤面が変わる。
  到達（在る・有効）までが重量級の持ち分で、**押した先は web の E2E が持っている。**

### §3.3 番人

| 主張 | 番人 | どこで回るか |
|---|---|---|
| すべての面が 3 種のどれかに分類される | `heavy/tests/unit/` に 1 本（純関数） | **毎回の PR**（`Heavy tooling`） |
| 分類が空で緑を返さない | 同じテストに下限——**操作 ≥ 1、トークン ≥ 1**（数は盤面から導き、46 などを書き写さない） | 同上 |
| `hist` は Shift の裏に 1 つ在り有効 | `reachability.spec.ts:88` | **`heavy:ui`＝CI だけ**（この作業機では走らない） |

**赤の確かめ方（名指しの前に）:** (1) 単体——トークンも操作も `kind` も持たない面を
1 つ差し込むと赤、(2) `heavy:ui`——`hist` を無効にすると赤。**(2) は手元で回せない**
ので、CI の `Heavy corpus` を枝で手動起動して確かめる。

---

## §4 期首を重量級に入れる

### §4.1 現物

- **harness** は複利の 3 op に `"end"` を固定で渡す（`heavy-harness.ts:161-190`）。
  註に「重量級を期首へ広げるのは、期末/期首を足した枝の範囲外」とある。
- **境界証明書**（`certificates.ts`）の `compoundDepositForProbes` と
  `compoundPeriodsForProbes` は、正算 `compound_grow` の入力 `base` を
  **principal・rate・periods_per_year・periods（または deposit）・tax から組み、
  timing を入れない。** harness の既定は `"end"` なので、**期首の逆算ケースを
  入れると、その答を期末の腕で検算してしまう。**
- **コーパス**は `finance-000.json` の 1 枚（`generate_corpus.py:2433`
  `FINANCE_COUNT = 3500`、`:2466` で seed `20260821`）。複利の層に timing は無い。
  試験空間モデルは `finance-v1`（`corpus_calls.py:3036`）、複利 3 op は
  2 因子間ペアワイズ（`:3080` 以降の 3 つの `Requirement`）。
- **検出力**: `step()` の期首の腕 2 本は重量級から見て**空白**である。
  harness が `"end"` しか渡さないので、そこに置いた変異は原理的に捕まらない。
- **golden** は期首を持つ（`cases.py:747-836`）。**「積立 0 は 1 通りに畳んである」**
  （`:754`）——積立が 0 なら期首と期末は同じ答を返すので、**期首の腕の違いを
  見るケースは積立 > 0 でなければならない。**
- **`heavy:ui`** の金融の面は周期と税を「打つのではなく選ぶもの」
  （`finance-cases.ts` の `CHOSEN_NOT_TYPED`）として chip で押す。**期首の chip は
  押していない。** 盤面の期首キーは `timing:start`、アクセシブルネーム
  「**積立を期首に行う**」（`web/src/ui/Keypad/finance.ts:388-392`）。

### §4.2 裁定案 3: 期首は新しいシャード `finance-start-000.json` に置く

**`finance-000.json` は 1 バイトも変えない。**

| | 案 1: 新しいシャード（推す） | 案 2: `finance-000.json` に足す |
|---|---|---|
| 既存 3,500 件 | **不変**（再現性の検査がそのまま通る） | 件数が変わる。層に軸を足せば `cell_id` の綴りも変わる（`corpus_coverage.py:92` 「並べ替えると `cell_id` の綴りが変わる」） |
| 読み方の表（868 / 315 / 2,317） | **そのまま使える** | 作り直し |
| v0.8.0 / 0.9.0 の検出率との比較 | **そのまま比べられる** | **また比べられなくなる**（09-10 に 1 本ほどいたばかりの形） |
| 既存変異の `minRate` | 鍵（シャード名）が変わらない | 分母が変わる |
| 手数 | `ALL_SHARDS` に 1 行（`detection-power.mjs`）。**定数の註は腐っている**——いまも「正当に 19 枚目を足す日には…」と書くが、`ALL_SHARDS` は**すでに 19 個**を持つ。期首のシャードは **20 枚目**で、**註もこの PR で直す**。**同じ形の腐りがもう 1 つ**——`generate_corpus.py` の `_shards` の docstring が「書き出す 18 枚」と書くが、実際は 19 枚を書き出す。**これも直す** | 少ない |

**案 1 を推す。** 手数の差より、**比べられることの値打ちが大きい。**

### §4.3 生成（`reference/`）

- **別の seed** で作る（例: `20260910`）。`finance-000.json` の乱数列をずらさない
  （`corpus_calls.py` の既存の流儀——「無関係な `compound_grow` の乱数列をずらさない」）。
- **op は複利の 3 つだけ**。すべて `timing: "start"`。**`timing: "end"` の明示は
  入れない**（それは `finance-000.json` と golden の `cases.py:785-796` が持つ）。
- **積立 > 0 を要求する**（§4.1 の `cases.py:754`）。積立 0 のケースは期首の腕の
  違いを 1 度も見ないので、**入れても検出力にならず、件数だけを水増しする。**
- **非単調な谷**（`compound_periods_for/non_monotone_net`、`corpus_calls.py:1886`）は
  **期首・積立 1 円で探してから決める。**【訂正 2026-09-10、計画を書く段で】
  既存の谷は**積立 0**（`_find_non_monotone_net_valley(…, 0, …)`）で、期首の腕を
  1 度も通らない。積立を 1 円足すと、谷を作っていた「利息 1 円ずつ × 税の床の
  同時跳び」が**積立の 1 円で相殺されて平らになる見込み**がある（**見立て。実測前**）。
  **見つかれば層を足し、見つからなければ層を入れずに理由を書く。**
  二分探索の変異（`periods-for-binary-search`）は timing に依らないので、
  `finance-000.json` の 1 件（`fin-000265`）がそのまま捕まえ続ける。
  **【実測 2026-09-11、Task 8】見つからなかった——見立て「平らになる」は当たった。**
  元本 100 万円・積立 1 円・年利 0.0001%・年 1 回・税あり・期首で
  `_find_non_monotone_net_valley(1_000_000, 1, num, den, 200, START)` を
  1〜200 期で探すと、`RuntimeError`（「search_limit=200 期以内に谷が見つからない」）
  で止まる。手取りの期ごとの差は 0（2 回、期 20 と 40——税の床の同時跳び）・
  1（36 回）・2（161 回）で、**負は 0 回**。積立 1 円が同時跳びの税の増分 2 円を
  ちょうど相殺し、谷ではなく平らな段になる（対照: 同じ入力の積立 0・期末は
  従来どおり期 19 → 20 → 21 で谷を見つける）。**よって期首のシャードに谷の層は
  入れない。** 二分探索の変異は上のとおり `fin-000265` が捕まえ続ける。
- **試験空間モデルは `finance-start-v1` を新しく名乗る。** 因子表は
  `PAIRWISE_COMPOUND_*_FACTORS` を**参照し、写さない**（`corpus_calls.py:3015` の
  「ここに水準を書き写さない」）。`finance-v1` の意味は変えない。
- **件数（実測前）。** まず `finance-000.json` の複利の比率（437 / 431 / 315＝1,183）に
  揃え、**被覆の門が要求するセル数で下限を決める。**
- **公開する参照関数は増えない。** 期待値は既存の `compound_ref.grow` /
  `deposit_for` / `periods_for`（`timing` 引数あり）から取る。CONTRIBUTING の
  「独立」の宣言は既存のものがそのまま効く——**`grow` は `独立: 不可能`（Rust と
  同じ 1 期ごとの floor のループ）**である。したがって**このシャードが確かめるのは
  「Rust の期首の腕と Python の期首の腕が同じ手順で同じ答を出すか」**であり、
  **別手順の検算は `closed_form`（`独立: 別手順`、期首を持つ）が参照側の
  テストで持つ。** そう読めるように、シャードの `generated_by` の説明に書く。

### §4.4 harness と境界証明書

- **harness**: 複利 3 op は `input.timing` を読み、**無ければ `"end"`**。
  `"end"` / `"start"` 以外は**例外で落とす**（黙って既定へ倒さない）。
  `finance-000.json` は `timing` を持たないので**振る舞いは変わらない。**
- **境界証明書（必須）**: `compoundDepositForProbes` / `compoundPeriodsForProbes` の
  `base` に **`timing` を運ぶ。** これを落とすと、期首の逆算ケースの証明が
  **期末の腕で行われる**——答が合っていれば赤くなって誤警報、**運悪く期末でも
  境界に当たれば黙って緑**になる。**このシャードと同じ PR で、証明書の直しを
  先のコミットにする**【訂正 2026-09-10】——先に証明書だけを直して `finance-000.json`
  の既存ケースが 1 件も動かないことを確かめ、それから期首を足す。**どちらの変更で
  何が動いたかが分けられ、しかも「シャードは在るが証明書が期末で検算する」状態が
  どのコミットにも無い。**
- **証明書の番人**: 期首のケースだけを 1 件入れた単体で、`base` が `timing` を
  持つことを見る（`heavy/tests/unit/`、純関数の側）。

### §4.5 検出力（`heavy:power`）

- **新しい変異 `compound-start-deposit-at-end`**: 期首の腕を期末の腕と同じにする
  （積立を利息の後へ）。**期首の腕 2 本を同時に動かす**——片方だけだと「積立が
  消える」別の、はるかに大きい壊れ方になる（09-10 の当て直しで確かめた教訓と同じ）。
  `expectShards` は **`finance-start-000.json (calls)` だけ。**
- **既存の `compound-deposit-at-start` は `finance-000.json` だけを期待したまま**
  にする。これは期末の腕しか動かさないので、**期首のシャードでは 0 件であるべき**
  ——`detection-power.mjs` は「捕まらないはずの変異が捕まっても失敗」とする。
  **2 本で腕の独立を両方向から確かめる**ことになる。
- **`compound-round-once-at-maturity` と `periods-for-binary-search`** は期首の
  シャードにも届く（前者は `grow_with_timing` のループ、後者は `periods_for` の
  ループに在り、どちらも timing を問わず通る）。**`verdictFor` は「反応した
  シャードの集合」が `expectShards` と一致することを求める**ので、期首のケースが通る
  コード（`compound.rs`・`compound_inverse.rs`・`tax.rs`・複利が使う `loan/rate.rs` の
  `Rate`）に置いた変異は、**`expectShards` に新しいシャードを足さないと赤くなる。**
  どれが反応するかは**推測せず `heavy:power` で測ってから**書く。**`finance-000.json` の件数は
  612 / 1 のまま変わらないこと**を回帰として見る。新しいシャードの件数は新しい行として
  報告書に出る。
- **`minRate`**: 新しい変異と新しいシャードの分は、**最初の CI の実測から決める。**
  それまでの版は下限 1（`minRate` 未指定＝`verdictFor` の `max(1, …)`）とし、
  **註に「未測定」と書く**。実測したら値を入れ、註を実測に置き換える。

### §4.6 `heavy:ui`（実画面）

- `timing` を `CHOSEN_NOT_TYPED` に足し、`TIMING_KEY = { end: "積立を期末に行う",
  start: "積立を期首に行う" }` を持つ（**アクセシブルネームは実装の日に
  `finance.ts` から当て直す**）。期首のケースは、方式の面で期首の chip を押してから
  打つ。
- **既存の 16 件は `finance-000.json` からだけ引き、1 件も変えない。** 期首は
  複利の 3 面に 1 件ずつ、新しいシャードの正常ケースから足す。
- **積立の位置は毎回押す。** 選択は `localStorage` に保存され、ページを開き直しても
  次のケースへ持ち越される——期首のケースのあとの期末のケースが期首で打たれる。
  **既定を仮定しない**（税を毎回押しているのと同じ理由。記憶
  `harness-state-outlives-ac` と同じ形）。
- 【訂正 2026-09-10】**押下台帳は動かない。** 当初「動く」と書いたが、
  `finance-ui.spec.ts` は**台帳に載らない**（docstring:「意図的に `recordPress` を
  呼ばない」）。期首のシャードは関数呼び出しでキー列を持たないので、科学計算の
  打鍵の標本にも入らない。
- **この作業機では走らない**（segfault）。確かめは CI の `Heavy corpus` の手動起動で行う。

### §4.7 読み方の表——何が変わり、何が変わらないか

**`finance-000.json (calls)` の表は変わらない:**

```
compound_grow 437 / compound_deposit_for 431 / compound_periods_for 315 / loan_* 2,317   計 3,500
```

**新しい表が 1 つ増える（件数は実測前）:**

```
finance-start-000.json (calls)   すべて複利・すべて期首・積立 > 0
  compound_grow / compound_deposit_for / compound_periods_for   （loan_* は 0 件）
```

報告書の検出の表では、**変異ごとの「赤くなったシャード」欄に新しいシャードが
並ぶ**。読むときの注意は 09-10 と同じ——**太字の「件数」は捕まえた数の合計で、
分母ではない。** 率はシャードごとに、そのシャードの件数で割って出す。

### §4.8 受け入れ

- `finance-000.json` の digest が**変わっていない**（再現性の検査）
- 新しいシャードが被覆の門を通る（`finance-start-v1` の要求セルに未達が 0、または宣言つき）
- CI の `Heavy corpus`（枝で手動起動）で:
  - `compound-start-deposit-at-end` が新しいシャードで**赤**、`finance-000.json` で**0**
  - `compound-deposit-at-start` が新しいシャードで**0**、`finance-000.json` で**910**（09-10 と同じ）
  - `compound-round-once-at-maturity` の `finance-000.json` が **612**、`periods-for-binary-search` が **1**
  - `Corpus vs reference` の所要時間（**実測前**。いまの 85 分の制限の内側であること）

---

## §5 番人の表（この門で足すもの）

| 主張 | 番人 | 回る所 |
|---|---|---|
| 入力中は `spell` の末尾が engine の表示と一致する | core の網羅の歩き（同値類 + `Zeros3` `Neg`）と入力寄せの proptest | 毎 PR（`Rust core`） |
| その守りが過去の欠陥を捕まえる | §2.6 の実測を設計書に記録（**常設のテストではない**——欠陥版は過去のコミットで、毎回は回せない） | 実装時 1 回 |
| 盤面の面は 3 種に分類され、漏れが無い | `heavy/tests/unit/` の分類テスト（下限つき） | 毎 PR（`Heavy tooling`） |
| `hist` は Shift の裏に在り有効 | `reachability.spec.ts:88` | CI の `heavy:ui` |
| 期首のケースが harness を期首で通る | harness の `timing` の読み取り（未知の値で落ちる）と単体 | 毎 PR（`Heavy tooling`） |
| 境界証明書が期首で検算する | `base` に `timing` があることの単体 | 毎 PR（`Heavy tooling`） |
| 期首の腕を重量級が見ている | `compound-start-deposit-at-end`（新シャードで赤、旧で 0） | リリースと手動（`Heavy corpus`） |
| 期末の変異は期首に触れない | `compound-deposit-at-start` が新シャードで 0 | 同上 |

---

## §6 やらないこと

- **`spell` の Python 参照実装を書かない**（§2.1。書けば `Buffer` の写しになる）
- **答の往復（案 B）を入れない**（§2.5。裁定で覆せる）
- **`finance-000.json` を変えない**（§4.2）
- **`release.yml` の既存の値、`finance-000.json` の既存の `minRate` を動かさない**
- **`heavy:ui` をこの作業機で回さない**（CI で確かめる）
- **門の 1 と 2 のファイルに触れない**

---

## §7 裁定が要ること

1. **`spell` の `独立: 不可能` を確定させてよいか**（§2.1）。代わりの守りは A（§2.4）
2. **案 B（答の往復）も入れるか**（§2.5）。推すのは「入れない」
3. **期首は新しいシャードに置くか**（§4.2）。推すのは案 1（新しいシャード）
4. **`heavy:ui` の期首 chip をこの門に含めるか**（§4.6）。含めると確かめは CI だけになる。
   推すのは「含める」——実画面の chip から core までの配線は、ほかの誰も見ていない
5. **試験空間モデルの名前**（§4.3）。推すのは `finance-start-v1` を新しく名乗る

---

## §8 この設計書の弱い所・測っていないこと

- **§2.2 の見積もりは見立てである。** 書かれなかった golden は測れない。
- **§2.6 の欠陥ごとの対応は 2026-09-10 に実測した**（欠陥 4 の中身も当てた）。
  ただし当てたのは**選んだ再現列**であって、欠陥の形を尽くしてはいない。**網羅と
  乱択が止まった列そのものは、4 版とも欠陥 1〜4 のどれでもなかった**（§2.6）。
- **網羅の歩きの所要時間は 3.88 秒、乱択（2000 件）は 1.47 秒**（2026-09-10 実測、
  debug ビルド、手元。再測で 3.88 秒・1.37 秒）。`Rust core` の 10 分に対して
  1% に満たない。**CI の機械での秒数は測っていない。**
- **期首で非単調な谷が見つかるか**は探してみるまで分からない。
- **新しいシャードの件数と、`Corpus vs reference` の伸び**は実測前。
- **`heavy:ui` の変更は手元で 1 度も走らない。** 確かめは CI の手動起動だけである。

---

## §9 実装の順と PR の分け方

3 つは互いに独立で、**3 本の PR に分ける**（同じファイルを触らない）。

1. **§3 到達性**（小さい。単体は手元、`heavy:ui` は CI）
2. **§2 `spell` の守り**（§2.6 の実測が受け入れ条件。core だけ）
3. **§4 期首**（いちばん大きい。`reference/`・harness・証明書・`heavy:power`・`heavy:ui`。
   CI の手動起動で §4.8 を測る）

各 PR は**変異させて赤を見てから番人を名指しする**。push と PR の作成は利用者の手番である。
