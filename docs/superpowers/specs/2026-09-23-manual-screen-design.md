# 0.9.6 マニュアルを読む画面 —— 設計（**草案。承認前に実装しない**）

**書いた日**: 2026-09-23。**土台**: `origin/main` = `3d8a422`（v0.9.5 出荷済み）。
**書いた人**: 実行役 calcarc-ba。**読む人**: レビュー役 calcarc-1e、監視役 calcarc-e3、利用者。

**この設計書が決めるのは「何を作るか」までで、裁定が要る点は §7 に集めた。**

---

## §0 出どころ（**利用者が言ったこと／こちらが足したこと**を分ける）

**利用者の言葉**（2026-09-23、監視役 calcarc-e3 経由。綴りは届いたまま）:

> **「iPhone からだとページが戻れない。内部のブラウザ表示で戻ることができない。」**
> **「PDF が開くが、閉じる・戻るのボタンが見当たらない。」**

**利用者の方針**（同日、e3 経由）:

> **「内部に PDF を持つ形に戻して、適切に表示画面を作る」**

**こちらが足した要件は、いまのところ 0 件である。** 幅に流れること・目次・検索は、
**利用者からは挙がっていない**（監視役の見立てとして出たが、選ばれていない）。
**足すなら §7 の裁定として出す**——**足した理由を「利用者が言ったこと」と混ぜない。**

## §0.1 前提の訂正（**発注文にあった 3 つの誤り。現物で当て直した**）

監視役の発注文は「0.9.3 の画面を畳んだ理由は利用者の『きれいになっていない』であり、
復活させるならその理由に答える形でなければ同じことになる」としていた。**3 点とも違った**
（2026-09-23、実行役が印字で確認。監視役は訂正を受け入れ済み）:

1. **「きれいになっていない」は、畳んだ先のリンク集の見た目を指す語**である。
   全リポジトリでこの語を含むコミットは `54adfd2` の 1 件だけで、その行は
   「**隙間を詰めた**。44px の標的が並ぶので、**隙間を足すと間延びして
   「6 つの浮いた文字」に見える**（利用者の「きれいになっていない」）」。
   **マニュアル画面の出来についての言葉ではない。**
2. **畳んだ理由は「PDF 3 冊がリンク集に直接並ぶので、画面を 1 つ持つ理由が無くなった」**
   （`54adfd2` の本文と、`web/src/route.test.ts:155-162` に残った註）。
3. **旧 `ManualPage` は本文を 1 文字も表示していなかった。** 121 行のうち中身は
   PDF 3 本のリンク・この版の Release・「計算機に戻る」だけで、`marked` の出現は **0 件**
   （`git show 54adfd2^:web/src/ui/Manual/ManualPage.tsx`）。

**含意（実行役の見立て）**: **本文を画面の中で読ませるものは、旧画面の復活ではない**。
**逆に「画面の中に PDF へのリンクだけを置く」案は、畳んだ理由にそのまま当たる**
（§3 の案 D）。**0.9.4 で消した理由を知らずに同じ物を作り直すのが、いちばん起きやすい失敗である。**

---

## §0.2 裁定（2026-09-23。**出どころを分ける**）

**利用者の裁定（3 件）**: **Q1 = (イ) タブではない画面 `#manual`** ／ **Q2 = PDF は画面の中に
保存・印刷用として 3 冊（サイト内配信に戻したうえで）** ／ **Q4 = 目次・節リンク・検索は 0.9.6 では作らない**。

**監視役 calcarc-e3 の裁定（4 件。利用者には上げていない。覆されうる）**:
**Q3 = 写真は画面に出さない**（裁定 #1 を動かさない。動かすならそれ自体を利用者に上げる別の話）／
**Q5 = 3 冊を 1 つの塊で。ただし最初の段でバンドルの増分を実測し、数が悪ければその時点で報告**／
**Q6 = 画面の綴りは日本語のまま**（本文だけ英語）／
**Q7 = `#manual` に戻し、`route.test.ts:163-166` は消さずに主張を替える**。

**この設計書の以降の節は、この裁定の上に立っている。**

---

## §1 いまの現物（**すべて 3d8a422 の印字**）

| 事実 | 出どころ |
|---|---|
| リンク集は 6 本（GitHub／PDF 3 冊／ライセンス／検査結果） | `web/src/ui/Footer/LinksPopup.tsx:85-121` |
| PDF の行き先は GitHub Release の添付 | 同 `:64` `manualPdfUrl` |
| **サイト内配信は生きている** | `deploy.yml:116` が PDF を `dist` に入れ、`functions/manual/[[path]].js` が配る。`check-sw.mjs:99-108` が SW の除外を見る |
| 素材は 3 冊 1,290 行・74,527 バイト（`detail.ja.md` が 863 行・51,366 バイト） | `wc -lc docs/manual/*.md` |
| 画面写真は `shot:` で 10 参照・6 種 | `manual-shots.test.ts:40` の `SHOT_REF_FLOOR = 10` と一致 |
| **写真はリポジトリにもバンドルにも無い** | `.gitignore` の `web/manual-shots/`（裁定 #1「写真はリポジトリに置かない」）。作業木にディレクトリが存在しない |
| Markdown → HTML → PDF は**ビルド時だけ** | `web/scripts/manual/markdown.ts`（`marked` 18.0.11）＋ `build.mjs`（Chromium の `page.pdf()`）。**実行時には 1 行も走らない** |
| **`marked` は devDependency** | `web/package.json`。dependencies は `react`・`react-dom`・`workbox-window` の 3 つだけ |
| **目次も見出しの id も無い** | `renderManual` が差し替えるレンダラは `image` の 1 つだけ（`markdown.ts:329-346`） |
| 章の形は `## <n>. <題>` | `quick.ja` 7 章・`quick.en` 7 章（＋番号なしの用語表）・`detail.ja` 10 章。`###` は detail に 49 個 |
| 画面は 4 タブ・13 route | `route.ts:14`・`screenName.ts:25-50` |

**2 回外した手**（どちらも「端末が PDF をどう扱うか」に賭けていた）:

- **0.9.4**: `Content-Disposition: attachment`。**本番で付いていた**（監視役の `curl`、09-18）が、
  **iPhone のホーム画面アプリでは戻れなかった**（利用者の実機、09-18）
- **0.9.5**: **別の生成元（GitHub Release）へ飛ばす**。**内部のブラウザ表示から戻れなかった**
  （利用者の実機、09-23）。**「別の生成元なら戻れる」は、私たちが「推測」と印を付けていた部分**で、
  **実機で否定された**

→ **設計の第一原則: 既定の読み口を PDF にしない。** PDF を開く道は、**保存・印刷したい人の副経路**とする。

---

## §2 この作業機では測れないもの（**前提にしない**）

- **iOS のホーム画面アプリで、`<iframe>` / `<object>` / `<embed>` の PDF がどう出るか**
  （そもそも出るか、スクロールできるか、**そこから戻れるか**）
- **iOS で「戻る」手段（バツ・戻る）が出るかどうか**の一般則
- **PDF ビューアの有無に依らない挙動**

**この機に iOS は無い**（0.9.4 の Function の註と同じ）。**測れないものを含む案は §3 でそう印を付け、
選ぶ場合は「利用者の実機での確認」を手順に組み込む**（§6）。

---

## §3 選択肢

| 案 | 何を読ませるか | 利用者の要件（戻れる）を満たすか | iOS で測れるか | 主な代償 |
|---|---|---|---|---|
| **A（推し）** | **本文を画面の中に出す。Markdown → HTML への変換はビルド時**（既存の `markdown.ts` を使い、成果をバンドルに入れる） | **満たす**（アプリの画面を出ない。戻るのはアプリの中の操作） | **測れる**（手元の E2E で実ブラウザを通せる） | バンドルが増える（本文 74KB 前後。gzip は**未実測**）。写真の扱いに裁定が要る（§7 Q3） |
| B | 本文を画面の中に出す。**変換は実行時**（`marked` を dependencies へ） | 満たす | 測れる | **同じ変換器が 2 つの経路で動く**（PDF とアプリ）。ライブラリがバンドルに入る（**未実測**）。**A に対する利点が無い**——素材は固定で、実行時に変換する理由が無い |
| C | **PDF そのものを画面の中に埋める**（`<iframe>` / `<object>`） | **不明**（iOS で中身がどう出るかも、戻れるかも**測れない**） | **測れない** | **2 回外した手と同じ賭け**になる。PC では見えても iPhone で外れる可能性が残る |
| D | 画面の中に **PDF へのリンクだけ**（＝0.9.3 の `ManualPage`） | **満たさない**（PDF を開いた先で行き止まりになる。**利用者が踏んだ不具合そのもの**） | — | **0.9.4 で畳んだ理由にそのまま当たる**（§0.1）。**推さない** |

**どの案とも併用する副案 E**: **PDF はサイト内配信に戻し**（`manualPdfUrl` を `/manual/…` へ）、
**画面の中から「保存・印刷用の PDF」として出す**。**既定の読み口にはしない。**

**推しは A ＋ E。** 理由:
- 利用者の要件（戻れる）を、**端末の挙動に賭けずに**満たす唯一の案である（C は賭けが残る）
- **素材は 1 つのまま**（`docs/manual/*.md`）。PDF と画面が同じ Markdown から出る
- **手元の実ブラウザで測れる**ので、「直った」を私たちの側で言える範囲が広い
  （**iPhone での最終確認は、それでも利用者が要る**——§6）

---

## §4 推し案（A ＋ E）の形

### §4.1 どこに置くか（**裁定済み: (イ)**。利用者、Q1）

| 形 | 中身 | 番人への当たり |
|---|---|---|
| (ア) **5 つ目のタブ** | ナビに「Manual」を足す | **4 タブの不変が壊れる**: `Nav.test.tsx:34`・`:89`、`loan.spec.ts:34`、`nav.spec.ts` の字面 4 行。**0.9.3 の設計書 §2.2 が明示的に避けた形**（「4 タブの不変を壊す」） |
| (イ) **タブでない画面**（`#manual`。0.9.3 と同じ口） | リンク集から開く。ナビに現在地を出さない | `route.test.ts:155-166` が**確実に赤**（畳んだことを固定している）。`route.ts` の `Record<ModuleId,…>` を増やすなら typecheck も動く |
| (ウ) **画面ではなく、重ねて出す面**（リンク集と同じ流儀の dialog） | route を増やさない | route の番人は動かないが、**URL で共有できない**・**Escape の層が 3 つ目になる**（`escapeLayers.ts:35-40`。順序の裁定が要る） |

**採るのは (イ)**（利用者の裁定。実行役の推しと同じ）。理由: **読み物は URL で指せるほうがよい**（「この節を見て」と言える）、
**0.9.3 で一度通した口**があり（`54adfd2` を読めば復元の形が分かる）、
**4 タブの不変を壊さない**。**(イ) を採ると `route.test.ts:155-166` を書き替えることになる**
——**これは「消す」のではなく「主張を替える」**（0.9.4 が同じことを逆向きにやった）。

### §4.2 戻る手段（**利用者の要件そのもの**）

- **画面の中に「計算機に戻る」**を置く（0.9.3 の画面が持っていた形。`#scientific` へのリンク）
- **リンク集から開いた場合**、リンク集は閉じる（いまの `hashchange` で閉じる仕掛けがそのまま働く）
- **Escape**: (イ) なら層は増えない（画面なので `ac` の層のまま）。**(ウ) を選ぶ場合だけ、層の順序の裁定が要る**
- **ブラウザの戻る**も効く（ハッシュなので履歴に載る）

### §4.3 中身（**最小**）

- **3 冊の入口**: いまのリンク集と同じ綴り・同じ順（`MANUALS`）。**画面の中で冊を選ぶ**
- **本文**: ビルド時に Markdown から作った HTML を、そのまま流す。**幅に流れる**
  （**利用者が言ったのは「戻れない」まで**だが、**画面に出す以上、幅の中に収める必要がある**
  ——これは実装上の必然であって、要件の追加ではない）
- **目次・節リンク・検索は、この版では作らない**（**裁定済み**。利用者、Q4）
- **PDF**: 「保存・印刷用」として、サイト内の `/manual/…` を 3 冊ぶん（副案 E）

### §4.4 写真（**裁定済み: 1**。監視役、Q3）

いまの写真は**追跡していない**（裁定 #1「マニュアルの写真はリポジトリに置かない」）。3 択のうち **1 を採る**:

1. **画面には写真を出さない**（本文だけ）。PDF には今までどおり入る。**裁定 #1 を動かさない**
2. **写真をバンドルに入れる**（`web/public/manual/` などへ。**裁定 #1 を覆す**）。
   **6 種の PNG のバイト数は未実測**
3. **写真の場所にリンクだけ置く**（PDF を見てください）——**PDF を既定の読み口にしない方針と噛み合わない**

**★ 帰結（1e の注記。実行役が現物で確認）: 画面用の変換に `renderManual` はそのまま使えない。**
`web/scripts/manual/markdown.ts:330-345` の `image` renderer は、**`shot:` を追跡外の写真の URL に
差し替え、写真が無ければ例外を投げる**（`写真が無い: shot:…`）。**写真を出さない（Q3）以上、
画面用には `image` を落とす別の renderer が要る。**
**落とし方**: 実行役は最初「**`![…](shot:…)` の説明文（alt）は 1 行として残す**」を選び、
**差し戻せる 1 か所**にしてあった。**利用者が実物を見て「落とす」と決めた**（裁定 2026-09-23）
——**簡易マニュアルの 2 章で「画面: …」の行が 4 本続く所**を見ての判断で、理由は
「**写真が無い以上、説明だけ残っても読み手には空振り**」。**写真も説明の行も出さない。**

### §4.5 分量とオフライン

- 本文 3 冊で 74,527 バイト（`detail.ja.md` が 51,366）。**HTML にしたあとの大きさ・gzip 後は未実測**
- **バンドルに入れれば precache に入り、オフラインで読める**（いまの SW は `.wasm` の有無しか見ていない
  ——`check-sw.mjs:77-79`。**大きさの番人は無い**）
- **3 冊を 1 つの塊にし、本体とは別にする**（動的 import。監視役の裁定 2026-09-23。
  **「1 つの塊」は満たしつつ、本体と混ぜない**）。**初回の読み込みは増えず、precache には入る**
  ——**オフラインで読めることを失わない**のが、この判断の条件である（番人を置く）。
- **Task 1 で印字する 4 つ**: **Vite 通過後の塊の実寸**（gz 込み）／**画面の CSS の増分**／
  **precache にマニュアルの塊が在るか**／**更新のたびに流れる量**（塊の名前のハッシュが版ごとに
  変わるかで見る。**「変わらないはず」は監視役の見立てで、未実測**）。
  **測った数はそのまま出す**——**「悪い」の線は実測の前には引けない。**

### §4.6 `PDF_DISPOSITION = "attachment"` をどうするか（**0.9.4 の手の後始末**）

**いまの註は「ホーム画面のアプリが PDF に乗っ取られるのを止めるため」と書いていない**
（0.9.5 で書き直した）が、**「撤去は 0.9.6」という前提の上に立っている**
——**その前提が逆になった**（§6）。

**0.9.6 での扱い（実行役の案）**: **ヘッダは残す。ただし理由を書き直す。**

- **残す理由（新）**: **PDF は保存・印刷用の副経路**になった（Q2）。**PC で「その場で開く」より
  「保存される」ほうが、その位置づけに合う。**
- **残さない理由にならないもの**: 「iPhone の乗っ取りを止める」——**止まらなかった**（実測 2 件）。
  **この理由はもう書かない。**
- **`Function` そのものは残す**——**役目は「実体の無い `.pdf` に 404 を返す」こと**で、
  これは `_redirects` の `/* /index.html 200` がある限り要る（`functions/manual/[[path]].js` の冒頭）。
  **0.9.6 では、この役目のほうが主になる。**

**代案**: ヘッダを外す（PC でその場に開く）。**iPhone では結果が変わらない**（乗っ取りは
ヘッダに依らない——実測）。**利用者に上げるほどの差ではない**と見ているが、裁定が要るなら §7 Q8。

---

## §5 動く番人（**grep の印字から。数は数えた**）

**確実に赤（(イ) を採る場合）**
- `web/src/route.test.ts:163-166`（`#manual` が既定へ倒れることを固定）→ **主張を替える**
- `web/src/route.ts:47`・`:55` の `Record<ModuleId, …>`（新しい id を足すなら typecheck が赤）
- `web/src/ui/screenName.ts:25`・`:71`、`web/src/ui/screenName.test.ts:24`（導出式 `1 + 8 + 3 + 1` が新画面を数えない）

**条件つきで赤**
- `web/tests/e2e/screen-identity.spec.ts:72`（同じ導出式）・`:85`（約束の表に足したら巡回にも足す）
- `web/tests/e2e/short-screens.spec.ts:64`（約束の表に足した画面に押せるボタンが無いと赤）
- `web/tests/unit/manual-key-names.test.ts:321`（マニュアル本文が新しい画面名を `【…】` で呼ぶなら、
  定義元を `SOURCES` に足す）
- `web/tests/unit/manual-shots.test.ts:72`（画面の写真を撮って本文から参照しないと赤）
- `web/src/ui/Footer/Footer.test.tsx:75`（リンク集の本数を変えるなら赤）・`:101`（`MANUALS` 3 冊）

**動かない（＝穴。置くべき番人がここに在る）**
- `web/src/route.test.ts:121` の下限 13（**下限なので画面を足しても緑**）
- **`web/tests/e2e/viewport-budget.spec.ts:55-69` の `TABS` は閉じた 11 行の字面表**
  ——**新しい画面は自動では巡回に入らない**（在庫は 3 か所: この表・`docs/definition-of-done.md:164`・同ファイルの註）
- `web/scripts/check-sw.mjs`（画面数も資産の大きさも見ていない）
- `web/src/ui/escapeLayers.test.ts`・`web/src/ui/escapeOrder.test.tsx`（**層の個数を見る assert は無い**。
  順序だけを 2 本で見る）

**★ 0.9.3 の設計書が「未実測」と書いた項目が、そのまま生き返る**
（`docs/superpowers/specs/2026-09-17-v0.9.3-groundwork-design.md:74-75`、検証役 calcarc-be の指摘。
実行役が現物で確認）:

> **`#manual` を足したときに読み上げ・WebKit・低い画面がどうなるか**は未実測
> （`<dialog>` も popover も**この製品では 1 度も使っていない**ので、先例が無い）。

**0.9.3 で `#manual` を出したときは、この 3 つを測らないまま出荷した**（そして 0.9.4 で畳んだので、
**測る機会そのものが消えた**）。**0.9.6 では測る**——**読み上げ（E2E）・WebKit（CI のジョブ）・
低い画面（`short-screens`）の 3 つを、下の番人の一覧に入れる**。**「先例が無い」はいまも真**である
（この製品は `<dialog>` も popover も使っていない。**リンク集は `role="dialog"` を手で当てた div**）。

**新しく要る番人（案）**
1. **本文が画面に届いていること**: 3 冊それぞれについて、`docs/manual/*.md` の**章の題**が画面に出る
   （`extractChapters` の出力と突き合わせる。**素材が正で画面を直す向き**）
2. **PDF の行き先がサイト内であること**（`manualPdfUrl` が `/manual/` を返す。0.9.5 の番人の向きを戻す）
3. **画面から出ないこと**: マニュアル画面の中のリンクで、**アプリの外へ出るものが 0 本**
   （PDF の保存リンクは別扱いにするなら、その 3 本だけを許す）
4. **E2E**: 実ブラウザで「リンク集 → マニュアル → 本文が見える → 計算機に戻る」が 1 本
   （**jsdom は読み上げの木を組まない**ので、役割の意味論は E2E）
5. **`viewport-budget` の穴**: 新しい画面を巡回に足すか、**足さない理由を 3 か所の在庫に書く**

---

## §6 段階（**0.9.6 の最小と、後に回すもの**）

**0.9.6 に入れる（最小）**
1. マニュアル画面（(イ)、3 冊、本文、計算機に戻る）
2. PDF をサイト内配信へ戻す（副案 E。`manualPdfUrl` の 1 か所）
3. リンク集の行き先を画面へ（PDF は画面の中の副経路に移す）
4. マニュアル 3 冊の本文の直し（読み方が変わる）
5. 上の番人 1〜5

**後に回す**
- 目次・節リンク・検索（§7 Q4）
- 写真をバンドルに入れる（§7 Q3 で 2 を選んだ場合、量が読めるまで）
- **サイト内配信の撤去**——**方針が逆になった。撤去しない**（下の「取り消しの後始末」）

### §6.1 取り消しの後始末（**「撤去は 0.9.6」と書いた場所。検証役 calcarc-be が挙げ、実行役が現物で確認**）

**直す（6 か所。行番号は 2026-09-23 の座標。実装のときに当て直す）**:

| 場所 | いま何と書いてあるか |
|---|---|
| `functions/manual/[[path]].js:94` | 「0.9.5 から、アプリはここを指していない」 |
| 同 `:112-115` | 「残す理由は出荷前の夜に経路を動かさないため。**撤去は 0.9.6 の宿題**」 |
| `.github/workflows/deploy.yml:247-248` | 「この経路は……残っている（**撤去は 0.9.6**）」 |
| `web/scripts/check-sw.mjs:94` | 「リンク集はここを指さず GitHub の Release を指すが、**配信は 0.9.6 まで残る**」 |
| `web/tests/unit/manual-function.test.ts:32,40` | テストの題と註に「**撤去は 0.9.6**」「0.9.5 からここは指さない」 |
| `docs/superpowers/sdd/2026-09-19-v0.9.5-implementation.md:61-88` | 「**0.9.6 の宿題: サイト内の PDF 配信を撤去する**」の節（対象 6 つの一覧つき） |

**直さない（1 か所）**: **`CHANGELOG.md:47-48`**（0.9.5 の項の「撤去は 0.9.6」）。
**出た版の記録は書き換えない**——**0.9.4 の台帳が 0.9.3 の CHANGELOG について決めた線**である
（`docs/superpowers/sdd/2026-09-18-v0.9.4-implementation.md`）。**向きが変わったことは 0.9.6 の項に書く。**

**出したあとに利用者が実機で確かめること**（**私たちには測れない**）
- iPhone のホーム画面アプリで、リンク集 → マニュアル → **本文が読めるか**、**電卓に戻れるか**
- PDF（副経路）を押したときに何が起きるか。**ここは「戻れない」ままの可能性がある**
  ——**その場合も、既定の読み口は画面なので、読む道は塞がらない**

---

## §7 裁定（**済み 7 件と、残り 1 件**）

**済み**（§0.2 に出どころつきで再掲）: Q1 = (イ) `#manual`／Q2 = 画面の中に保存・印刷用の PDF 3 冊
（サイト内配信に戻す）／Q3 = 写真は出さない／Q4 = 目次・節リンク・検索は作らない／
Q5 = 3 冊を 1 つの塊で（増分を実測して報告）／Q6 = 画面の綴りは日本語のまま／Q7 = `#manual` に戻す。

**残り**:

- **Q8（`PDF_DISPOSITION`）**: **ヘッダを残して理由を書き直す（実行役の推し。§4.6）** ／ 外す。
  **iPhone では結果が変わらない**（実測）ので、**差は PC の挙動だけ**である。

## §8 この設計書が**断定しないこと**

- **iOS でどう見えるか**（§2）。**「直った」は利用者の実機まで言わない**
- **バンドルの増分・gzip 後の大きさ・写真の PNG のバイト数**（**未実測**。実装の最初の段で測る）
- **読み上げが本文の見出し階層をどう読むか**（`<h1>` は `App` が持ち、本文には `##` 以下が入る。
  **jsdom では測れない**ので E2E で見る）
