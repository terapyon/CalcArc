# 引き継ぎ（重量級テスト／検証）— まずこれを読む

> **この文書の場所: `docs/superpowers/sdd/heavy-HANDOFF.md`（リポジトリの追跡下）。**
> 2026-08-28 に `.superpowers/sdd/HANDOFF.md` から移した。**`.superpowers/` は
> `.gitignore` が無視している**ので、あそこに置いた台帳は**そのクローンにしか
> 存在せず、書いた本人が消えれば一緒に消える。** 名前に `heavy-` を付けたのは、
> 実装側の台帳と同じ場所に並ぶためである。

## 0. あなたが誰か（クリア直後に必ず読む）

**あなたは重量級テスト／検証の担当で、作業ディレクトリは `/home/terapyon/dev/CalcArc-e2e` である。**

**`/home/terapyon/dev/CalcArc` は実装側の別セッションが使っているので、読み書きとも一切しない。**
シェルの cwd は呼び出しごとにそこへリセットされるので、**全コマンドを
`cd /home/terapyon/dev/CalcArc-e2e && ...` の形で打つ**。

**cwd と起動時の git status を役割の根拠にしない。** 3 セッションとも process cwd は
`/home/terapyon/dev/CalcArc` で、クリア直後の手がかりは**全部が実装側を指す**。
2026-08-20、それで自分を実装側と誤認し、隣のセッションの作業台を触った。

**【重要な変更】監視役の Fable（`calcarc-96`）はクレジットが尽きて居ない。**
以前の引き継ぎにある「まず Fable へ点呼」は**もう使えない**。**ユーザーと直接やり取りする。**
迷ったらユーザーに聞く。代理承認者は居ない。


## 0.5. 再起動の後始末（2026-08-25 19:00 に書き、19:47 に**全部片付いた**）

**機械を再起動した。** 原因は 18:00 の `pnpm e2e` が起こした
`chrome-headless` の **soft lockup**（CPU#1 をカーネル空間で握ったまま離さず、
`SIGKILL` も届かない）。それが RCU の猶予期間を止め、`runc init` が 18 個
`wait_rcu_gp` で固まり、load average が 32 まで膨らんでいた。ディスクでも
メモリでもない。

**再起動の瞬間、`pnpm heavy:power` が走行中だった。** 4 項目とも済んでいる:

1. ~~`crates/` に残った変異~~ → **戻した**（`format.rs` の `DISPLAY_DIGITS`
   10→9 が当たったままだった。差分はその 1 行だけで人の編集は無し）。
2. ~~`heavy/scripts/detection-power.mjs` の 3 行の修正~~ → **コミットした**（`ecd1efb`）。
3. ~~`pnpm heavy:power` を回し直す~~ → **18/18 ok、652 秒。移動前と検出件数が完全一致**
   （18 種の総数も `display-digits` の 11 枚の内訳も、機械で突き合わせて食い違い 0 件）。
   記録は `docs/corpus-measurements.md` の末尾の節。
4. ~~`pnpm e2e`~~ → **181 passed（14.9 秒）、リトライも flaky も 0 件**（`254ea99`）。
   2 度落ちたのは機械のせいだった、と裏が取れた。

**移動の検証はこれで 4 本とも揃った。**「測れなかった」ものは残っていない。


## 0.7. リリース経路は通った（2026-08-25 夜、実測）

**`v*` タグだけが本番への扉になり、v0.4.1 で端から端まで通った**（run 32908847850）。
版数ゲート 11 秒 → CI 全 5 段 → 重量級 **37 分 8 秒** → 本番展開 52 秒 → 証拠と Release 9 秒。
Release に `release-evidence.md` / `heavy-report.md` / `calcarc-v0.4.1-dist.tar.gz` の 3 点。
実物は `build-info.json` に `83c9030` を刻んで配信中。

**1 回目（run 32857987219）は重量級で落ちて本番へ出なかった**——`heavy/` の型検査が
wasm ビルドの前に走っていたため（PR #68 で修正）。**ゲートは意図どおり働いた。**
手元のワークツリーには `web/src/wasm/` が残るので、**この種の欠落は手元では緑のまま
気づけない**（memory: local-green-is-not-clean-clone-green）。

**監視役が Fable（calcarc-10）から calcarc-25 へ移った**（2026-08-26 のユーザー裁定）。
点呼・報告・発注の突き合わせは calcarc-25 が相手。

## 0.8. 裁定待ちで停めているもの（2026-08-26、監視役 calcarc-25 の判断）

**ユーザーは外出中で、指示は「指摘や見直し項目を列挙するところまで」。** 監視役は代理承認の
権限を持たないので、下の 2 本は**帰宅後の裁定まで動かさない**（追加のコミットもしない）。
依頼 3 の継続（Task 4 以降）だけは続けてよい、という判断を受けている。

| ブランチ | tip | 中身 | 状態 |
|---|---|---|---|
| `feature/coverage-covered-cells` | `2c7fed2` | 依頼 3 の Task 3・4 | **Task 4 まで進めて停止** |
| `docs/release-path-confirmed` | `a6f77fe` | 文書の「未確認」→ 実測 | **停止** |
| `fix/release-notes-carry-the-evidence` | `4d1df07` | R-10 の直し | **停止** |

**裁定に載っている論点（自分で潰さないこと）:**

1. **R-10 の直しの置き場。** 貼り直しのロジックは `release.yml` の**インラインシェル**にあり、
   見張るテストが 0 本である。検証は「実物の本文で 3 回、548 バイト不変」の**1 回きりの手作業**で、
   これは過去に 5 時間半で破れた形と同じ。**`release-evidence.mjs` 側へ寄せて vitest で固定するか**が論点。
2. **`gh release edit` が `gh release upload` より先**（`release.yml` の 150 行目と 151 行目）。
   添付に失敗すると**存在しない添付を語る本文**が残る。順序を入れ替えるか、失敗時の後始末を決めるか。
3. **`renderEvidence` は `skipped`/`neutral` を「進行中」と書く。** いまの `needs` の連鎖では
   起きないが、`if:` を足した日に静かに現れる。(a) 独立の表記にする / (b) 証拠を書かせない側に倒す。

**Task 5 に入る前の門（監視役と合意、2026-08-26）。** Task 5 は `loan_term` の 52 行の月額を
変えるので、**`finance-000.json` が動く最初の Task** である。入る前に監視役へ声をかけ、
終わったら 3 つを報告する: ①変更前の 18 変異の実測率 ②変更後の同じ 18 の実測率
③**下限（`minRate`）に触ったか触っていないか**——触ったなら、それは「測り直した」ではなく
**「基準を動かした」**なので、そう書く。粒度は 0.4.0 の検証と同じで、総数だけでなく
`display-digits` の 11 枚の内訳まで機械で突き合わせる。

**Task 5 は裁定の後に回すのが安全**（golden が動いた後に「置き場」の裁定が出ると、
両方を同時にほどくことになる）。

## 1. いまの状態（2026-08-25）

| | |
|---|---|
| `origin/main` | **`835cf0a`**（PR #64 = 0.4.0 がマージ済み） |
| タグ | **v0.4.0 まで発行済み** |
| 重量級 CI | **3 本連続 success**——v0.3.0 34m42s / v0.3.1 35m0s / **v0.4.0 41m13s** |
| 私のブランチ（縦積み） | **`feature/release-version-gate` @ `27d2121`**（origin/main から。**push 待ち**）→ その上に **`feature/heavy-tests-split` @ `254ea99`**（下記 2 の移動。**13 コミット・検証まで完了、push 待ち**） |
| **手元の作業台** | **きれい（未コミットの変更なし）**。依頼 1・2 は完了して push 待ち。次は依頼 3（実験計画法。詳細待ち） |

**`git push` と PR 作成は行わない**（ユーザー専権）。コミット末尾は
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`、件名の次は空行。

## 2. 0.4.0 について確かめたこと（2026-08-25、`origin/feature/0-4-0-polish` = `7a30141`）

**main と 0.4.0 の試しマージのツリーで、実際に走らせて全部緑だった。**

| 検査 | 結果 |
|---|---|
| 試しマージ | 衝突 0 件。私の 4 ファイルは 0.4.0 が触らない |
| 変異 28 個（detection 18 + exact 10）の綴りが当たるか | **28/28** |
| コーパスの再現性 | 2 passed |
| `pnpm heavy` | **195 passed** |
| `heavy:power:exact` | **10/10 ok**、赤の本数も不変 |
| `heavy:ui` | **36 passed / 指摘 0 / 46 トークン全押下**（11.9 分） |
| `heavy:power` | **18/18 ok**（10.8 分） |

**検出率は 1 つも動いていない**——各変異の実測率が下限のちょうど 2.00 倍（下限は実測率の半分と
決めてあるので、下限を決めた日と同じ率）。

**未確認**: 実装側のローカルは既に **`d3671e9`「Stop asking the footer to fit on one line」**へ
進んでいる（私が測ったのは 1 つ前の `7a30141`）。増えたぶんは Footer の CSS と
`footer.spec.ts` だけで重量級の触る場所ではないが、**測ってはいない**。

**残っているのは v0.4.0 のタグを打ったときの CI の確認だけ**（ユーザーがマージとタグを行う）。

## 3. 使える道具（自分で作ったもの）

**どれも `cd /home/terapyon/dev/CalcArc-e2e/heavy` から打つ**（依頼 2 の移動後）。

| コマンド | 何をするか | 実測時間 |
|---|---|---|
| `pnpm heavy` | 生成コーパス（18 枚 33,567 件）と参照の照合 | 32 秒 |
| `pnpm heavy:power` | 18 変異の検出力（シャードが反応するか） | 11 分（移動後の実測 652 秒） |
| **`pnpm heavy:power:exact`** | **10 変異の検出力（`cargo test` の赤の集合で見る）**。convert・data_scale・currency 用 | 4 分 |
| `pnpm heavy:ui` | 本物の画面を盤面から叩く（46 トークン全押下・1,266 ケース） | 12 分 |

**`runOneMutation(mutation, { root, measure, verdict })`** に測り方と判定の注入口がある。
**変異を当てて戻してバイトで確かめる手続きは 1 か所だけ**——写さない。

## 4. 守っていること（この仕事の作法）

- **測ってから足す。** 件数を増やす前に「いま在る検査がその欠陥を捕まえるか」を変異で測り、
  **赤くならなかった帯にだけ**足す。0.3.0 では 6 変異を測って**単体 1 件と e2e 6 件だけ**、
  0.3.1 では 4 変異を測って**単体 2 件だけ**足した（golden は 1 件も足していない）。
- **表ではなく合成を壊す。** 係数表を壊す変異は、同じファイルの literal テストで止まる。
- **赤くならない赤確認は、何も測っていない。**
- **「別システムがどう見るか」を断定形で書かない。** 実測を付けるか（未確認）と書く。
- **`crates/` の計算コードを変えない。** 変異は測定中に当てるだけで、走行後に差分が空。
- **戻しは再編集**（`git checkout` を使わない）。

## 5. 記録の場所

| 何 | どこ |
|---|---|
| 実測の記録（**数字はここに書く**） | `docs/corpus-measurements.md`（追跡対象） |
| 0.3.1 の台帳 | `.superpowers/sdd/2026-08-25-verify-0-3-1-currency/progress.md` |
| 0.3.0 の台帳と引き継ぎ | `.superpowers/sdd/2026-08-20-verify-0-3-0-calculations/progress.md`（作業ファイル・追跡外）/ `docs/superpowers/sdd/heavy-HANDOFF-verify-0-3-0.md` |
| 重量級 D+E（マージ済み） | `docs/superpowers/sdd/heavy-HANDOFF-de.md` |
| GitHub の照会 | **`gh-ro`**（読み取り専用ラッパー。素の `gh` は未ログイン。`gh auth login` はしない） |

**`.superpowers/` は `.gitignore:15` が丸ごと無視している**ので、そこに在る
**日付つきの作業ディレクトリ**は `git show` では読めない——ファイルを直接読む。
**横断的な台帳（この文書を含む 4 本）は `docs/superpowers/sdd/` に移してある。**

## 6. 次にやること

**ユーザーから 3 件の依頼を受けた（2026-08-25）。順番は 1 → 2 → 3 で合意済み。**

1. ~~cron を消す＋タグと版数の一致検査~~ → **完了（`ecb4ad4` + `27d2121`、push 待ち）。**
   `check-version.mjs` が 5 箇所を見るようになり、タグ時は `--tag` でタグ名との
   一致と CHANGELOG の日付まで見る。`heavy-corpus.yml` は `version` ジョブが
   先に立ち、`corpus` が `needs` する。週 1 の cron は消えた。
2. ~~重量級テストを実装側から分離する~~ → **完了（`feature/heavy-tests-split` @ `254ea99`、
   13 コミット、push 待ち）。** `heavy/` が独立した pnpm パッケージ（`calcarc-heavy`）に
   なり、`web/` は重量級の存在を知らない。**コマンドは `cd heavy` から**打つ——
   `pnpm heavy` / `heavy:ui` / `heavy:power` / `heavy:power:exact`（`pnpm wasm` は
   `pnpm --dir ../web wasm` に、Playwright の設定は `playwright.corpus.config.ts` /
   `playwright.ui.config.ts` に変わった）。**検証は 4 本とも移動前と一致**（§0.5 を見よ）。
3. **実験計画法に基づくテストの追加**（＝テスト空間モデル・除外理由の可視化）。
   **仕様は 2026-08-25 に受領済み。設計書と第1段階の計画まで書き終えて、着手待ちで静止している。**

   | 下書き（`.superpowers/` なので追跡されない） | 中身 |
   |---|---|
   | `.superpowers/sdd/2026-08-25-coverage-model/design.md` | 仕様全文 + §21 に座標の訂正 |
   | `.../plan-stage1.md` | 第1段階 `finance-v1` の 12 Task（TDD・実コード入り） |
   | `.../preflight.md` | 着手前の実測（下の数字の出どころ） |

   **着手の合図はユーザーのマージ**（依頼 1・2 をまとめて main に入れる、と 2026-08-25 に合意）。
   マージ後に新しい main から **`feature/coverage-model`** を切り、下書き 2 本を
   `docs/superpowers/specs/2026-08-25-coverage-model-design.md` と
   `docs/superpowers/plans/2026-08-25-coverage-model-finance.md` へ移してコミットしてから Task 1。

   **スコープは第1段階＋第2段階（科学計算 §14）の通し**（ユーザー選択）。ただし第2段階の
   計画は第1段階の結果が出てから書く（仕様 §6.2 の指示）。

   **実測で分かっている当たり所**: 8 対象のうち**手を入れる必要があるのは 2 つだけ**。
   `loan_term` は 74 被覆 → 決定的構成で +52 → 126、残り 24 を除外（うち `target_n=1201` の
   7 件は `not_applicable`——`MAX_TERM_MONTHS = 1200` なので答になり得ない）。
   `compound_deposit_for` は 247 被覆 + 19 除外（`source_overflow`）。他の 6 対象は既に全被覆。

   **仕様への訂正 3 件**（すべて実測で裏を取った。設計書 §21 に書いてある）:
   §16 の TS 3 ファイルは `heavy/tests/corpus/` へ移動済み／§11.1 の「15 シャード」は 18 枚／
   §9.1 の「246 / 20」は仕様自身の §9.3 の規則で数えると「247 / 19」。

それとは別に、開いている項目:

- ~~v0.4.0 のタグの CI~~ → **完了（41m13s、success）。0.4.0 のリリースは終わっている。**
  私が試しマージで先に測った 7 項目の予測（全部緑）は、**実物の CI でも当たった**。
- 次に重量級を回す機会は、**次のタグか、`workflow_dispatch` の手動実行**である
  （**push では走らない**。週 1 の cron は依頼 1 で消した）。
- ~~CHANGELOG の 0.3.1 と 0.4.0 が「未リリース」のまま~~ → **直した（`27d2121`）。**
  どちらも 2026-08-25。**`CHANGELOG.md` は実装側セッションも触っている**ので、
  マージのとき衝突しうる（向こうが 0.5.0 の節を先頭に足すと隣接する）。
- **未確認**: `heavy-corpus.yml` の `version` ジョブが実際に動くのは、
  次に実タグを打ったときにしか分からない。手元では分岐を再現して確かめただけ。
- **`needs: version` の副作用**: 版数が食い違っていると `workflow_dispatch` の
  手動実行も止まる。手で回したいときはまず版数を揃える。
- **0.3.1 の申し送り**: 通貨の `Overflow` の帯は**単体テストの 1 層でしか見張れない**
  （Python 参照は無限精度で `Overflow` を持たない。境を足すと Rust の移植になり CLAUDE.md 違反）。
