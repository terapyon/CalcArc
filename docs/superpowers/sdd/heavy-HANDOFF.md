# 引き継ぎ（重量級テスト／検証）— まずこれを読む

> **この文書の場所: `docs/superpowers/sdd/heavy-HANDOFF.md`（リポジトリの追跡下）。**
> 2026-08-28 に `.superpowers/sdd/HANDOFF.md` から移した。**`.superpowers/` は
> `.gitignore` が無視している**ので、あそこに置いた台帳は**そのクローンにしか
> 存在せず、書いた本人が消えれば一緒に消える。** 名前に `heavy-` を付けたのは、
> 実装側の台帳と同じ場所に並ぶためである。

## 0. あなたが誰か（クリア直後に必ず読む）

**あなたは重量級テスト／検証の担当で、作業ディレクトリは `/home/terapyon/dev/CalcArc-e2e` である。**

**`/home/terapyon/dev/CalcArc` は実装側の別セッションが使っているので、読み書きとも一切しない。**
シェルの cwd は呼び出しごとにそこへリセットされるので、**全コマンドを
`cd /home/terapyon/dev/CalcArc-e2e && ...` の形で打つ**。

**cwd と起動時の git status を役割の根拠にしない。** 3 セッションとも process cwd は
`/home/terapyon/dev/CalcArc` で、クリア直後の手がかりは**全部が実装側を指す**。
2026-08-20、それで自分を実装側と誤認し、隣のセッションの作業台を触った。

**【重要な変更】監視役の Fable（`calcarc-96`）はクレジットが尽きて居ない。**
以前の引き継ぎにある「まず Fable へ点呼」は**もう使えない**。**ユーザーと直接やり取りする。**
迷ったらユーザーに聞く。代理承認者は居ない。


## 1. いまの状態（2026-08-30 夜）

| | |
|---|---|
| `origin/main` | **`9c87589`**（PR #95） |
| 私の枝 | **`docs/coverage-model-scientific-plan`**（未 push。**22 コミット**） |
| いま何をしていたか | **試験空間モデル 第 2 段階（`scientific-v1`）の段 C**。**Task 17 まで完了**、**フルスイープの途中で再起動** |
| `pnpm heavy` | **緑**（250 passed）。**意図した赤は同日中に解けた** |
| 作業台 | **きれい**（この記録の時点でコミット済み） |

**★ この作業台で環境の異常が 2 種類出た（2026-08-30）。**
**`pnpm heavy` の Segmentation fault が 4 回**（落ちる位置は一定せず、
1 回は 250 本すべて ✓ のあと）、**コーパスのファイルが 1 枚、同じ長さで
書き換わって壊れた**（`corrections-000.json`。空白 8 個が
「2 空白 + バッククォート + 5 空白」に）。**生成器は決定的である**ことを
2 回生成して確認済み（ダイジェスト一致）。**この 2 つが同じ原因なら、
この作業台で出た緑も赤も等しく疑わしい**——詳細と再現条件は
[known-flaky-tests.md](known-flaky-tests.md)。**ユーザーはこの直後に
マシンを再起動している。**

## 2. 次にやること

1. **フルスイープの残り**を回す。**`reference` 467 / `heavy` 183 + `pnpm heavy` 250 /
   `cargo fmt` `clippy` は通っている**が、**`cargo test` の件数を印字で取り直し、
   `web`（vitest・typecheck・lint・検査 3 本・e2e）が未実行**である
2. **押下台帳（`pnpm heavy:ui`）を 1 回**。**予測は「動く」だけにしてある**
   ——**押下数はコーパスの関数**であり（`selectSample` の `spread(count, length)`）、
   コーパスは elementary 2000→2002・inverse-trig 2000→2005・display 2000→2002・
   complex 2000→2001・**新シャード 13 件**と動いた。**打鍵ケース数 1,266 は
   変わらない見込み**
3. **`heavy:power` / `power:exact` は回さない**——`crates/` の差分が空で、
   コーパスの計算経路も動いていない（動いたのは注記と、新しい表示/エラーのケース）
4. **段 C の記録を `docs/corpus-measurements.md` に書く**（段 B の節と同じ形で）
5. **そのあと PR 文は監視役が書く**（段 B + 段 C を 1 本で出す方針）

**段 C で確定した数（すべて印字から）:**

```
要求セル 73 → 78    被覆 40 → 66    除外 0 → 1    未達 33 → 11
測れない起因 30 → 11    **本当の穴 7 → 0**
コーパス 18 枚 33,567 件 → **19 枚 33,590 件**
```

**残る「測れない」11 件は `cancellation`（shape 4 + band 3）と
`precedence/grammar_class`（4）だけ**で、**どちらも理由を実測で書き直してある。**

## 3. これまで（1 行ずつ）

| 版 | 私の担当分 |
|---|---|
| 0.3.0 / 0.3.1 | 計算の検証（変異で測ってから足す。golden は 1 件も足していない） |
| 0.4.0 | 重量級コーパスの改善（検出力・Finance の層別・押下台帳） |
| 0.4.1 | cron 廃止＋版数ゲート／重量級を `heavy/` へ分離／**リリース経路をタグ主導に** |
| **0.6.0** | Python の保守性 5 件／台帳 4 本を追跡下へ／**統合検証** |
| **#94**（未タグ） | **赤の伝播**——報告書が土台を語る信号（5 状態・指紋つき）／`always()` を「集める／前提を整える／判定・測る」に分ける |
| **#95**（未タグ） | **試験空間モデル `finance-v1`** 完了（Task 5〜12）。`loan_term` 74 → 126、`compound_deposit_for` 247 → 258、除外 32 件に理由。**受け入れ門と網羅表つき** |

**リリース経路は v0.4.1 で端から端まで実地確認済み**（版数ゲート → CI → 重量級 37 分 8 秒 →
展開 → 証拠と Release）。**1 回目は重量級で落ちて本番へ出なかった**——`heavy/` の型検査が
wasm ビルドの前に走っていたためで、**ゲートは意図どおり働いた**。

**0.6.0 の統合検証で、T-3（境界を `Outcome<T>` にする変更）が `heavy/` に届いておらず、
`pnpm heavy` が 5,500 件全滅していたのを見つけた。** E2E は緑で `web` は追随済みだった
——**追随していなかったのは重量級の経路だけで、そこは E2E が踏まない。** タグを打っていたら
37 分後に見ていた。

## 4. 使える道具（自分で作ったもの）

**どれも `cd /home/terapyon/dev/CalcArc-e2e/heavy` から打つ**（依頼 2 の移動後）。

| コマンド | 何をするか | 実測時間 |
|---|---|---|
| `pnpm heavy` | 生成コーパス（18 枚 33,567 件）と参照の照合 | 32 秒 |
| `pnpm heavy:power` | 18 変異の検出力（シャードが反応するか） | 11 分（移動後の実測 652 秒） |
| **`pnpm heavy:power:exact`** | **10 変異の検出力（`cargo test` の赤の集合で見る）**。convert・data_scale・currency 用 | 4 分 |
| `pnpm heavy:ui` | 本物の画面を盤面から叩く（46 トークン全押下・1,266 ケース） | 12 分 |

**`runOneMutation(mutation, { root, measure, verdict })`** に測り方と判定の注入口がある。
**変異を当てて戻してバイトで確かめる手続きは 1 か所だけ**——写さない。

## 5. 守っていること（この仕事の作法）

- **測ってから足す。** 件数を増やす前に「いま在る検査がその欠陥を捕まえるか」を変異で測り、
  **赤くならなかった帯にだけ**足す。0.3.0 では 6 変異を測って**単体 1 件と e2e 6 件だけ**、
  0.3.1 では 4 変異を測って**単体 2 件だけ**足した（golden は 1 件も足していない）。
- **表ではなく合成を壊す。** 係数表を壊す変異は、同じファイルの literal テストで止まる。
- **赤くならない赤確認は、何も測っていない。**
- **「別システムがどう見るか」を断定形で書かない。** 実測を付けるか（未確認）と書く。
- **`crates/` の計算コードを変えない。** 変異は測定中に当てるだけで、走行後に差分が空。
- **戻しは再編集**（`git checkout` を使わない）。
- **長走行は重ねない。** `heavy` / `heavy:power` / `heavy:ui` は**どれも wasm を作り直し、
  プレビューのサーバを立てる**。同時に走らせると干渉し、**`heavy:power` が当てた変異が
  別の走行に載る**（2026-08-28 に実際にそうした）。**途中で止めたら、まず
  `git status --short crates/` を見る**——変異が残っていることがある。
- **数字が出たら、それが「どの走行・どの木」のものかを先に確かめる。** 2026-08-28 に
  3 回踏んだ——別の木の数字を同じ表に並べ、集計方法を 2 通り混ぜ、**再起動で走行が
  始まっていないのに前回の結果ファイルを読みかけた**（ファイルの時刻で気づいた）。
  **数字だけ見ても、どれも正しそうに見える。**
- **翻訳や変換を足す前に、既存のものを探す。** T-3 の直しで camelCase の変換を書いたが、
  **比較側が前から持っていた**（`tests/corpus/calls.ts` の `normalise`）。**2 か所が同じ
  仕事をすると、片方だけ直した日に静かに壊れる**——既存の証明書テストが落ちて気づいた。

## 6. 記録の場所

| 何 | どこ |
|---|---|
| 実測の記録（**数字はここに書く**） | `docs/corpus-measurements.md`（追跡対象） |
| 0.3.1 の台帳 | `.superpowers/sdd/2026-08-25-verify-0-3-1-currency/progress.md` |
| 0.3.0 の台帳と引き継ぎ | `.superpowers/sdd/2026-08-20-verify-0-3-0-calculations/progress.md`（作業ファイル・追跡外）/ `docs/superpowers/sdd/heavy-HANDOFF-verify-0-3-0.md` |
| 重量級 D+E（マージ済み） | `docs/superpowers/sdd/heavy-HANDOFF-de.md` |
| GitHub の照会 | **`gh-ro`**（読み取り専用ラッパー。素の `gh` は未ログイン。`gh auth login` はしない） |

**`.superpowers/` は `.gitignore:15` が丸ごと無視している**ので、そこに在る
**日付つきの作業ディレクトリ**は `git show` では読めない——ファイルを直接読む。
**横断的な台帳（この文書を含む 4 本）は `docs/superpowers/sdd/` に移してある。**


## 7. 台帳の書き方（この文書自身のこと）

**日付つきの節を積み上げない。** 2026-08-29 に整理したとき、`0.5` `0.7` `0.8` `0.9` と
**その日の状態を書いた節が 5 つ**溜まっていて、**そのほとんどが嘘になっていた**
（消えた枝を指し、終わった裁定を「待ち」と書いていた）。

**書く場所は 3 つだけ:**

- **§1 いまの状態**——上書きする。**足さない。**
- **§2 次にやること**——終わったら消す。
- **§3 これまで**——1 版 1 行。**経緯は残すが、状態としては読ませない。**

**引用は行番号でなく名前で指す。** `heavy-corpus.yml:150-155` ではなく
`Type a sample on the real keypad` の段。**名前は編集で動かない**——2026-08-28 に
自分で書いた座標が 1 日でずれ、同じ日に 3 人が行番号で転んだ。
