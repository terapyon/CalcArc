# 0.3.x 未消化の全件（triage 待ち）

**2026-08-20 に 4 つの台帳から grep で起こした統合版。** 出どころの台帳が一次資料で、
ここは索引である——**中身が食い違ったら台帳を信じること**。

- S-0 `.superpowers/sdd/2026-08-19-scale-llm-transfer/progress.md` §4
- U-1 `.superpowers/sdd/2026-08-20-unit-engine/progress.md` §4
- U-2 `.superpowers/sdd/2026-08-20-convert-categories/progress.md` §4
- U-4 `.superpowers/sdd/2026-08-20-currency/progress.md`（レビュー 2 節）

**合計 28 件 + DoD バックログ 3 件 = 31 件**（起票時 29 件のうち **A8 は起こした時点で既に閉じていた**——下記）。 どれも「直さないと決めた」ものではなく
「後で裁定に載せる」もの。**直さないと決めた既知の状態**（LLM の溢れ許容など）は
各台帳の §5 にあり、この表には入れない。

## A. 実際にコードが直る見込みのあるもの（9 件）

| # | 中身 | 出どころ | 手数 |
|---|---|---|---|
| A1 | **押せて何も起きない演算子キーが 7 個**（`( ) ÷ × − + =`）。**2 パネル横断**（LLM と DataScale の両方）。配線しているのは FinancePanel だけ | S-0 Ruling 19/26 | 中（2 パネル） |
| A2 | `valueKey` の `as LlmKeyToken` キャスト（`prefix: string` なので綴り間違いが黙って通る） | S-0 Task 8 | **triage 棚（A1 と同じ）**。「1 行」は過小評価だった——`prefix` を 4 literal に絞るのは 1 行だが**キャストは消えない**（`LlmKeyToken` の該当項は `` `param:${ParamValue}` `` で**値の側も literal union**、`value: number` が広い）。generic / overload の設計判断を含み、**S-0 のファイルでもある** |
| A3 | `FIELD_ORDER` の二重定義（`Keypad/transfer.ts` と `TransferPanel.tsx`。`llm.ts`/`LlmPanel.tsx` も同型） | S-0 Ruling 25 | 小 |
| A4 | 単位キーの処理が active に追随していない（`LlmPanel.tsx:210,290`。いま安全なのは `llmPad` が `unit:b/m` を parameters のときしか出さないから） | S-0 Task 9 | 中 |
| ~~A5~~ | **`calcarc-wasm/src/lib.rs` の理由コメントが最初から嘘**——「`_` に束ねると `from` が素通りする」は誤り（`?` が先に効く。検査しているのは `?` であって束縛名ではない） | U-4 T2-4 Minor 1 | **完了 `b239823`** |
| ~~A6~~ | `test_a_tie_rounds_to_even` のコメントが**未来形のまま**（「Task 3 の探索が確定させる」。Task 3 は完了済み） | U-4 T2-4 Minor 2 | **完了 `b239823`** |
| A7 | `offline` が**開いた瞬間の値で固定**（`useCurrencyRates.ts:122` が `deps.online()` を 1 度読むだけ。`online`/`offline` を購読していない）。**spec §5 は要求していない**——記録が無いだけ | U-4 T5-8 m-3 | 小〜中 |
| ~~A8~~ | ~~`LlmPanel` / `DataScalePanel` の max-content の脆さが未確認~~ → **閉じた**。`fdb3c2a` が **両方に `width: 100%` を入れている**（現物で確認）。`LlmPanel` 側にはさらに「**この面は縮んでいなかったが、寸法を守っていたのではなく `パラメータ数` が長くて max-content が 390 を超えていただけ**」という理由まで書かれている | U-1 §5 | **完了** |
| ~~A9~~ | ~~`toBeGreaterThan(0)` が冗長~~ → **指摘が誤り。却下（`b239823`）。** 直前は `toBe(100)` ではなく **`toBe(FACES.reduce(...))`**——**ループが歩く表そのものから期待値を作っている**ので、`FACES` が空になれば両辺 0 で素通りする。85 行は**その場合だけを捕まえる見張り**で、削ると穴が開く | S-0 Task 9 再レビュー | **却下** |

## B. 検査の穴（足すかどうかの裁定が要る。7 件）

| # | 中身 | 出どころ |
|---|---|---|
| B1 | **`viewport-budget.spec.ts` の巡回の外に 7 route**。`#scale/llm` `#scale/transfer` / U-2 の 4 つ / **`#convert/currency`**。**3 種類あって性質が違う**——LLM=溢れを許容、U-2 の 4 つ=収まっているが未確認（足せば緑）、通貨=**溢れており、かつあの巡回は通信を塞いでいないので足すと非決定**（`994102b` で全部書いた） | S-0 Ruling 28 / U-2 / U-4 |
| B2 | **ラベル 63 件の相異を見張る検査が無い**（読み上げ名は `spoken.size` が見ている） | U-2 |
| B3 | **`scrollWidth ≤ clientWidth`（ラベルが切れないこと）の検査が無い**——手計測のみ。spec §4 に「機械が見張っていないもの」として明記済み | U-2 |
| B4 | `convert.spec.ts` の `FACES` に mass の面が無い（別テストが 3 カテゴリ分見ているので被覆の穴ではない） | U-1 |
| B5 | **U-4 の塞ぎが `convert.spec.ts` の中でしか効かない**。別ファイルが `#convert/currency` を開くと**黙って本物に出て緑になる**。fixture の共通化 | U-4 T5-8 m-5 |
| B6 | `llm.spec.ts:130-133` の失敗メッセージが「枠が動いた」だが**未計測でも同じ文言**になる | S-0 Task 9 再レビュー |
| B7 | **`from` の未知トークンは golden で表現できない**（`Input` が `from` を deserialize しない）。実装の食い違いでないのに赤くなるので `cases.py` に一言 | U-4 T2-4 Minor 4 |

## C. 到達不能・意図的・害の無い記録（10 件）

| # | 中身 | 出どころ |
|---|---|---|
| C1 | **Ruling 23**: `allFilled` が**打った文字**の空を見る一方**評価値**が空だと `SyntaxError` の下に結果行が生き残る。**現状到達不能** | S-0 |
| C2 | 実装者が自己申告した未検査の設計判断 3 件（選択のみの項目で AC → fp16 / 候補面で AC → Entry を空 / `entry:choose` が値を保つ側） | S-0 Task 9 |
| C3 | `the_kv_side_never_needs_the_ceiling` の右辺が呼び出し順と違う並び（可換） | S-0 Task 3 |
| C4 | spec の取り下げ行が打ち消し線で残っている（訂正印の流儀として意図的） | S-0 Task 3 |
| C5 | `overflow_is_an_error_not_a_wrap` の分け方が `llm.rs` と違う（意図的） | S-0 Task 4 |
| C6 | `BandwidthUnit::ALL` / `DurationUnit::ALL` がテストからしか参照されない | S-0 Task 4 |
| C7 | `llm_memory`/`data_transfer` が IIFE 形、隣の `data_scale` は `.and_then` 連鎖（同ファイルに 2 流儀） | S-0 Task 5 |
| C8 | `CONVERT_INPUTS` が plan の逐語でなく増補（理由はコメントに在り spec §6 に過不足なく応えている）／`oz`/`st` を `_LB` 経由の導出に変えた件 | U-1 Task 3 |
| C9 | `convert-result` が Readout と同じ情報を 4 行目に再掲（plan の Interfaces が要求）／`convert::convert` のスタッター（**下の訂正を見よ**） | U-1 |
| C10 | 裁定 4-4 の「`-0.006 → -0.01` の対」は golden ではなく各言語のユニットテスト——**対が言語間で突き合わされていない**／`Overflow` の枝が golden で一度も踏まれない（Python は多倍長。既存 `convert.json` と同型）／plan の裁定の並びが 1→2→4→3 | U-4 T2-4 Minor 3/5/6 |

**【訂正 2026-08-28】C9 の「spec §5 が名前を指定」は、半分が誤りだった。**

- **正しかった部分**: spec §5（`specs/2026-08-19-unit-engine-design.md:232-238`）は
  確かに `pub fn convert(…) -> JsValue` を指定している。**ただしそれは
  WASM 境界の関数**（`crates/calcarc-wasm/src/lib.rs:701` の
  `#[wasm_bindgen] pub fn convert`）である。**そちらの名前は spec が縛っている。**
- **誤っていた部分**: **core の `convert`（`-> CalcResult<Rational>`）を縛るものは
  spec に無い。** core の署名が出てくるのは**計画書**のほう
  （`plans/2026-08-20-unit-engine.md:821,1090`）で、しかもその計画書は
  **衝突を承知で通している**——`:1498-1500`「Rust の関数名 `convert` はモジュール名
  `convert` と衝突する。…別名で入れるか、フルパスで呼ぶこと。**`#[wasm_bindgen]` の
  関数名は spec §5 が `convert` と決めているので変えない。**」
- **なぜ間違えたか**: 2026-08-26 の当て直しで「記録どおり」と確認したとき、
  **spec が縛っているのが境界の名前か core の名前かまで降りなかった**。
  **「spec に `pub fn convert` と書いてある」は真で、「だから core の名前は
  変えられない」は偽**だった。

**裁定（ユーザー、2026-08-28）: 改名しない。`api-style.md` に例外条項を書く。**
理由は spec ではなく、**あの文書の 2 つの規約が正面から衝突する**ことである
——「スタッターを避ける」と「flat import を維持する（だから `format_*` の接頭辞を
残す）」。**C9 のこの項目は閉じた。**

## D. golden の設計（1 件）

| # | 中身 | 出どころ |
|---|---|---|
| D1 | 新規 24 件が「カテゴリ内の隣接比」に寄っている。**spec §5 の名指し 7 件は全部揃っている**ので spec 違反ではなく、絶対値ピンで守りの穴は塞いだ | U-2 |

## E. `docs/definition-of-done.md` の「0.3.0 の後に直すもの」（3 件・公開文書に記載済み）

| # | 中身 |
|---|---|
| E1 | **`(29 nCr 4) nCr 2` が Math ERROR**（ユーザー裁定: 後で直す。**誤値ではなくエラー**なので急がない） |
| E2 | `FinancePanel.tsx:74` の「コアの `MAX_TERM_MONTHS` と同じ」は**掛かる場所が違う** |
| E3 | **期数 0 の複利は、コアの `SyntaxError` が画面から観測できない** |

---

## 手数の見立て（**実測前の概算**）

~~1 行で閉じるものが 4 件（A2 / A5 / A6 / A9）。~~ **4 件のうち 1 行だったのは 2 件だけだった**（A5 / A6 = 完了）。**A9 は指摘そのものが誤りで却下**、**A2 は「1 行」が過小評価**。**いちばん重いのは A1**（2 パネル横断で、
「押せて何も起きないキーを消すか配線するか」という設計の裁定が先に要る）。

**B1 は 0.3.x で 3 回書き直している。** 足すかどうかより、**3 種類の性質を混ぜないこと**が本体。

---

## この索引を作って分かったこと

**A8 は、起こした時点で既に閉じていた。** U-1 が「`LlmPanel` と `DataScalePanel` は未確認」と
書き残し、その 3 日後にデザイン修正（`fdb3c2a`）が**別の動機で**同じ 2 ファイルに
`width: 100%` を入れていた。**台帳は「未確認」のまま**で、閉じたことを誰も書き戻していない。

**未消化リストは、放っておくと実態より長くなる。** 溜めた側が閉じるとは限らず、
**閉じた側は自分が何を閉じたか知らない**（`fdb3c2a` は幅の話をしていて、
U-1 の残置事項を閉じたつもりは無い）。**統合するときは全件を現物に当て直すこと。**
