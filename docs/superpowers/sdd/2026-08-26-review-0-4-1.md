# v0.4.0 → v0.4.1 レビュー（calcarc-25 の分）2026-08-26

対象: `v0.4.0`(835cf0a) .. `v0.4.1`(83c9030)、マージ除き 23 コミット、81 ファイル、+7,257/−185。
Fable(calcarc-10) には R（リリース経路）と第 2 部を発注済み。以下は H/C/F と構造面の私の分。
**手は入れていない。列挙のみ。**

## 第 1 部：この期間の作業への指摘

### 1. `heavy/` は push でも PR でも一切検査されない（確度: 実測）

`ci.yml` の `on:` は `push: branches:[main]` / `pull_request` / `workflow_dispatch` / `workflow_call`。
`git grep -n heavy origin/main -- .github/workflows/ci.yml` は **0 件**。
`heavy/` の `typecheck && lint && test` は `heavy-corpus.yml` の
"The measuring instrument must be sound"(:90) にしかなく、そのワークフローの `on:` は
`workflow_dispatch` と `workflow_call` **だけ**（`f179937` で週 1 cron を削除済み）。

分離前、この 6 本の単体テストは `web/tests/unit/` にいて CI の `web` ジョブの
`pnpm test` が毎 push 回していた。**分離で毎回の CI から落ちた。**

筋道: 計測器の欠陥は、**45 分ジョブの中でしか露見しなくなった**。実際 1 回目の
v0.4.1 リリース `32857987219` はここで落ちている（`heavy/` の型検査が wasm ビルド前）。
PR で 2 分で分かるはずのものが、リリースを 1 本潰して分かった。
memory `local-green-is-not-clean-clone-green` の実例が構造として残っている。

### 2. 「`web` は heavy を知らない」という不変条件は、同じ日に破れている（確度: 実測・確定）

**この不変条件は、それ自身が測り方を宣言している。** `512a64d` のコミット本文:

    確認: `git grep -i heavy -- web/` が空。

設計書 `docs/superpowers/specs/2026-08-25-heavy-package-split-design.md:30` も
「web が重量級の存在を **1 バイトも知らなくなる**」とディレクトリ単位で書いている。

**その同じ方法で今日測ると 6 行 / 2 ファイル当たる**（`web/pnpm-lock.yaml` 除く。実測）:

    web/scripts/release-evidence.mjs:18    const HEAVY_MARKER = "Heavy corpus";
    web/scripts/release-evidence.mjs:105   if (!jobs.some((job) => job.name.includes(HEAVY_MARKER))) {
    web/tests/unit/release-evidence.test.ts:26   attachments: ["heavy-report.md", …]
    web/tests/unit/release-evidence.test.ts:47   job("Heavy corpus / Corpus vs reference", "success"),
    web/tests/unit/release-evidence.test.ts:51   expect(out).toContain("Heavy corpus / Corpus vs reference");
    web/tests/unit/release-evidence.test.ts:84   expect(out).toContain("heavy-report.md")

**前後関係（committer date は rebase で全部 23:02:05 に潰れているので author date で見る）:**

| コミット | author | 何をしたか |
|---|---|---|
| `cd3d1f5` | 15:12:27 | 分離の設計書（不変条件を宣言） |
| **`512a64d`** | **17:25:59** | **`git grep -i heavy -- web/` が空になった** |
| `ae7b503` | 22:15:56 | リリースゲートの設計書 |
| **`764a4c4`** | **22:56:13** | **`release-evidence.mjs` を追加（破れた）** |

**例外を認める記録は無い**（実装役が確認）: release-gate 設計書の `heavy` 13 行はすべて
ワークフローの話で、スクリプトの置き場に触れていない。release-gate には plan 自体が無く、
`web/scripts/release-evidence.mjs` を名指す文書は specs にも plans にも 0 件。

**見張りも無い**: 境界を grep で見張るスクリプトは 0 件。`web/package.json` の `scripts` は
`check:sw` と `check:version` だけで、`check:boundary` に相当するものは無い。
**`512a64d` の `git grep` はコミット本文に書かれた 1 回きりの手作業**——
宣言はされたが、実行され続ける形になっていない（memory `success-is-not-quality`
「上限は宣言しただけでは検証できない」）。**5 時間半後に破れたのはその帰結。**

**公平な反論も記録する（実装役の指摘、支持する）**: `web/scripts/` には既に
`check-sw.mjs` と `check-version.mjs` が居て、どちらもアプリのコードではなく CI の配管である。
`release-evidence.mjs` はその前例に素直に従った形で、「不変条件はアプリのビルド/テストグラフの
話であって `web/` ディレクトリ全体の話ではない」という読みはあり得る。
**ただし不変条件自身の測り方（`git grep -- web/`）で測れば破れている。**
→ **どちらの読みを採るかが裁定事項**。R-2（道具の置き場）を決めれば自動的に決まる。

### 3. 重量級の在席判定が、10 秒のジョブで満たされる（確度: 実測、実行中の走行で確認）

`release-evidence.mjs:105` は `job.name.includes("Heavy corpus")` で在席を見る。
走行中の `32908847850` が返すジョブ名は実測で:

    "Heavy corpus / Version numbers agree"     ← 版数ゲート、実測 11 秒
    "Heavy corpus / Corpus vs reference"       ← 本体、実測 35〜41 分

**前者だけでも判定は通る。** 35 分の本体が走らなくても「重量級はこの走行に含まれている」
と書ける。memory「文字列で分類する規則は、その文字列が否定文に出てくる日に壊れる」の型。
さらに `release.yml` を読むテストは **0 本**（`git grep -l release.yml -- web heavy reference` が空）
なので、`release.yml` 側のジョブ名を変えても誰も気づかない。書き手と読み手の欄一致テストが無い。

### 4. `skipped` は「落ちた」にも「成功」にも入らず、「進行中」と書かれる（確度: 実測・コード読み）

`release-evidence.mjs:15` の `BAD_CONCLUSIONS` は
`failure` / `cancelled` / `timed_out` / `action_required` の 4 つ。
`skipped` と `neutral` はどちらにも入らないので `running` に落ち、表には **「進行中」** と出る。
さらに `running.length > 0` のとき本文が
「（進行中 N——**この証拠を書いているジョブ自身がそれである**）」と断定するので、
**skip された検査が「証拠を書いているジョブ自身」だと名指しされる。**
テスト 11 本に `skipped` の例は無い（`failure` と `cancelled` だけ）。

「1 つでもあれば証拠を書かない」という冒頭コメントの主張より、実装は弱い。

### 5. 重量級レポートの欠落が、2 か所の許容で黙って通る（確度: コード読み）

`heavy-corpus.yml:188` `if-no-files-found: warn`（上げられなくても警告だけ）と
`release.yml` の heavy-report 取得の `continue-on-error: true`（取れなくても続行）。
`release-evidence.mjs` の在席判定は**ジョブ名しか見ず、添付の中身を見ない**ので、
「重量級 = 成功」と書かれた証拠が、報告書を添付せずに Release へ載りうる。
読む人は添付があると思う。memory「在って失敗の 2 種類」「テストは何も主張しないことがある」。

### 6. `web/tsconfig.json` の理由が腐った——**証拠だけが運び出された**（確度: 実測・確定）

`include` に `tests` を足した理由として「重量級コーパスのテストコードが丸ごと型検査の
外にいた」と書いてある。**削除行で裏を取った**（memory `past-tense-claims-need-old-code`）:
書いたのは `1cbfa04`「Typecheck the tests, and find what was hiding there」(2026-08-17)、
その差分の削除行が `"include": ["src", "vite.config.ts", "vite.heavy.config.ts"]`。
**主張は書いた時点で真**で、コミット本文は実害まで記録している
（`summaryName(name, "calls")` が存在しない引数を渡していた）。**A5 のような「最初から嘘」ではない。**

**腐ったのは証拠のほう。** 名指された `summaryName` は**いま `heavy/tests/corpus/report.ts` に居り、
`web/` を grep しても 1 件も出ない**——**運び出したのは同じ `512a64d`**。
しかも `512a64d` はこのコメントを意識的に残している（本文「実害の記録も残す——
理由は残ったテストにそのまま当てはまる」）。一般則は当てはまるが、
**指し示す指だけ残して、指の先の現物は同じコミットで外へ出た。**

`include` の指定自体は今も正しい（`web/tests/e2e` 27 本 + `web/tests/unit` 2 本）。
直すなら「重量級コーパスの」を落として残ったテストで言い直すだけ。裁定不要の小さな仕事。

### 7. 見たが問題なし（却下）

- **ロールバック手順は在る。** `docs/deploy.md:111-117`、`CLAUDE.md:72`、
  設計書 §5 に通常（revert + パッチ版タグ）と緊急（古いタグで `workflow_dispatch`）の 2 経路。
  当初「誰も書いていない疑い」を持ったが、現物にある。
- **`deploy.yml` の手動経路は素通しではない。** `:30` の
  `if: !startsWith(github.ref,'refs/tags/v')` で v* タグ以外は落ち、
  `workflow_dispatch` のときは job summary に「検査を迂回して配った」と書く。
- **2 つの tsconfig の `compilerOptions` は `types` 以外一致**（手で確認）。今は揃っている。
- **`--tag` に値が無いときの黙認は塞がれている**（`check-version.mjs` main()、テストあり）。

## 第 2 部：構築のベストプラクティス（見直し項目）

### R-1. `heavy/` を毎回の CI に載せるか、載せない理由を書く

いまは「載せない」が**どこにも書かれていない**。分離の設計書
(`docs/superpowers/specs/2026-08-25-heavy-split-design.md` 系) が意図として書いているかは要確認。
選択肢: (a) `ci.yml` に heavy の typecheck/lint/test だけの軽いジョブを足す（重量級本体は載せない）
(b) `paths:` で `heavy/**` が触られた PR だけ走らせる (c) 現状維持＋理由を明記。

### R-2. リリースの道具が `web` パッケージの中にある

`check-version.mjs`（リポジトリ直下の `Cargo.toml`/`README`×2/`CHANGELOG` を読む）と
`release-evidence.mjs`（GitHub の走行を読む）は、どちらも **`web` と無関係**なのに
`web/scripts/` にあり、`working-directory: web` で走り、`web` の vitest がテストしている。
`heavy` を `web` から出した判断の鏡像。リポジトリ直下の `tools/` か `scripts/` が筋では。
これが直れば指摘 2（境界破れ）も自然に消える。

### R-3. `web` と `heavy` は独立した lockfile で同じ道具を持っている

両方が `typescript ^5.7.0` / `vite ^6.0.0` / `vitest ^3.0.0` / `@playwright/test ^1.50.0` /
`@biomejs/biome ^2.0.0` / `@types/node ^26.1.2` を宣言し、**lockfile は別**。
pnpm workspace は無い（`pnpm-workspace.yaml` 不在を確認）。
→ **計測器がアプリと違う版の vitest/playwright で走りうる。** 今は偶然揃っている。
加えて `heavy/package.json:5` は `packageManager: pnpm@10.32.1` を自分で宣言するが、
CI で実際に入る pnpm は `setup-web` が **`web/package.json` から**決める。
`node` の cache も `web/pnpm-lock.yaml` だけを鍵にしている（heavy の install は毎回素）。
検討: pnpm workspace 化、または共有 base tsconfig（`extends`）+ 道具版の一元化。

### R-4. `heavy` は `web` の `src/` を相対パスで深く掴んでいる

`heavy/harness/heavy-harness.ts:15` が `../../web/src/calc`、
`heavy/tests/ui/keys.ts:2` が **`../../../web/src/ui/Keypad/scientific`**（UI 層）。
CLAUDE.md の境界（`web/src/calc` は UI 非依存）は守られているが、
**heavy は `calc` だけでなく `ui` にも依存している**。分離は「web が heavy を知らない」
片方向だけで、逆向きの依存は移動前より見えにくくなった（別パッケージからの相対パス）。
宣言された依存（workspace 参照や `paths` エイリアス）にすると、壊れたとき機械が言う。

### R-5. タグが main の上に在ることを誰も見ていない

`release.yml` は `push: tags: ["v*"]`、`deploy.yml` は ref が `v*` タグかだけを見る。
**そのタグが `main` の子孫か**は検査されない。任意のブランチに `v0.5.0` を打ち、
版数 5 箇所を揃えれば、本番へ出る。「タグが本番への唯一の扉」は成立しているが、
**扉の向こうが main とは限らない**。1 行の `git merge-base --is-ancestor` で塞げる。

### R-6. 一次資料が追跡外の `.superpowers/` に置かれている

`.gitignore:15` が `.superpowers/` を丸ごと無視する。
実装側の 0.3.x triage（30 件）も、今日の当て直しも、この下にある。
重量級側の HANDOFF.md も同じ。**リポジトリを clone した人には見えず、
`git show` でも読めない**。今回のように「索引と現物が食い違う」ことが繰り返し起きる場所が、
版管理の外にある。設計書と計画は `docs/superpowers/` へ移す運用が既に始まっている
（`bc1d42d`）ので、台帳もそちらへ寄せるか、少なくとも所在を CLAUDE.md に書く。

### R-7. 役割復元の手段が非対称（セッション体制）

重量級側には `CalcArc-e2e/.superpowers/sdd/HANDOFF.md` の §0「あなたが誰か」があるが、
**実装側には HANDOFF.md が無い**（`.superpowers/sdd/` 直下は triage 2 本のみ）。
3 セッションとも process cwd は `/home/terapyon/dev/CalcArc` で、harness の手がかりは
全部実装側を指す。2026-08-20 に実際に役割取り違えの事故が起きている。
