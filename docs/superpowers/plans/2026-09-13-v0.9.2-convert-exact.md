# 0.9.2 枝 B（Convert の `=` を丸めずに値でまとめる）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert で `=` を押したとき、値の欄に正確な値（有限小数か既約分数 `p/q`）を入れる（外部監査 F3）。`1 ÷ 3 =` は `1/3`、1e10 以上でも `=` が効く。

**Architecture:** コアに `convert::settle::settle(value) -> CalcResult<String>` を置き、wasm が `settle_expression` として渡す。web の `ConvertCalc` に `settle` を足し、`UnitPanel.settled()` はそれを呼んで、返った文字列から値の欄を組み直す。**表示用の 10 桁の文字列を書き戻す迂回（`FOLD_CATEGORY` の恒等換算）をやめる**——為替も同じ口を通る。

**Tech Stack:** Rust（`calcarc-core`・`calcarc-wasm`）、TypeScript・React・vitest（実物の wasm を `initSync` で読む新しい形）、Playwright。

**Spec:** `docs/superpowers/specs/2026-09-12-v0.9.2-calc-fixes-design.md`（枝 `docs/v0.9.2-calc-fixes`。作業木 `/tmp/claude-1000/-home-terapyon-dev-CalcArc/5ec1639a-7bb9-411e-a4ab-2c148331ac6a/scratchpad/wt-091` にある）の §4（F3）と §9 の 3（分数 `1/3` で見せる、裁定済み）。

## Global Constraints

- **作業木は `/tmp/claude-1000/-home-terapyon-dev-CalcArc/5ec1639a-7bb9-411e-a4ab-2c148331ac6a/scratchpad/wt-B`**（枝 `fix/convert-exact`、起点 `eb94e72`）。`/home/terapyon/dev/CalcArc` と `/home/terapyon/dev/CalcArc-e2e` には触らない
- **`git push` と PR 作成をしない。`git add -A` を打たない。コミット前に `git branch --show-current` が `fix/convert-exact`**
- **コミットの前段は `&&` だけでつなぐ。**コミットメッセージはファイルに書いて `git commit -F`。末尾 2 行: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` と `Claude-Session: https://claude.ai/code/session_01BWysjEZm5GHRCYaC373V2S`
- **`calcarc-core` は panic しない。許容誤差をテストコードに書かない**
- **「39 字」は Entry の数え方**——値の欄の数字の語の `text.length`（`web/src/units/entry.ts:73,85`、上限は `web/src/convert/entry.ts` の `MAX_VALUE_DIGITS = 39`）。**小数点と先頭の `0` も 1 字**、符号は数えない（`UnitPanel` の `negative` が持つ）
- **重い段は回さない**: `pnpm wasm`・`wasm-pack build`・`wasm-pack test`・E2E・vite build。該当ステップは**統括（実装役）が calcarc-e3 に一言してから回す**と書いてある。実行者はそこで止めて報告する
- 版数は上げない。マニュアルの本文（設計書 §4.4）は枝 C が書くので、この枝では触らない

---

### Task 1: コアの `settle`

**Files:**
- Create: `crates/calcarc-core/src/convert/settle.rs`
- Modify: `crates/calcarc-core/src/convert/mod.rs`（`pub mod settle;`）

**Interfaces:**
- Produces: `pub fn settle(value: &str) -> CalcResult<String>`（`calcarc_core::convert::settle::settle`）、`pub const MAX_VALUE_CHARS: usize = 39`

- [ ] **Step 1: ファイルを作り、テストを先に書く**（`settle.rs`。`mod.rs` の他の `pub mod` の並びに `pub mod settle;` を足す。`evaluate_to_rational` と `UnitSet` の `use` の書き方は `mod.rs` の既存の行に合わせる）

```rust
//! `=` で式を値にまとめる(0.9.2 設計書 §4、外部監査 F3)。
//!
//! **丸めない。** 値を正確に書く——有限小数が 39 字に収まればそれ、そうでなければ既約分数
//! `p/q`。表示の 10 桁(`format::format_rational`)とは別の口である。書いた文字列は Convert の
//! 値の欄(web の Entry)にそのまま入り、次の計算でコアに読み直される。
//!
//! **39 字は Entry の数え方**(`web/src/units/entry.ts` の `pushDigit`/`pushDot` が見る
//! `text.length`、上限は `web/src/convert/entry.ts` の `MAX_VALUE_DIGITS`)——小数点と先頭の
//! `0` も 1 字。符号は数えない(Entry の外の `negative` が持つ)。
//!
//! **この数え方なら出力は必ず読み直せる。** i128 の値は高々 39 桁なので、整数と分数の分子・
//! 分母は 39 字に収まる。小数点を含む 39 字の文字列は数字が高々 38 個なので、読み直した分子は
//! 10^38 未満、分母は 10^37 以下で、どちらも i128 に収まる(`expr/parse.rs` の `literal` は
//! i128 に収まらない数字を `Overflow` にする)。**数字の個数で数えるとこの論法は崩れる**
//! ——`5^16/2^39` は小数部が 39 桁で、読み直した分子が 5^55(i128 の上限を超える)。

use crate::expr::{UnitSet, evaluate_to_rational};
use crate::CalcResult;

/// 値の欄に入る字数の上限(`web/src/convert/entry.ts` の `MAX_VALUE_DIGITS` と同じ数)。
pub const MAX_VALUE_CHARS: usize = 39;

/// 式を正確な値の文字列にする。有限小数(39 字以内)か既約分数 `p/q`、負なら先頭に `-`。
pub fn settle(value: &str) -> CalcResult<String> {
    // 単項マイナスは構文解析器に無い——`convert()` と同じく `0` を前置する(`mod.rs` の註)。
    let owned;
    let text = if value.starts_with('-') {
        owned = format!("0{value}");
        &owned
    } else {
        value
    };
    let (num, den) = evaluate_to_rational(text, UnitSet::None)?.parts();
    let sign = if num < 0 { "-" } else { "" };
    // `Rational` は `i128::MIN` を持たない(`rational.rs` の註)ので絶対値は i128 に収まる。
    let magnitude = num.unsigned_abs();
    let denominator = den.unsigned_abs();
    Ok(match decimal(magnitude, denominator) {
        Some(text) if text.len() <= MAX_VALUE_CHARS => format!("{sign}{text}"),
        _ => format!("{sign}{magnitude}/{denominator}"),
    })
}

/// `n/d`(既約)が有限小数なら、指数もカンマも使わない 10 進の文字列。有限小数でなければ
/// None。桁が u128 に収まらないとき(そのときは必ず 39 字を超える)も None。
fn decimal(n: u128, d: u128) -> Option<String> {
    let (mut rest, mut twos, mut fives) = (d, 0u32, 0u32);
    while rest % 2 == 0 {
        rest /= 2;
        twos += 1;
    }
    while rest % 5 == 0 {
        rest /= 5;
        fives += 1;
    }
    if rest != 1 {
        return None;
    }
    // n/d = n·2^(k−twos)·5^(k−fives) / 10^k
    let k = twos.max(fives);
    let scaled = n
        .checked_mul(2u128.checked_pow(k - twos)?)?
        .checked_mul(5u128.checked_pow(k - fives)?)?;
    let digits = scaled.to_string();
    let k = k as usize;
    Some(if k == 0 {
        digits
    } else if digits.len() > k {
        let (whole, part) = digits.split_at(digits.len() - k);
        format!("{whole}.{part}")
    } else {
        format!("0.{}{digits}", "0".repeat(k - digits.len()))
    })
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::CalcError;

    /// 読み直す(`convert()` と同じく、先頭の `-` には `0` を前置する)。
    fn reread(text: &str) -> (i128, i128) {
        let owned;
        let text = if text.starts_with('-') {
            owned = format!("0{text}");
            &owned
        } else {
            text
        };
        evaluate_to_rational(text, UnitSet::None).unwrap().parts()
    }

    /// 出力が値の欄に入り(語ごとに 39 字以内)、読み直すと元の値に戻ること(注記 D)。
    fn settles_to(value: &str) -> String {
        let settled = settle(value).unwrap();
        assert_eq!(reread(&settled), reread(value), "{value} → {settled} が同じ値に戻らない");
        assert!(
            settled
                .trim_start_matches('-')
                .split('/')
                .all(|part| part.len() <= MAX_VALUE_CHARS),
            "{settled} は値の欄に入らない"
        );
        settled
    }

    #[test]
    fn a_terminating_value_stays_a_decimal() {
        // 監査の例(設計書 §1 の f3-probe)。どれも 10 桁に丸めない。
        assert_eq!(settles_to("0.123456789012"), "0.123456789012");
        assert_eq!(settles_to("9999999999.4"), "9999999999.4");
        assert_eq!(settles_to("12345678901.5"), "12345678901.5");
        assert_eq!(settles_to("1/4"), "0.25");
        assert_eq!(settles_to("5*12"), "60");
        assert_eq!(settles_to("0"), "0");
    }

    #[test]
    fn a_repeating_value_becomes_a_reduced_fraction() {
        // 裁定(§9 の 3): 割り切れない値は分数で見せる。
        assert_eq!(settles_to("1/3"), "1/3");
        assert_eq!(settles_to("2/6"), "1/3");
        assert_eq!(settles_to("10/4/3"), "5/6");
    }

    #[test]
    fn the_sign_leads() {
        assert_eq!(settles_to("-12.5"), "-12.5");
        assert_eq!(settles_to("-1/3"), "-1/3");
        assert_eq!(settles_to("1-3/2"), "-0.5");
    }

    #[test]
    fn the_boundary_is_counted_the_way_the_entry_counts() {
        // 注記 D の境目(字数は設計書 §4.3、Python の fractions で数えた)。
        // 1/2^37 は小数で 39 字 → 有限小数のまま。
        assert_eq!(
            settles_to("1/137438953472"),
            "0.0000000000072759576141834259033203125"
        );
        // 1/2^38 は小数なら 40 字 → 分数。
        assert_eq!(settles_to("1/274877906944"), "1/274877906944");
        // 5^16/2^39 は小数なら 41 字(読み直した分子は 5^55 で i128 を超える)→ 分数。
        assert_eq!(
            settles_to("152587890625/549755813888"),
            "152587890625/549755813888"
        );
        // i128 の上限の整数(39 桁)もそのまま入る。
        assert_eq!(
            settles_to("170141183460469231731687303715884105727"),
            "170141183460469231731687303715884105727"
        );
    }

    #[test]
    fn a_value_that_cannot_be_evaluated_is_an_error() {
        assert_eq!(settle("1/0"), Err(CalcError::DivisionByZero));
        assert_eq!(settle("1+"), Err(CalcError::SyntaxError));
    }
}
```

- [ ] **Step 2: 赤→緑を見る**

先に `settle` の本体を `todo!()` ではなく `Ok(String::new())` にして走らせ、赤を見てから上の本体を入れる（`todo!()` は panic なので使わない）。
Run: `cd <wt-B> && cargo test -p calcarc-core convert::settle`
Expected: 本体を入れる前は FAIL、入れたら PASS。**`1/0` が `DivisionByZero` 以外、`1+` が `SyntaxError` 以外なら、コアが返す種類を報告して assert をそれに合わせる**（種類の名前は `crates/calcarc-core/src/error.rs`）

- [ ] **Step 3: 門を通してコミットする**

```bash
cd <wt-B> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p calcarc-core
```

コミット（名指し: `settle.rs`・`convert/mod.rs`）。件名例: `Settle an expression to its exact value, a decimal or a reduced fraction`

- [ ] **Step 4: 番人を名指しする前に変異させる**（コミットのあと、再編集で戻す）

`MAX_VALUE_CHARS` の比べ方を「数字の個数」（`text.chars().filter(char::is_ascii_digit).count()`）に変える → `the_boundary_is_counted_the_way_the_entry_counts`（`5^16/2^39` は小数部 39 桁で小数を選び、読み直しが Overflow）が FAIL することを見る。

---

### Task 2: wasm の口 `settle_expression`

**Files:**
- Modify: `crates/calcarc-wasm/src/lib.rs`（`convert` の直後）
- Test: `crates/calcarc-wasm/tests/web.rs`（`a_conversion_crosses_the_boundary_as_text` の直後）

**Interfaces:**
- Consumes: `calcarc_core::convert::settle::settle`
- Produces: wasm の `settle_expression(value: &str) -> JsValue`（`Outcome<ConvertText>`: `{kind:"ok", text}` / `{kind:"error", code}`）

- [ ] **Step 1: 境界の行を書く**（`web.rs`）

```rust
#[wasm_bindgen_test]
fn settling_keeps_the_exact_value_across_the_boundary() {
    // 0.9.2 設計書 §4: `=` は丸めない。表示の 10 桁(`convert`)とは別の口。
    let text = |value: &str| {
        get(&calcarc_wasm::settle_expression(value), "text").as_string()
    };
    assert_eq!(text("1/3").as_deref(), Some("1/3"));
    assert_eq!(text("12345678901.5").as_deref(), Some("12345678901.5"));
    assert_eq!(text("-12.5").as_deref(), Some("-12.5"));
    let bad = calcarc_wasm::settle_expression("1/0");
    assert_eq!(get(&bad, "kind").as_string().as_deref(), Some("error"));
}
```

- [ ] **Step 2: 口を足す**（`lib.rs`）

```rust
/// `=` で式を値にまとめる(0.9.2 設計書 §4、外部監査 F3)。**丸めない**——有限小数か
/// 既約分数 `p/q`。表示の 10 桁(`convert`)とは別の口で、Convert の値の欄に入る。
/// 計算は `calcarc-core` の `convert::settle` が持つ(ここには置かない)。
#[wasm_bindgen]
pub fn settle_expression(value: &str) -> JsValue {
    let result: Outcome<ConvertText> = convert_core::settle::settle(value)
        .map(|text| ConvertText { text })
        .into();
    to_js_value(&result)
}
```

（`convert_core` は `lib.rs` の既存の別名。無ければ `calcarc_core::convert` を直接書く。）

- [ ] **Step 3: 門**

```bash
cd <wt-B> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace
```

**`wasm-pack test` は重い段なので、ここで止めて統括に渡す。**統括が calcarc-e3 に一言してから
`wasm-pack test --headless --chrome --chromedriver ~/.cache/.wasm-pack/chromedriver-d65213741bcf1a26/chromedriver crates/calcarc-wasm`
と `cd web && pnpm install && pnpm wasm` を回す（この機の Chrome に合う chromedriver を明示する）。

- [ ] **Step 4: コミット**（名指し: `lib.rs`・`web.rs`）。件名例: `Expose settle across the wasm boundary`

---

### Task 3: 値の欄を正確な値で組み直す（web）

**前提**: Task 2 の Step 3 で統括が `web/node_modules` と、`settle_expression` を含む `web/src/wasm/` を用意済み。

**Files:**
- Modify: `web/src/convert/types.ts`（`SettleResult`・`ConvertCalc.settle`）
- Modify: `web/src/convert/index.ts`（`settle` を包む）
- Modify: `web/src/convert/entry.ts`（`fromSettled`）
- Modify: `web/src/ui/Convert/UnitPanel.tsx`（`settled()`・`press` の `eq`・`FOLD_*` を消す）
- Modify: `web/src/ui/Convert/UnitPanel.test.tsx`（偽物に `settle`）
- Create: `web/src/ui/Convert/UnitPanel.wasm.test.tsx`（実物の wasm を通す）

**Interfaces:**
- Consumes: wasm の `settle_expression`
- Produces: `ConvertCalc.settle(value: string): SettleResult`、`fromSettled(value: string): Entry`

- [ ] **Step 1: 型と包み**

`web/src/convert/types.ts` の `ConvertResult` の近くに:

```ts
/** `=` の答え(0.9.2 設計書 §4)。有限小数か既約分数 `p/q`、負なら先頭に `-`。丸めない。 */
export type SettleResult = Outcome<{ text: string }, ConvertErrorCode>;
```

`ConvertCalc` に（`convert` の直後）:

```ts
  /**
   * `=` で式を値にまとめる(0.9.2 設計書 §4)。**丸めない**——`convert` の表示の 10 桁とは
   * 別の口で、答えは値の欄にそのまま入り、次の計算で読み直される。
   */
  settle(value: string): SettleResult;
```

`web/src/convert/index.ts` の wasm の import に `settle_expression` を足し、`initConvert` が組む物に:

```ts
        settle: (value) => settle_expression(value) as SettleResult,
```

- [ ] **Step 2: 値の欄を答えから組み直す関数**（`web/src/convert/entry.ts`）

```ts
/**
 * `=` の答え(コアの `settle`: 有限小数か既約分数 `p/q`、**符号なし**)から値の欄を組み直す
 * (0.9.2 設計書 §4)。分数は「数・/・数」の 3 語にする——次に打つ演算子はそのあとに続き、
 * コアが式として読み直す(`/` は `×`・`÷` と同じ段で左から畳むので、`1/3 × 3` は 1)。
 */
export function fromSettled(value: string): units.Entry {
  const [numerator = "", denominator] = value.split("/");
  if (denominator === undefined) return units.fromDigits(numerator);
  return {
    tokens: [
      { kind: "digits", text: numerator },
      { kind: "op", op: "/" },
      { kind: "digits", text: denominator },
    ],
  };
}
```

- [ ] **Step 3: 実物の wasm を通す検査を先に書く**（新規 `web/src/ui/Convert/UnitPanel.wasm.test.tsx`。**描き方・押し方・欄の読み方は `UnitPanel.test.tsx` の `renderPanel`・`press`・`echo` などの補助に倣う**——必要な補助はこのファイルに写す）

wasm の読み込みはこの形にする（このリポジトリで初めての形。**jsdom の大域 `URL` は Node の fs に拒まれる**ので、ファイルの URL ではなく文字列のパスで読む——2026-09-11 の `3eb46cc` で実際に踏んだ）:

```tsx
// 実物の wasm を通す(0.9.2 設計書 §4.3)。`UnitPanel.test.tsx` の偽物の算数では、`=` が
// 桁を落とすかどうかが見えない——落としていたのは wasm の境界の書式だったからである。
// **wasm の `init`(fetch で読む)だけを、ファイルから同期で読む `initSync` に差し替える。**
// 包み(`initConvert`)は本物のまま通す。
vi.mock("../../wasm/calcarc_wasm.js", async (importOriginal) => {
  const actual =
    await importOriginal<typeof import("../../wasm/calcarc_wasm.js")>();
  const { readFileSync } = await import("node:fs");
  const { join } = await import("node:path");
  const bytes = readFileSync(join(process.cwd(), "src/wasm/calcarc_wasm_bg.wasm"));
  return {
    ...actual,
    default: async () => actual.initSync({ module: bytes }),
  };
});
```

（`initSync` の引数の形は `web/src/wasm/calcarc_wasm.d.ts` で確かめる。`process.cwd()` は vitest の起点 `web/` である——違えば `import.meta.dirname` から組む。）

検査の中身（**監査の例、設計書 §4.3 のとおり**）:
1. 長さ（km→mi など、`UnitPanel.test.tsx` が使う単位）で `1 ÷ 3 =` → 値の欄が `1/3`。続けて `× 3 − 1` → 換算の答えが `0`
2. `0.123456789012 =` → 値の欄がそのまま `0.123456789012`
3. `9999999999.4 =` → そのまま
4. `12345678901.5 =` → そのまま（**`=` が効く**。以前は何もしなかった）
5. 為替（JPY を着地に選ぶ）で `12.5 =` → `12.5`（着地通貨の桁に丸めない）、`0.123456789012 =` → そのまま。**為替の盤面がレートの取得を要るなら、`UnitPanel.test.tsx` の為替の検査がどう用意しているかに倣う**

- [ ] **Step 4: 赤を見る**

Run: `cd <wt-B>/web && pnpm exec vitest run src/ui/Convert/UnitPanel.wasm.test.tsx`
Expected: FAIL（`1/3 =` が `0.3333333333`、`12345678901.5 =` で値が変わらない）

- [ ] **Step 5: `settled()` を直す**（`UnitPanel.tsx`）

`FOLD_CATEGORY`・`FOLD_UNIT` とその註（`:114-122`）を消し、`settled()` を:

```tsx
  function settled(): string | null {
    if (calc === null || typed === "") return null;
    // **`=` は式を値にまとめるだけで、桁を落とさない**(0.9.2 設計書 §4、外部監査 F3)。
    // 以前は表示用の 10 桁の文字列を書き戻していた——`1/3 =` が 0.3333333333 になり、
    // 1e10 以上は書き戻せず `=` が効かなかった。為替も同じ口で、着地通貨の桁にも丸めない。
    const result = calc.settle(typed);
    return result.kind === "ok" ? result.text : null;
  }
```

`press` の `case "eq"` の `setEntry(fromDigits(...))` を `setEntry(fromSettled(folded.replace(/^-/, "")))` にし、import を合わせる（`fromDigits` を他で使っていなければ import から外す）。

- [ ] **Step 6: 偽物を合わせる**（`UnitPanel.test.tsx`）

偽物の `calc` に `settle` を足す。**偽物は配線のための物**（このファイルの冒頭の註）なので、既存の `evaluate()` で式を数にし、`String(n)` を `{ kind: "ok", text }` で返せば足りる（評価できなければ `{ kind: "error", code: "SyntaxError" }`）。既存の `settles the expression into the value when = is pressed` と `keeps \`=\` disabled while the expression cannot be evaluated` が緑のままであること。

- [ ] **Step 7: 緑を見る**

Run: `cd <wt-B>/web && pnpm typecheck && pnpm lint && pnpm exec vitest run src/ui/Convert`
Expected: PASS

- [ ] **Step 8: 番人を名指しする前に変異させる**（コミットのあと、再編集で戻す）

`settled()` を以前の形（`calc.convert(typed, category, from, from)` の表示の文字列を返す）に一時的に戻す → `UnitPanel.wasm.test.tsx` が FAIL（`1/3` が `0.3333333333`）。**偽物の `UnitPanel.test.tsx` は緑のまま**であることも報告に書く（それがこの検査を実物で置く理由）。

- [ ] **Step 9: 門を通してコミットする**

```bash
cd <wt-B>/web && pnpm typecheck && pnpm lint && pnpm test
```

コミット（名指し: 上の Files の 6 つ）。件名例: `Keep the exact value when = settles a conversion`

---

### Task 4: 実ブラウザで確かめる（E2E、Chromium・WebKit）

**Files:**
- Modify: `web/tests/e2e/convert.spec.ts`

- [ ] **Step 1: 書く**（`convert.spec.ts` の既存の補助 `panel`・`press`・`main`・`echo` と、`convert-result` の testid を使う。`=` のアクセシブルネームは「計算する」、`÷` は「割る」）

```ts
test("= keeps the exact value, so 1 ÷ 3 = × 3 − 1 is 0", async ({ page }) => {
  // 0.9.2 設計書 §4(外部監査 F3)。以前は `=` が 10 桁に丸めて書き戻し、
  // 答えが 0 でなく -6.213711922e-11 になっていた。
  await page.goto("/#convert/length");
  await expect(panel(page)).toBeVisible();
  // 単位の選び方は既存の検査(km→mi)に倣う
  await press(page, ["1", "割る", "3", "計算する"]);
  // 値の欄が「1/3」であることを、既存の検査が値の欄を読むのと同じ locator で見る
  await press(page, ["掛ける", "3", "引く", "1"]);
  // 換算の答え(`convert-result`)が 0 であることを見る
});
```

（コメントで示した 3 か所は、`convert.spec.ts` の既存の行の書き方をそのまま使って埋める。）

- [ ] **Step 2: 統括に渡す**

E2E は重い段。**実行者は回さずに止め、統括に渡す。**統括が calcarc-e3 に一言してから Chromium で
`cd web && pnpm exec playwright test tests/e2e/convert.spec.ts --project <chromium の名前>` を回す（プロジェクト名は
`playwright.config.ts` で確かめる）。WebKit は CI で確かめる。

- [ ] **Step 3: コミット**（名指し: `convert.spec.ts`）。件名例: `Check in a real browser that = keeps the exact value`

---

### Task 5: 枝の末尾（統括）

- [ ] `cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`
- [ ] `cd web && pnpm typecheck && pnpm lint && pnpm test`
- [ ] 重い段（`wasm-pack test`・E2E の該当 spec）は calcarc-e3 に一言してから
- [ ] 枝の先端 SHA を calcarc-e3 に伝える
