# 0.9.2 枝 D（ローン月額の 10 億円の上限）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** f64 で決める定例月額と賞与額が 10 億円を超えたら、答えを出さずに `Overflow` のエラーにする（外部監査 F2 の追加の裁定）。

**Architecture:** 上限を掛ける場所は `crates/calcarc-core/src/finance/loan/closed_form.rs` の `monthly_payment` の f64 の枝 1 か所。**切り捨てたあとの月額**（`a as u64`）が上限を超えたら `Err(CalcError::Overflow)`。定例月額と、賞与を使うときの 2 つの分（`bonus::compute_forward` が `forward::compute` 経由で同じ関数を 2 回呼ぶ）がここで止まる。金利 0%・1 回払い・逆算の答えは厳密な経路なので掛けない。画面は既存の「Math ERROR」。

**Tech Stack:** Rust（`calcarc-core`）、Markdown（`docs/numerical-policy.md`）、Python（`reference` の pin テストを走らせるだけ）。

**Spec:** `docs/superpowers/specs/2026-09-12-v0.9.2-calc-fixes-design.md`（枝 `docs/v0.9.2-calc-fixes`。作業木 `/tmp/claude-1000/-home-terapyon-dev-CalcArc/5ec1639a-7bb9-411e-a4ab-2c148331ac6a/scratchpad/wt-091` にある）の §5.1・§8（枝 D）・§9 の 6・7。

## Global Constraints

- **作業木は `/tmp/claude-1000/-home-terapyon-dev-CalcArc/5ec1639a-7bb9-411e-a4ab-2c148331ac6a/scratchpad/wt-D`**（枝 `fix/loan-monthly-cap`、起点 `eb94e72`——calcarc-88 の PR A（#135）が入った main）。`/home/terapyon/dev/CalcArc` と `/home/terapyon/dev/CalcArc-e2e` には触らない
- **`git push` と PR 作成をしない。`git add -A` を打たない。コミット前に `git branch --show-current` が `fix/loan-monthly-cap`**
- **コミットの前段は `&&` だけでつなぐ。**コミットメッセージはファイルに書いて `git commit -F`。末尾 2 行: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` と `Claude-Session: https://claude.ai/code/session_01BWysjEZm5GHRCYaC373V2S`
- **`calcarc-core` は panic しない。許容誤差をテストコードに書かない**
- **上限の比べ方は `a as u64 > MAX_VERIFIED_MONTHLY_YEN`**——ちょうど 10 億円は通す（calcarc-1e の条件 2。calcarc-88 の golden に理論月額がちょうど 10 億円の行 `residual/exact/15/2/987654400/81` がある）
- **上限の上の検査は、理論月額が上限 + 2 円以上の入力で**（(10 億, 10 億 + 1) は f64 のずれで両側に落ちうる。calcarc-88 の実測で最悪 −0.866 円）
- **`uv` は `--no-config` を付ける**（`run` も）。走らせたあと `git status` で `reference/uv.lock` が変わっていないことを見る
- 版数は上げない。重い段（wasm のビルド・E2E）は要らない

---

### Task 1: 月額の上限

**Files:**
- Modify: `crates/calcarc-core/src/finance/loan/closed_form.rs`（定数・`monthly_payment`・テスト）
- Test: `crates/calcarc-core/src/finance/loan/bonus.rs`（テスト 1 本）
- Modify: `docs/numerical-policy.md:177`（「予定」の注記を外す）

- [ ] **Step 1: 先に赤くするテストを書く**（`closed_form.rs` の `mod tests` の末尾）

```rust
    #[test]
    fn a_monthly_payment_above_the_verified_cap_is_an_error() {
        // 0.9.2 設計書 §5.1(外部監査 F2 の追加の裁定): f64 で決める月額は 10 億円まで
        // しか精度を確かめていない。**切り捨てた月額**がそれを超えたら答えを出さない。
        let r = Rate::from_percent("1").unwrap();
        // 理論月額は約 1,001,250,000 円(上限 + 2 円より十分に上。境目の 1 円は f64 で揺れる)。
        assert_eq!(
            monthly_payment(2_000_000_000, &r, 2, 0),
            Err(CalcError::Overflow)
        );
        // 上限の下は答えが出る(約 996,000,000 円)。
        assert!(monthly_payment(1_990_000_000, &r, 2, 0).is_ok());
    }

    #[test]
    fn exactly_the_cap_is_still_an_answer() {
        // ちょうど 10 億円は通す(calcarc-1e の条件 2)。calcarc-88 の境界 golden の
        // `residual/exact/15/2/987654400/81`(`monthly_floor` = 1,000,000,000)と同じ入力。
        let r = Rate::from_percent("15").unwrap();
        assert_eq!(monthly_payment(987_654_400, &r, 2, 81), Ok(1_000_000_000));
    }

    #[test]
    fn the_exact_paths_are_outside_the_cap() {
        // 金利 0% と 1 回払いは f64 を通らない厳密な経路で、精度の心配が無い(§9 の 7)。
        // 0%: finance.json の `loan_forward/18446744073709551615/0/600/0` と同じ答え。
        let zero = Rate::from_percent("0").unwrap();
        assert_eq!(
            monthly_payment(18_446_744_073_709_551_615, &zero, 600, 0),
            Ok(30_744_573_456_182_586)
        );
        let r = Rate::from_percent("1").unwrap();
        assert!(monthly_payment(5_000_000_000, &r, 1, 0).is_ok());
    }
```

`bonus.rs` の `mod tests` の末尾に:

```rust
    #[test]
    fn a_bonus_payment_above_the_cap_is_an_error() {
        // 賞与の分も同じ `monthly_payment` の f64 の枝を通る(0.9.2 設計書 §5.1)。
        // 月払い分は約 4.6 億円で上限の下、賞与分は 4 回で 1 回あたり約 22.8 億円。
        let r = Rate::from_percent("1").unwrap();
        assert_eq!(
            compute_forward(20_000_000_000, 9_000_000_000, &r, 24),
            Err(CalcError::Overflow)
        );
    }
```

（`bonus.rs` のテストの `use` に `Rate`・`CalcError` が無ければ足す。）

- [ ] **Step 2: 赤を見る**

Run: `cd <wt-D> && cargo test -p calcarc-core finance::loan`
Expected: `a_monthly_payment_above_the_verified_cap_is_an_error` と `a_bonus_payment_above_the_cap_is_an_error` が FAIL（Ok が返る）。**`exactly_the_cap_is_still_an_answer` と `the_exact_paths_are_outside_the_cap` はこの時点で PASS のはず**——`exactly…` が 999,999,999 などで FAIL したら、calcarc-88 の実測（この入力の製品の月額は 1,000,000,000）と食い違うので、**止めて報告**

- [ ] **Step 3: 上限を置く**（`closed_form.rs`）

`use` の直後に:

```rust
/// f64 で決める月額の上限(円)。**これを超える月額は答えを出さず `Overflow` にする。**
///
/// 月額は f64 で決めるので、金額が大きいと切り捨てが理論値から 1 円を超えてずれうる
/// (Python で閉形式を写した計算で、年 0.0001%・2 回の月額約 45 億円から 1 円に届いた)。
/// 精度を確かめたのはここまで——出どころは `docs/numerical-policy.md` の節
/// 「定例月額の許容（2026-09-12、外部監査 F2・利用者の裁定）」で、同じ数を
/// `testdata/loan_boundary.json` の `max_monthly_yen` が持つ(参照実装が照合する)。
///
/// **比べるのは切り捨てたあとの月額**——ちょうど 10 億円は通す(境界 golden の
/// `residual/exact/15/2/987654400/81`)。**f64 を通らない経路(金利 0%・1 回払い)と
/// 逆算の答えには掛けない**(0.9.2 設計書 §9 の 7)。
const MAX_VERIFIED_MONTHLY_YEN: u64 = 1_000_000_000;
```

`monthly_payment` の末尾 `Ok(a as u64) // 円未満切り捨て(設計書 §2 の 1 語)` を:

```rust
    let monthly = a as u64; // 円未満切り捨て(設計書 §2 の 1 語)
    if monthly > MAX_VERIFIED_MONTHLY_YEN {
        // 精度を確かめていない答えは出さない(0.9.2 設計書 §5.1。画面は既存の Math ERROR)。
        return Err(CalcError::Overflow);
    }
    Ok(monthly)
```

関数の docstring の「金利 0% と 1 回払いは f64 を通さない——…」の段落の後に「f64 の枝の答えは `MAX_VERIFIED_MONTHLY_YEN` まで(超えたら `Overflow`)。」を 1 行足す。

- [ ] **Step 4: 緑を見る**

Run: `cargo test -p calcarc-core`
Expected: PASS（`finance_golden`・`loan_boundary_golden`・`bonus`・`forward`・`inverse` の既存のテストを含む。**金額の大きい既存のテストが Overflow で落ちたら止めて報告**——golden とコーパスの期待値に 10 億円超の f64 経路の件が無いことは統括が確かめた）

- [ ] **Step 5: 数値方針の「予定」の注記を外す**（`docs/numerical-policy.md:177`）

「**10 億円を超える月額は `Overflow` のエラーにする（予定。製品の変更が入る PR でこの注記を外す）。**」を
「**10 億円を超える月額は `Overflow` のエラーにする。**」にする。次の文（「それより大きい金額では、…」）と `:164` は変えない。
（**この文言は 2026-09-13 に calcarc-88 に送ってあり、合意待ち**。統括が合意を確かめてから Task 1 を発注する——変わったらこの行を直す。範囲の一文は calcarc-88 の PR D が足す。）

Run: `cd <wt-D>/reference && uv run --no-config pytest tests/test_policy_matches_loan_boundary.py -q && cd .. && git status --porcelain reference/`
Expected: PASS、`reference/` に変更なし（`uv.lock` が書き換わっていない）

- [ ] **Step 6: 門を通してコミットする**

```bash
cd <wt-D> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace
```

コミット（名指し: `closed_form.rs`・`bonus.rs`・`docs/numerical-policy.md`）。件名例: `Refuse a monthly payment above the verified ¥1bn cap`

- [ ] **Step 7: 番人を名指しする前に変異させる**（コミットのあと、再編集で戻す）

1. `monthly > MAX_VERIFIED_MONTHLY_YEN` を `monthly >= MAX_VERIFIED_MONTHLY_YEN` にする → `exactly_the_cap_is_still_an_answer` と `loan_boundary_golden`（`residual/exact/15/2/987654400/81`）が FAIL
2. 上限の `if` を消す → `a_monthly_payment_above…` と `a_bonus_payment_above…` が FAIL

それぞれ落ちたテストの名前を報告に写し、戻して `git diff` が空であることを見る。

---

### Task 2: 枝の末尾（統括）

- [ ] `cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`
- [ ] 枝の先端 SHA を calcarc-e3 と calcarc-88（PR D をこの枝の上に積む）に伝える
