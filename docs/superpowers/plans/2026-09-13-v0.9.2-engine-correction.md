# 0.9.2 枝 A（演算子の押し直しと、数を捨てる入力）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 演算子の押し直しを「最初から正しい演算子を打った」と同じにし（F1）、画面の数を黙って捨てるキーを押せなくする（F5）。

**Architecture:** 判断はすべて `calcarc-core` のエンジンに置く。押し直しは `EngineState.replace_base`（演算子を積む直前の状態）へ戻して積み直す。押せないキーは純関数 `engine::refuses(state, key)` の 1 か所で決め、`reduce` は拒まれたキーで何も変えない。wasm の `Step` が押せないキーの一覧 `refused` を運び、web はそれを読んで盤面を押せなくし、打鍵の列にも積まない。

**Tech Stack:** Rust（`calcarc-core`・`calcarc-wasm`、proptest）、wasm-bindgen、TypeScript・React・vitest、Playwright。

**Spec:** `docs/superpowers/specs/2026-09-12-v0.9.2-calc-fixes-design.md`（枝 `docs/v0.9.2-calc-fixes`。作業木 `/tmp/claude-1000/-home-terapyon-dev-CalcArc/5ec1639a-7bb9-411e-a4ab-2c148331ac6a/scratchpad/wt-091` にある）。§2（F1）・§3（F5）・§9（裁定 1〜11）・§11（レビューの条件）を読む。

## Global Constraints

- **作業木は `/tmp/claude-1000/-home-terapyon-dev-CalcArc/5ec1639a-7bb9-411e-a4ab-2c148331ac6a/scratchpad/wt-A`**（枝 `fix/engine-correction`、起点 `eb94e72`）。`/home/terapyon/dev/CalcArc`（共有ワークツリー）と `/home/terapyon/dev/CalcArc-e2e` には触らない
- **`git push` と PR 作成をしない。`git add -A` を打たない**（ファイルを名指しする）。**コミット前に `git branch --show-current` が `fix/engine-correction`**
- **コミットの前段は `&&` だけでつなぐ。**`;` や `| grep` で終了コードを捨てない。順は `cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && <そのタスクのテスト>`
- **コミットメッセージはファイルに書いて `git commit -F <file>`**（heredoc を `&&` の鎖に混ぜない）。末尾の 2 行:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` と `Claude-Session: https://claude.ai/code/session_01BWysjEZm5GHRCYaC373V2S`
- **`calcarc-core` は panic しない**（ライブラリで `unwrap`/`expect` を使わない。テストでは可）
- **許容誤差をテストコードに書かない**
- **`crates/calcarc-core/tests/engine_table.rs` が仕様書。**挙動を変える前にその行を書き、赤を見てから実装する
- **番人は名指しする前に変異させる**（赤確認: 直しをコミットしてから壊し、赤を見て、**再編集で戻す**。`git checkout <file>` を使わない。戻したら `git diff` が空であることを見る）
- **重い段は回さない**: wasm のビルド（`pnpm wasm`・`wasm-pack build`・`wasm-pack test`）・E2E（`pnpm e2e`・`playwright test`）・vite build。Task 5〜7 の該当ステップは**統括（実装役）が回す**と書いてある。実行者はそこで止めて報告する
- **版数は上げない**
- **既存の engine_table・spell_table の行が、この計画に書いていない形で赤くなったら、直さずに止めて報告する**
- **設計書の裁定（§0・§9 の 1〜11）を変えない**

## ファイルの地図

| ファイル | 役割 | タスク |
|---|---|---|
| `crates/calcarc-core/src/engine/state.rs` | `ReplaceBase`・`EngineState.replace_base`・`on_hand`・`STATE_SCHEMA` 7 | 1・2 |
| `crates/calcarc-core/src/engine/mod.rs` | `push_binop` の押し直し、`reduce` の拒みと旗、`refuses`・`refused_keys`、`delete_one` の戻し | 1・2 |
| `crates/calcarc-core/src/engine/spell.rs` | 押し直しは最後の演算子だけを綴る | 3 |
| `crates/calcarc-core/tests/engine_table.rs` | 仕様の行 | 1・2 |
| `crates/calcarc-core/tests/engine_robustness.rs` | I4 の書き換え、I8（拒まれたキーは何も変えない）、走査が拒まれたキーの枝を降りない | 1・2 |
| `crates/calcarc-core/tests/spell_differential.rs` | 拒まれたキーを列に足さない、下限の測り直し | 2 |
| `crates/calcarc-core/tests/spell_table.rs` | 捨てる意味を固定した行の書き直し、押し直しの綴りの行 | 2・3 |
| `crates/calcarc-core/tests/engine_values.rs`（新規） | 全列挙の最終値の照合（別手順の評価器） | 4 |
| `crates/calcarc-wasm/src/lib.rs`・`tests/web.rs` | `Step.refused` | 5 |
| `web/src/calc/types.ts`・`web/src/ui/ScientificPanel.tsx`・`ScientificPanel.test.tsx` | 盤面の `off`、列に積まない | 5・6 |
| `web/tests/e2e/refused-keys.spec.ts`（新規） | 実ブラウザで押せない・キーボード・押し直し | 7 |

**設計書からの 1 点の詰め（実装の置き場）**: 設計書 §3.3 は押せないキーの一覧を `DisplayState.refused` に置くと書いたが、**この計画では wasm の `Step.refused` に置く**。`render` は網羅走査で 1 本あたり約 1,350 万回呼ばれ（`engine_robustness.rs` の註）、そこで毎回 46 キーを判定して一覧を作ると、既に 20 秒台の走査がさらに伸びる。`reduce` の戻り（`Step`）に載る点は calcarc-1e の条件 1(a)「reduce の戻り（Step/DisplayState）に載せる」のとおりで、判断の出どころは `refuses` の 1 か所のまま。

---

### Task 1: 押し直しを「最初から正しい演算子」と同じにする（F1）

**Files:**
- Modify: `crates/calcarc-core/src/engine/state.rs`（`STATE_SCHEMA`・`ReplaceBase`・`EngineState`・`initial`）
- Modify: `crates/calcarc-core/src/engine/mod.rs`（`push_binop`・`reduce`）
- Test: `crates/calcarc-core/tests/engine_table.rs`（新しい fn）
- Test: `crates/calcarc-core/tests/engine_robustness.rs`（I4 の書き換え）

**Interfaces:**
- Produces: `pub struct ReplaceBase { pub operands: Vec<Value>, pub operators: Vec<OpToken>, pub current: Value }`（`state.rs`）、
  `EngineState.replace_base: Option<ReplaceBase>`、`STATE_SCHEMA == 7`

- [ ] **Step 1: 仕様の行を先に書く**（`engine_table.rs` の `a_second_operator_replaces_the_first` の直後）

```rust
#[test]
fn a_corrected_operator_means_what_the_right_one_would_have() {
    // 0.9.2 設計書 §2(外部監査 F1、利用者の裁定): 訂正した列は、最初から正しい演算子を
    // 打った列と同じになる。いままでは先頭の演算子を差し替えるだけで、下にある演算子との
    // 優先順位も、積むときに畳んだ値も戻さなかった(`2 + 3 + × 4 =` が 20)。
    assert_eq!(main_of(&["8", "sub", "3", "mul", "add", "2", "eq"]), "7");
    assert_eq!(main_of(&["3", "sub", "3", "div", "sub", "3", "eq"]), "-3");
    assert_eq!(main_of(&["8", "div", "2", "pow", "mul", "2", "eq"]), "8");
    assert_eq!(main_of(&["2", "add", "3", "add", "mul", "4", "eq"]), "14");
    assert_eq!(main_of(&["2", "add", "3", "add", "mul"]), "3");
    assert_eq!(echo_of(&["2", "add", "3", "add", "mul"]), "2 + 3 ×");
    // 原則そのもの: 押し直した列と最初から正しい演算子の列は、表示のすべてが同じ。
    for (corrected, direct) in [
        (&["8", "sub", "3", "mul", "add"][..], &["8", "sub", "3", "add"][..]),
        (&["3", "sub", "3", "div", "sub"][..], &["3", "sub", "3", "sub"][..]),
        (&["8", "div", "2", "pow", "mul"][..], &["8", "div", "2", "mul"][..]),
        (&["2", "add", "3", "add", "mul"][..], &["2", "add", "3", "mul"][..]),
        (&["3", "mul", "4", "del", "add"][..], &["3", "add"][..]),
    ] {
        assert_eq!(run(corrected), run(direct), "{corrected:?} と {direct:?}");
    }
}
```

- [ ] **Step 2: 赤を見る**

Run: `cd <wt-A> && cargo test -p calcarc-core --test engine_table a_corrected_operator_means_what_the_right_one_would_have`
Expected: FAIL（`8 − 3 × + 2 =` が "3"）

- [ ] **Step 3: 状態に戻り先を足す**（`state.rs`）

`STATE_SCHEMA` の註の最後に 1 行足して 7 にする:

```rust
/// 7: 押し直しの戻り先 `replace_base` が入った(0.9.2 設計書 §2.2)。
pub const STATE_SCHEMA: u32 = 7;
```

`OpToken` の定義の直後に:

```rust
/// 二項演算子を積む直前の状態(0.9.2 設計書 §2.2)。
///
/// 押し直しはここへ戻してから、新しい演算子を**通常の経路で**積み直す。畳み・優先順位・
/// 結合方向はいまのコードがそのまま担う——押し直しのための計算の規則を別に持たない。
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct ReplaceBase {
    pub operands: Vec<Value>,
    pub operators: Vec<OpToken>,
    pub current: Value,
}
```

`EngineState` の `operator_pending` の直後に:

```rust
    /// 押し直しの戻り先(0.9.2 設計書 §2.2)。**`operator_pending` に従属する**
    /// (calcarc-1e の注記 A)——取るのは `push_binop` の通常の経路だけ、捨てるのは
    /// `reduce` の後判定で `operator_pending` が偽になる所だけ。DEL・表示トグル・
    /// 演算子の直後の `(` は旗を動かさないので、これも残る(`3 × ( DEL +` が `3 +` に
    /// なるのに要る)。
    #[serde(default)]
    pub replace_base: Option<ReplaceBase>,
```

`initial()` に `replace_base: None,` を足す。

- [ ] **Step 4: 押し直しを戻して積み直す形にする**（`mod.rs` の `push_binop`）

`use state::{...}` に `ReplaceBase` を足す。`push_binop` の冒頭の差し替えの分岐（`if state.operator_pending && ... { *last = OpToken::Op(op); return Ok(()); }`）と、その直後の `commit_entry(state)?; state.operands.push(state.current);` を次で置き換える。冒頭の註も書き直す:

```rust
fn push_binop(state: &mut EngineState, op: BinOp) -> CalcResult<()> {
    // 演算子を続けて押したときは訂正である(もう一度計算しろという意味ではない)。
    // **訂正した列は、最初から正しい演算子を打った列と同じになる**(0.9.2 設計書 §2、
    // 外部監査 F1)。先頭を差し替えるだけでは、下にある演算子との優先順位も、積むときに
    // 畳んだ値も戻らない(`8 − 3 × +` が `8 − (3 + …)`、`2 + 3 + ×` が `5 × …` になっていた)。
    // そこで積む直前の状態へ戻し、下の通常の経路で積み直す。
    //
    // 訂正してよいのは、演算子の直後から一歩も動いていないときだけである。入力中の
    // バッファがあれば `3 + 4 +` の 4 が消えるし、先頭が開き括弧なら `3 + ( +` はその
    // 括弧を上書きしてしまう(先頭が括弧のときは訂正ではなく、括弧の中の新しい演算になる)。
    if state.operator_pending
        && state.buffer.is_none()
        && matches!(state.operators.last(), Some(OpToken::Op(_)))
    {
        match state.replace_base.take() {
            Some(base) => {
                state.operands = base.operands;
                state.operators = base.operators;
                state.current = base.current;
            }
            None => {
                // 到達しない(戻り先は通常の経路で必ず取られ、`operator_pending` が偽に
                // なるまで残る)。万一のときは従来の差し替えに落ちる——panic しない。
                if let Some(last) = state.operators.last_mut() {
                    *last = OpToken::Op(op);
                }
                return Ok(());
            }
        }
    }
    commit_entry(state)?;
    state.replace_base = Some(ReplaceBase {
        operands: state.operands.clone(),
        operators: state.operators.clone(),
        current: state.current,
    });
    state.operands.push(state.current);
```

（`while matches!(...)` 以降は変えない。）

- [ ] **Step 5: 戻り先を捨てる所を 1 か所にする**（`mod.rs` の `reduce`）

`next.operator_pending = ...;` の文の直後に:

```rust
        // 押し直しの戻り先は「演算子の直後」のあいだだけ生きる(0.9.2 設計書 §2.2、
        // calcarc-1e の注記 A)。捨てる条件はここ 1 か所。
        if !next.operator_pending {
            next.replace_base = None;
        }
```

- [ ] **Step 6: 仕様の行が緑になり、既存の行が変わらないことを見る**

Run: `cargo test -p calcarc-core --test engine_table`
Expected: 全部 PASS（設計書 §2.4: F1 で変わる既存の行は 0）。**別の行が赤なら止めて報告**

- [ ] **Step 7: I4 を書き換える**（`engine_robustness.rs`）

`invariants::Step` に `anchor` の直後へ足す:

```rust
        /// `anchor` のキーを押す**直前**の状態。I4 が「その状態から、いま押した演算子を
        /// 直接押した姿」を作るのに使う(0.9.2 設計書 §2.5)。`anchor` と同じく走査側が積む。
        pub anchor_base: Option<&'a EngineState>,
```

`invariants` の `use` に `calcarc_core::reduce` を足す（`use calcarc_core::{DisplayState, EngineState, Key, reduce, render};`）。`operator_press_replaces` を註ごと次で置き換える:

```rust
    /// I4: 二項演算子を続けて押したら、**最初からその演算子を押したのと同じ**になる
    /// (0.9.2 設計書 §2.5、外部監査 F1)。
    ///
    /// 以前は「被演算数も現在値も変わらない」を要求していた——**内部構造を固定していた**
    /// (監査の指摘)。`2 + 3 + ×` の正しい姿は `2 + 3 ×` で、被演算数は 5 から 2・3 に戻る。
    ///
    /// 局所的に言い換える。`anchor` の演算子を押す直前の状態から、いま押した演算子を
    /// **直接**押した状態と、押し直した後の状態が、位置を決めるフィールドで一致すること。
    /// これで「数を二重に積まない」(被演算数が直接の経路と同じ)と「訂正後の意味が直接の
    /// 入力と一致する」の両方を見る。
    ///
    /// **前提は `anchor` から取る。** engine の `operator_pending` を読むと、旗が誤って
    /// クリアされるバグでは前提そのものが偽になり、この検査が黙って無効化される(実測:
    /// 二項演算子の腕を `true` から `false` に退行させると、旗を読む版は 7 本すべて緑のまま
    /// 通り、`anchor` を読む版は `3 + +` で落ちた)。
    ///
    /// 何かを消した DEL を挟んだ形(`3 × 4 DEL × 5 =`)は前提から外れる。そちらは
    /// engine_table.rs の `del_returns_to_the_pending_operator` が受け持つ。
    fn operator_press_replaces(step: &Step<'_>) -> Result<(), String> {
        let Some(op) = binop_of(step.key) else {
            return Ok(());
        };
        if !step.anchor.is_some_and(|k| binop_of(k).is_some()) {
            return Ok(());
        }
        let Some(base) = step.anchor_base else {
            return Ok(());
        };
        if step.before.error.is_some() {
            // 直前の演算子は畳み込みに失敗している。I5 の領域。
            return Ok(());
        }
        let (direct, _) = reduce(base, step.key);
        let after = step.after;
        if after.buffer != direct.buffer
            || after.current != direct.current
            || after.operands != direct.operands
            || after.operators != direct.operators
            || after.error != direct.error
            || after.replace_base != direct.replace_base
        {
            return Err(format!(
                "I4: {} after a pending operator differs from pressing it directly\n  corrected: {after:?}\n  direct:    {direct:?}",
                step.key.token()
            ));
        }
        // 押した演算子がスタックの先頭に載っていること。見ないと、押し直しを
        // 「何もせず return」に退行させても(直接の経路も同じ誤りでない限り)通る。
        if after.error.is_none() && after.operators.last() != Some(&OpToken::Op(op)) {
            return Err(format!(
                "I4: {} left {:?} on top of the operator stack",
                step.key.token(),
                after.operators.last()
            ));
        }
        Ok(())
    }
```

`walk` に `anchor_base: Option<&EngineState>` を `anchor` の直後の引数として足し、`Step` に `anchor_base` を渡し、再帰の前に

```rust
        let next_anchor_base = if invariants::keeps_the_position(key, state, &next) {
            anchor_base
        } else {
            Some(state)
        };
```

を置いて再帰へ渡す（`next_anchor` と同じ判定。1 回の呼び出しで両方を決めてよい）。`walk_from_the_start` は `None` を渡す。`Run` に `anchor_base: Option<EngineState>` を足し（`new` で `None`）、`press_without_recovery` で `Step` に `anchor_base: self.anchor_base.as_ref()` を渡し、`anchor` を更新する所で `self.anchor_base = Some(self.state.clone());` も更新する（`self.state = next;` より前）。**`invariants::Step {` を作っている所をすべて直す**（`grep -n 'invariants::Step {' crates/calcarc-core/tests/engine_robustness.rs`）。

- [ ] **Step 8: 門を通してコミットする**

```bash
cd <wt-A> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p calcarc-core --test engine_table && cargo test -p calcarc-core --test engine_robustness --release
```

走査の時間を、**直す前（`eb94e72`）と後で**それぞれ `--release` なしの `cargo test -p calcarc-core --test engine_robustness` で測り、報告に書く。
Expected: PASS。コミット（ファイルを名指し: `state.rs`・`mod.rs`・`engine_table.rs`・`engine_robustness.rs`）。件名例: `Make a corrected operator mean what the right one would have`

- [ ] **Step 9: 番人を名指しする前に変異させる**

コミットのあとで `push_binop` の `Some(base) => { ... }` の腕の中身を `if let Some(last) = state.operators.last_mut() { *last = OpToken::Op(op); } return Ok(());` に一時的に書き換え、

Run: `cargo test -p calcarc-core --test engine_robustness every_sequence_over_all_classes` と `cargo test -p calcarc-core --test engine_table a_corrected_operator`
Expected: **どちらも FAIL**（I4 は `3 − 3 ÷ −` のような列で落ちる。`ALL_CLASSES` に `Sub`・`Div`・`Pow` がある）。落ちた列を報告に写す。**再編集で戻し、`git diff` が空であることを見る**

---

### Task 2: 数を捨てるキーを押せなくする（F5）——エンジンと、綴りの走査

**Files:**
- Modify: `crates/calcarc-core/src/engine/state.rs`（`on_hand`、`STATE_SCHEMA` の註）
- Modify: `crates/calcarc-core/src/engine/mod.rs`（`refuses`・`refused_keys`・`reduce`・`delete_one`）
- Test: `crates/calcarc-core/tests/engine_table.rs`（新しい行 2 本、書き直す行 3 本）
- Test: `crates/calcarc-core/tests/engine_robustness.rs`（I8、走査の枝刈り）
- Test: `crates/calcarc-core/tests/spell_differential.rs`（拒まれたキーを列に足さない、下限）
- Test: `crates/calcarc-core/tests/spell_table.rs`（`:270-280` と `:283-300` の書き直し）

**Interfaces:**
- Consumes: Task 1 の `replace_base`
- Produces: `pub fn refuses(state: &EngineState, key: Key) -> bool`、`pub fn refused_keys(state: &EngineState) -> Vec<Key>`
  （どちらも `calcarc_core::engine` の直下）、`EngineState.on_hand: bool`

- [ ] **Step 1: 仕様の行を先に書く**（`engine_table.rs`。`run`・`main_of`・`echo_of` の直後に補助を 2 つ、末尾に行を 2 本）

```rust
/// `keys` を打ったあと、`key` が押せない(拒まれる)こと。**押しても状態も表示も変わらない**
/// (0.9.2 設計書 §3.4)。
fn refused_after(keys: &[&str], key: &str) {
    let mut state = EngineState::initial();
    for token in keys {
        let k = Key::from_token(token).unwrap_or_else(|| panic!("unknown key: {token}"));
        state = reduce(&state, k).0;
    }
    let k = Key::from_token(key).unwrap_or_else(|| panic!("unknown key: {key}"));
    assert!(
        calcarc_core::engine::refuses(&state, k),
        "{keys:?} のあと {key} は押せないはず"
    );
    let (after, shown) = reduce(&state, k);
    assert_eq!(after, state, "{keys:?} のあと {key}: 押せないキーが状態を変えた");
    assert_eq!(shown, render(&state));
}

/// `keys` を打ったあと、`key` が押せること。
fn accepted_after(keys: &[&str], key: &str) {
    let mut state = EngineState::initial();
    for token in keys {
        let k = Key::from_token(token).unwrap_or_else(|| panic!("unknown key: {token}"));
        state = reduce(&state, k).0;
    }
    let k = Key::from_token(key).unwrap_or_else(|| panic!("unknown key: {key}"));
    assert!(
        !calcarc_core::engine::refuses(&state, k),
        "{keys:?} のあと {key} は押せるはず"
    );
}
```

```rust
#[test]
fn a_key_that_would_drop_a_number_cannot_be_pressed() {
    // 0.9.2 設計書 §3.2(外部監査 F5、利用者の裁定「ボタンが押せない」)。
    // S1 打ちかけ: `(`・`π`・`e` はバッファを確定せずに捨てる。
    refused_after(&["2"], "lparen");
    refused_after(&["3"], "pi");
    refused_after(&["3"], "e");
    // S4 `)` の直後: 新しい数を始めるキーはどれも 7 を捨てる。
    for key in ["5", "zeros3", "dot", "exp", "j", "lparen", "pi", "e"] {
        refused_after(&["lparen", "3", "add", "4", "rparen"], key);
    }
    // S5 関数の答え・π・e の直後(§9 の 1)。`+/−` も答えに数える(§9 の 8)。
    refused_after(&["4", "sqrt"], "5");
    refused_after(&["pi"], "3");
    refused_after(&["e"], "lparen");
    refused_after(&["1", "2", "neg"], "3");
    refused_after(&["neg"], "5");
    refused_after(&["3", "add", "neg"], "5");
    refused_after(&["4", "sqrt"], "j");
    refused_after(&["lparen", "3", "rparen"], "j");
    // `j` が打ちかけの Exp・60 進の段を捨てる形(§9 の 11。実測は設計書に)。
    refused_after(&["exp"], "j");
    refused_after(&["exp", "2"], "j");
    refused_after(&["j", "exp"], "j");
    refused_after(&["1", "dms"], "j");
    // 押せないキーを押した列は、押さなかった列と同じ。
    assert_eq!(main_of(&["lparen", "3", "add", "4", "rparen", "5", "eq"]), "7");
    assert_eq!(main_of(&["4", "sqrt", "5", "eq"]), "2");
    assert_eq!(main_of(&["3", "add", "4", "sqrt", "5", "eq"]), "5");
    assert_eq!(main_of(&["1", "2", "neg", "3", "eq"]), "-12");
    assert_eq!(main_of(&["2", "lparen"]), "2");
    assert_eq!(echo_of(&["2", "lparen"]), "");
}

#[test]
fn keys_that_keep_the_number_stay_pressable() {
    // 二項演算子はどの状態でも押せる——F1 の訂正(演算子の直後の演算子)も押せる
    // (calcarc-1e の注記 A)。後置関数・`+/−`・`=`・`)`・DEL・AC・表示トグルも、
    // 手元の値に掛かるか何も捨てないので押せる(0.9.2 設計書 §3.2)。
    let states: [&[&str]; 5] = [&["2"], &["lparen", "3", "rparen"], &["4", "sqrt"], &["3", "add"], &["pi"]];
    for keys in states {
        for key in [
            "add", "sub", "mul", "div", "pow", "n_p_r", "n_c_r", "eq", "rparen", "del", "ac",
            "sqrt", "neg", "angle_toggle", "polar_toggle", "eng", "dms",
        ] {
            accepted_after(keys, key);
        }
    }
    // 指数の符号は打ちかけのまま(S1)。
    accepted_after(&["1", "dot", "5", "exp"], "neg");
    assert_eq!(main_of(&["1", "dot", "5", "exp", "neg", "3"]), "1.5e-3");
    // `j` が何も捨てない形(§9 の 11 の「捨てない」側)。
    for keys in [&["5"][..], &["j"], &["exp", "del"], &["j", "5"], &["1", "dms", "3"], &["5", "exp", "2"]] {
        accepted_after(keys, "j");
    }
    // `=` のあとは新しい計算(S6、§9 の 5)。数字も `(`・`π`・`e` も押せる。
    for key in ["5", "lparen", "pi", "e", "j", "dot"] {
        accepted_after(&["2", "add", "3", "eq"], key);
    }
    assert_eq!(main_of(&["2", "add", "3", "eq", "lparen", "4", "rparen", "eq"]), "4");
    // エラー中は押せなくしない(AC 以外が既に何もしない。S7 はいまの見た目のまま)。
    accepted_after(&["1", "div", "0", "eq"], "5");
}
```

**`eng` と `dms` のトークンは `key.rs` の `from_token` で確かめる**（違えば正しい綴りに直す）。

- [ ] **Step 2: 赤を見る**

Run: `cargo test -p calcarc-core --test engine_table a_key_that_would_drop keys_that_keep`
Expected: コンパイルエラー（`refuses` が無い）

- [ ] **Step 3: 手元の値の旗を足す**（`state.rs`）

`STATE_SCHEMA` の 7 の行を `/// 7: 押し直しの戻り先 `replace_base`(0.9.2 設計書 §2.2)と、手元の値の旗 `on_hand`(§3.3)が入った。` にする。`EngineState` の `replace_base` の直後に:

```rust
    /// **手元の値があるか**(0.9.2 設計書 §3.2・§3.3)。`)`・後置関数・`+/−`(確定値に
    /// 掛けたとき)・`π`・`e` の直後に真。まだどの演算子の被演算数にもなっていない、画面の
    /// 値である。これがあるあいだ、新しい数を始めるキーは押せない(`refuses`)。
    /// **`operator_pending` と同じく、DEL と表示トグルでは動かさない。**
    #[serde(default)]
    pub on_hand: bool,
```

`initial()` に `on_hand: false,` を足す。

- [ ] **Step 4: `refuses` と `refused_keys` を置く**（`mod.rs`。`reduce` の直前）

```rust
/// このキーを押すと、画面の数(打ちかけの数・手元の値)を**黙って捨てる**か
/// (0.9.2 設計書 §3.2、外部監査 F5。§9 の 1・8・11)。
///
/// **真なら `reduce` は何も変えない**——押せないキーは押されなかったのと同じ。盤面と
/// キーボードはこの答え(wasm の `Step.refused`)を読むだけで、規則を持たない。
/// **二項演算子はどの状態でも拒まない**(F1 の訂正が押せる。calcarc-1e の注記 A)。
/// 拒む集合は、数字・`000`・`.`・`Exp`・`j`・`(`・`π`・`e` の外に出ない。
pub fn refuses(state: &EngineState, key: Key) -> bool {
    if state.error.is_some() {
        // エラー中は AC 以外が既に何もしない(`reduce`)。押せなくはしない(S7)。
        return false;
    }
    if let Some(buffer) = &state.buffer {
        // S1 打ちかけ。`(`・`π`・`e` はバッファを確定せずに捨てる(`open_paren`・`Key::Pi`・
        // `Key::E`)。`j` は仮数の数字が無く、指数か 60 進の段があるときだけ、新しい虚数入力で
        // 上書きして捨てる(§9 の 11。`Key::J` の「数字が無ければ新しい虚数入力」)。
        return match key {
            Key::LParen | Key::Pi | Key::E => true,
            Key::J => {
                !buffer.has_digits()
                    && (buffer.exponent.is_some() || !buffer.sexagesimal.is_empty())
            }
            _ => false,
        };
    }
    // S4・S5 手元の値。新しい数を始めるキーはどれも手元の値を捨てる。
    state.on_hand
        && matches!(
            key,
            Key::Digit(_)
                | Key::Zeros3
                | Key::Dot
                | Key::Exp
                | Key::J
                | Key::LParen
                | Key::Pi
                | Key::E
        )
}

/// `refuses` が真になるキーの一覧(`Key::ALL` の順)。wasm の `Step.refused` がこれを運ぶ。
pub fn refused_keys(state: &EngineState) -> Vec<Key> {
    Key::ALL
        .iter()
        .copied()
        .filter(|&key| refuses(state, key))
        .collect()
}
```

- [ ] **Step 5: `reduce` で拒み、旗を更新する**（`mod.rs`）

`let mut next = ...;` の直後に:

```rust
    // 押せないキーは押されなかったのと同じ(0.9.2 設計書 §3.3)。**表示の一時状態(60 進・
    // ENG)も戻さない**ので、下の解除より前で返す。
    if refuses(&next, key) {
        let shown = display::render(&next);
        return (next, shown);
    }
```

`let was_pending = next.operator_pending;` の直後に `let was_on_hand = next.on_hand;`。`next.operator_pending = ...;` の後（Task 1 の `replace_base` の行の前）に:

```rust
        // 手元の値があるか(0.9.2 設計書 §3.3)。`operator_pending` と同じく、場所を
        // 動かさないキー(DEL・表示トグル)では引き継ぐ。
        next.on_hand = next.error.is_none()
            && match key {
                Key::RParen
                | Key::Pi
                | Key::E
                | Key::Sqrt
                | Key::Sqr
                | Key::Sin
                | Key::Cos
                | Key::Tan
                | Key::Ln
                | Key::Log10
                | Key::ExpE
                | Key::Recip
                | Key::Asin
                | Key::Acos
                | Key::Atan
                | Key::NFact => true,
                // 確定値に掛かったならバッファは消えている。指数の符号ならバッファが残り、
                // 打ちかけのまま(§3.2)。
                Key::Neg => next.buffer.is_none(),
                Key::Del | Key::AngleToggle | Key::PolarToggle | Key::EngToggle | Key::Dms => {
                    was_on_hand
                }
                Key::Digit(_)
                | Key::Zeros3
                | Key::Dot
                | Key::Exp
                | Key::J
                | Key::LParen
                | Key::Add
                | Key::Sub
                | Key::Mul
                | Key::Div
                | Key::Pow
                | Key::Npr
                | Key::Ncr
                | Key::Eq
                | Key::Ac => false,
            };
```

- [ ] **Step 6: DEL が開き括弧を消したら、`(` の前へ戻す**（`mod.rs` の `delete_one`。§9 の 2・10 と、§9 のあとの詰め）

`delete_one` の末尾の `if matches!(state.operators.last(), Some(OpToken::OpenParen)) { state.operators.pop(); }` を次で置き換え、関数の註の「どれも無ければ何もしない」の後に「開き括弧を消したときの戻し方は下の註」と足す:

```rust
    if matches!(state.operators.last(), Some(OpToken::OpenParen)) {
        state.operators.pop();
        // `(` を DEL で消すのは訂正であり、`3 × ( DEL =` は `3 × =` と同じ 9 になる
        // (0.9.2 設計書 §9 の 2、F1 の原則)。`(` が 0 にした `current` を、戻った先の
        // 演算子の被演算数へ戻す。
        // - **`(` のあとに手元の値があれば戻さない**(`3 + ( 4 √ DEL =` は 5 のまま)。
        //   戻すと √ の答えを黙って捨てる(§9 のあとの詰め)。
        // - **先頭が演算子にならないときは戻さない**(`2 + 3 = ( DEL =` は 0 のまま。
        //   `=` のあとの `(` は新しい計算の始まり。§9 の 10)。
        if !state.on_hand
            && matches!(state.operators.last(), Some(OpToken::Op(_)))
            && let Some(&operand) = state.operands.last()
        {
            state.current = operand;
        }
    }
```

- [ ] **Step 7: 意味の変わる既存の行を書き直す**（`engine_table.rs`。値の変わる行は設計書 §3.6 と §9 の 2 のとおり）

`del_removes_an_unclosed_paren` を:

```rust
#[test]
fn del_removes_an_unclosed_paren() {
    // 閉じていない括弧を DEL で片付けられる。打ちかけの数のあとの `(` は押せない
    // (0.9.2 設計書 §3.2)ので、片付けるのは演算子の直後に開いた括弧である。
    assert_eq!(main_of(&["3", "add", "lparen", "del"]), "3");
    assert_eq!(run(&["3", "add", "lparen", "del"]).pending_depth, 0);

    // 押し間違いが綺麗に戻る。
    assert_eq!(main_of(&["3", "add", "lparen", "del", "4", "eq"]), "7");
}
```

`del_does_not_restore_the_value_a_paren_discarded` を名前ごと:

```rust
#[test]
fn del_on_a_fresh_paren_returns_to_the_operator_before_it() {
    // `(` を DEL で消すのは訂正であり、`3 × =`・`3 + =` と同じになる(0.9.2 設計書 §9 の 2、
    // F1 の原則「訂正した列は、最初から正しく打った列と同じ」)。
    assert_eq!(main_of(&["3", "mul", "lparen", "del", "eq"]), "9");
    assert_eq!(main_of(&["3", "add", "lparen", "del", "eq"]), "6");
    // 戻すのは `(` が 0 にした値だけ。`(` のあとに手元の値があれば戻さない——戻すと
    // √ の答えを黙って捨てる(§9 のあとの詰め)。
    assert_eq!(main_of(&["3", "add", "lparen", "4", "sqrt", "del", "eq"]), "5");
    // `=` のあとの `(` は新しい計算の始まり。DEL で消しても答えは戻らない(§9 の 10)。
    assert_eq!(main_of(&["2", "add", "3", "eq", "lparen", "del", "eq"]), "0");
}
```

`del_after_a_closing_paren_does_not_fake_a_pending_operator` の最初の註の 1〜2 行目（「`)` の直後は「演算子の直後」ではない。入力した 5 を DEL で消しても戻るのはそこであって、…」）を「`)` の直後は「演算子の直後」ではない。**数字は押せない**(0.9.2 設計書 §3.2 の S4)ので `5` は何も起こさず、DEL も消すものが無い。次の `+` は差し替えではなく通常の演算になる。」に書き直す（**値 13・12 は変えない**）。

- [ ] **Step 8: I8 を足す**（`engine_robustness.rs` の `invariants`）

`check` の末尾（`Ok(())` の前）に `a_refused_key_changes_nothing(step)?;` を足し、関数を置く:

```rust
    /// I8: 押せないキーは状態を変えない(0.9.2 設計書 §3.3)。**押されなかったのと同じ**
    /// ——表示の一時状態(60 進・ENG)も含めて、状態をまるごと比べる。
    fn a_refused_key_changes_nothing(step: &Step<'_>) -> Result<(), String> {
        if !calcarc_core::engine::refuses(step.before, step.key) {
            return Ok(());
        }
        if step.after != step.before {
            return Err(format!(
                "I8: refused {} changed the state\n  before: {:?}\n  after:  {:?}",
                step.key.token(),
                step.before,
                step.after
            ));
        }
        Ok(())
    }
```

`walk`（網羅）で、不変条件を検査した**あと**、再帰の前に:

```rust
        // 拒まれたキーの先は、拒む前の状態と同じなので辿らない(I8 は上で検査した)。
        // web は拒まれたキーを打鍵の列に積まない(0.9.2 設計書 §3.3 の条件 1)。
        if calcarc_core::engine::refuses(state, key) {
            trail.pop();
            continue;
        }
```

（乱択の `Run` は拒まれたキーも押したまま——I8 を乱択でも見るため。）

- [ ] **Step 9: 綴りの走査の赤を見る（条件 1 の赤）**

Run: `cargo test -p calcarc-core --test spell_differential every_sequence_up_to_five_keys`
Expected: **FAIL**（`入力中なのに綴りの最後の語が表示と違う: 表示 "…" / 綴り "… ("` のように、拒まれた `(`・`π`・`e` を列に足した形）。落ちた列を報告に写す——これが calcarc-1e の条件 1 で読みから言った赤の実測になる

- [ ] **Step 10: 綴りの走査が拒まれたキーを列に足さないようにする**（`spell_differential.rs`）

`walk` の `for &key in &CLASSES {` の直後に:

```rust
        // 拒まれたキーは web の打鍵の列に積まれない(ScientificPanel、0.9.2 設計書 §3.3 の
        // 条件 1)。列に足さず、その枝も降りない。判断は engine の `refuses`(写しではない)。
        if calcarc_core::engine::refuses(state, key) {
            continue;
        }
```

乱択の `random_entry_heavy_sequences_spell_the_entry_as_the_engine_shows_it` も、列を組む所で同じく拒まれたキーを列にも `reduce` にも渡さない形にする（関数を読んで、キーを積む箇所の直前に同じ判定を置く）。

Run: `cargo test -p calcarc-core --test spell_differential`
Expected: 網羅は**下限の assert で落ちるか通る**。比べた回数（`compared`）を印字させて測り、`:116-120` の下限と `:101-105` の註の実測値・日付（2026-09-13、理由「拒まれたキーの枝を降りなくなった」）を書き直す。**下限は実測値そのもの**にする

- [ ] **Step 11: 捨てる意味を固定していた綴りの行を書き直す**（`spell_table.rs`）

ファイル冒頭の `use` に `calcarc_core::engine::refuses` と、`EngineState`・`Key`・`reduce` が無ければ足す。補助を足す:

```rust
/// engine が受け付けたキーだけの列。**web は拒まれたキーを打鍵の列に積まない**
/// (ScientificPanel、0.9.2 設計書 §3.3 の条件 1)ので、綴りに渡るのはこの列である。
fn accepted(tokens: &[&str]) -> Vec<Key> {
    let mut state = EngineState::initial();
    let mut keys = Vec::new();
    for token in tokens {
        let key = Key::from_token(token).unwrap_or_else(|| panic!("unknown key: {token}"));
        if refuses(&state, key) {
            continue;
        }
        state = reduce(&state, key).0;
        keys.push(key);
    }
    keys
}
```

`lparen_pi_and_e_discard_the_entry_being_typed_instead_of_committing_it`（`:269-280`）を名前ごと:

```rust
#[test]
fn a_refused_key_never_reaches_the_spelling() {
    // 打ちかけの数のあとの `(`・`π`・`e` は数を捨てるので押せない(0.9.2 設計書 §3.2)。
    // 拒まれたキーは打鍵の列に来ないので、綴りにも出ない。
    assert_eq!(spell(&accepted(&["3", "lparen", "del"])), "");
    assert_eq!(spell(&accepted(&["3", "pi"])), "3");
    assert_eq!(spell(&accepted(&["3", "e"])), "3");
    assert_eq!(
        spell(&accepted(&["lparen", "3", "add", "4", "rparen", "5"])),
        "( 3 + 4 )"
    );
}
```

（`spell` の import 名はファイルの既存の `spell_of` の中身に合わせる。）
`del_after_lparen_cannot_recover_the_zero_it_silently_substituted`（`:282-300`）は名前を `del_after_lparen_returns_to_the_operator` に変え、註を「`(` を DEL で消すと、engine は `(` の前へ戻る(0.9.2 設計書 §9 の 2。engine_table の `del_on_a_fresh_paren_returns_to_the_operator_before_it`)。綴りは「3 ×」「3 +」のままで、答え 9・6 をそのまま説明する——以前ここにあった「既知の穴」は閉じた。」に書き直す。**assert の 2 行は変えない**。

- [ ] **Step 12: 門を通してコミットする**

```bash
cd <wt-A> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p calcarc-core
```

Expected: 全部 PASS。**計画に書いていない行が赤なら止めて報告**。走査 3 本（`engine_robustness` の網羅 2 本・`spell_differential`）の時間を Task 1 の値と並べて報告する。コミット（名指し: `state.rs`・`mod.rs`・`engine_table.rs`・`engine_robustness.rs`・`spell_differential.rs`・`spell_table.rs`）。件名例: `Refuse the keys that would silently drop the number on screen`

- [ ] **Step 13: 番人を名指しする前に変異させる**（コミットのあと、1 つずつ・再編集で戻す）

1. `reduce` の `if refuses(&next, key) { ... return ...; }` を消す → `engine_table` の `a_key_that_would_drop_a_number_cannot_be_pressed` と `engine_robustness`（I8）が FAIL
2. `refuses` の `Key::J => { ... }` を `Key::J => false` にする → `a_key_that_would_drop…`（`exp j`）が FAIL
3. `delete_one` の `!state.on_hand &&` を消す → `del_on_a_fresh_paren…`（`3 + ( 4 √ DEL =` が 6）が FAIL
4. `spell_differential` の `if refuses(...) { continue; }` を消す → 網羅が FAIL（Step 9 と同じ）

それぞれ赤を見たら戻し、`git diff` が空であることを見る。

---

### Task 3: 押し直した演算子は最後の 1 つだけを綴る（§9 の 9）

**Files:**
- Modify: `crates/calcarc-core/src/engine/spell.rs`
- Test: `crates/calcarc-core/tests/spell_table.rs`

- [ ] **Step 1: 綴りの行を先に書く**（`spell_table.rs` の末尾）

```rust
#[test]
fn a_corrected_operator_is_spelled_once() {
    // 押し直しは訂正であって計算ではない(engine_table の `a_second_operator_replaces_the_first`)。
    // 綴りも最後の 1 つだけを残す(0.9.2 設計書 §9 の 9、利用者の裁定)。以前は
    // 同じ演算子も違う演算子も両方並べていた(実測: `3 + + 4` → 「3 + + 4」)。
    assert_eq!(spell_of(&["3", "add", "add", "4"]), "3 + 4");
    assert_eq!(spell_of(&["3", "mul", "mul", "4"]), "3 × 4");
    assert_eq!(spell_of(&["3", "add", "mul", "4"]), "3 × 4");
    assert_eq!(spell_of(&["3", "add", "sub", "mul", "4"]), "3 × 4");
    assert_eq!(spell_of(&["2", "add", "3", "add", "mul", "4"]), "2 + 3 × 4");
    // DEL で演算子の直後に戻ってからの押し直しも訂正(engine と同じ)。
    assert_eq!(spell_of(&["3", "mul", "4", "del", "add", "5"]), "3 + 5");
    assert_eq!(spell_of(&["3", "mul", "lparen", "del", "add", "4"]), "3 + 4");
    // 開き括弧の直後の演算子は訂正ではない(engine も差し替えない)。
    assert_eq!(spell_of(&["3", "mul", "lparen", "add", "4"]), "3 × ( + 4");
}
```

- [ ] **Step 2: 赤を見る**

Run: `cargo test -p calcarc-core --test spell_table a_corrected_operator_is_spelled_once`
Expected: FAIL（「3 + + 4」）

- [ ] **Step 3: 綴りを直す**（`spell.rs`）

`commit_glyph` の直後に:

```rust
/// 二項演算子の字面。**押し直しの判定にだけ使う**(`spell` の `_` の腕)。
const BINARY_GLYPHS: [&str; 7] = ["+", "−", "×", "÷", "xʸ", "nPr", "nCr"];

fn is_binary(key: Key) -> bool {
    matches!(
        key,
        Key::Add | Key::Sub | Key::Mul | Key::Div | Key::Pow | Key::Npr | Key::Ncr
    )
}
```

`spell` の `_` の腕を:

```rust
            _ => {
                // 二項演算子・`)`・後置関数。
                commit_into(&mut current, &mut parts);
                // 演算子の直後の演算子は訂正(engine の `push_binop`)。綴りも最後の 1 つ
                // だけを残す(0.9.2 設計書 §9 の 9)。バッファがあれば上の行が数を流し込む
                // ので、ここで末尾が演算子なのは「演算子の直後から動いていない」ときだけ。
                if is_binary(key)
                    && parts
                        .last()
                        .is_some_and(|part| BINARY_GLYPHS.contains(&part.as_str()))
                {
                    parts.pop();
                }
                if let Some(text) = commit_glyph(key) {
                    parts.push(text.to_string());
                }
            }
```

モジュール冒頭の註の「**打った順に、キーのラベルをそのまま並べる。**」の直後に「**ただし演算子の直後の演算子は訂正なので、最後の 1 つだけを残す**(0.9.2 設計書 §9 の 9。engine の押し直しと同じ意味)。」を足す。

- [ ] **Step 4: 緑を見て、既存の行を確かめる**

Run: `cargo test -p calcarc-core --test spell_table && cargo test -p calcarc-core --test spell_differential`
Expected: PASS。**既存の行で押し直しを 2 つ並べて固定していたものが赤なら、それは §9 の 9 の意図した変更**——行を「最後の 1 つ」に直し、どの行を直したかを報告に書く。それ以外の赤は止めて報告

- [ ] **Step 5: 門を通してコミットし、変異させる**

```bash
cd <wt-A> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p calcarc-core
```

コミット（名指し: `spell.rs`・`spell_table.rs`）。件名例: `Spell a corrected operator once, as the engine means it`。コミットのあと `parts.pop();` を一時的に消して `a_corrected_operator_is_spelled_once` が FAIL することを見て、再編集で戻す。

---

### Task 4: 全列挙の最終値を、別手順の評価器と照合する（§2.6）

**Files:**
- Create: `crates/calcarc-core/tests/engine_values.rs`

**Interfaces:**
- Consumes: `calcarc_core::{reduce, EngineState, Key, CalcError}`、`calcarc_core::numeric::format::format_real`（書式だけを借りる。計算は借りない）

- [ ] **Step 1: 評価器と網を書く**（新規ファイル。**入力の決まりは各腕の註で engine_table の fn 名を名指しする**——書く前に `grep -n 'fn ' crates/calcarc-core/tests/engine_table.rs` で名前を確かめ、下の註の名前が違えば正しい名前に直す）

```rust
//! 全列挙の最終値の照合(0.9.2 設計書 §2.6)。
//!
//! 独立: 別手順。エンジンは演算子スタックで打鍵のたびに畳むが、ここはキー列を
//! **式のトークン列**に正規化し、`=` で**再帰下降の構文解析により有理数のまま**評価する。
//! 入力の決まり(押し直し・DEL・押せないキー・閉じ忘れ・`x op =`)は `Typed::press` の
//! 各腕が engine_table.rs の行を名指しして持つ——**写しの出どころを 1 か所にする**。
//!
//! **押せないキーも列に残す**(calcarc-1e の確認、設計書 §2.6)。エンジンには全キーを渡し、
//! ここは自前の規則で押せないキーを無視する。`refuses` は呼ばない——呼べば、エンジンが
//! 拒みすぎても拒み足りなくても、ここが同じ誤りに従って黙る。

use calcarc_core::numeric::format::format_real;
use calcarc_core::{CalcError, EngineState, Key, reduce};

/// 既約の有理数。分母は正。
#[derive(Clone, Copy, Debug, PartialEq)]
struct Q {
    n: i128,
    d: i128,
}

fn gcd(a: i128, b: i128) -> i128 {
    if b == 0 { a.abs() } else { gcd(b, a % b) }
}

impl Q {
    fn int(n: i128) -> Q {
        Q { n, d: 1 }
    }
    fn new(n: i128, d: i128) -> Q {
        let g = gcd(n, d).max(1);
        let s = if d < 0 { -1 } else { 1 };
        Q { n: s * n / g, d: s * d / g }
    }
    fn add(self, o: Q) -> Q {
        Q::new(self.n * o.d + o.n * self.d, self.d * o.d)
    }
    fn sub(self, o: Q) -> Q {
        Q::new(self.n * o.d - o.n * self.d, self.d * o.d)
    }
    fn mul(self, o: Q) -> Q {
        Q::new(self.n * o.n, self.d * o.d)
    }
    fn div(self, o: Q) -> Result<Q, CalcError> {
        if o.n == 0 {
            Err(CalcError::DivisionByZero)
        } else {
            Ok(Q::new(self.n * o.d, self.d * o.n))
        }
    }
    fn to_f64(self) -> f64 {
        self.n as f64 / self.d as f64
    }
}

#[derive(Clone, Debug, PartialEq)]
enum Tok {
    Num(String),
    Val(Q),
    Op(char),
    Open,
    Close,
}

fn prec(op: char) -> u8 {
    if op == '+' || op == '−' { 1 } else { 2 }
}

fn glyph(key: Key) -> char {
    match key {
        Key::Add => '+',
        Key::Sub => '−',
        Key::Mul => '×',
        _ => '÷',
    }
}

/// 閉じていない `(` の数。
fn depth(toks: &[Tok]) -> usize {
    toks.iter().fold(0usize, |d, t| match t {
        Tok::Open => d + 1,
        Tok::Close => d.saturating_sub(1),
        _ => d,
    })
}

/// `x op =`・`x op )` の右辺(engine_table: `3 + =` が 6、echo の `2 × 3 +` が「6 +」)。
/// **演算子を押した瞬間に画面に出る値**＝押した演算子より優先順位が低くない演算だけで
/// 結ばれた、直前の項の値。いちばん内側の開いた括弧より前へは戻らない。
fn implied(prefix: &[Tok], p: u8) -> Result<Q, CalcError> {
    let mut depth = 0usize;
    let mut start = 0;
    for (i, t) in prefix.iter().enumerate().rev() {
        match t {
            Tok::Close => depth += 1,
            Tok::Open if depth > 0 => depth -= 1,
            Tok::Open => {
                start = i + 1;
                break;
            }
            Tok::Op(op) if depth == 0 && prec(*op) < p => {
                start = i + 1;
                break;
            }
            _ => {}
        }
    }
    eval(&prefix[start..])
}

fn eval(toks: &[Tok]) -> Result<Q, CalcError> {
    let mut at = 0;
    let v = expr(toks, &mut at)?;
    if at == toks.len() { Ok(v) } else { Err(CalcError::SyntaxError) }
}

fn expr(t: &[Tok], at: &mut usize) -> Result<Q, CalcError> {
    let mut v = term(t, at)?;
    while let Some(Tok::Op(op @ ('+' | '−'))) = t.get(*at) {
        *at += 1;
        let r = term(t, at)?;
        v = if *op == '+' { v.add(r) } else { v.sub(r) };
    }
    Ok(v)
}

fn term(t: &[Tok], at: &mut usize) -> Result<Q, CalcError> {
    let mut v = factor(t, at)?;
    while let Some(Tok::Op(op @ ('×' | '÷'))) = t.get(*at) {
        *at += 1;
        let r = factor(t, at)?;
        v = if *op == '×' { v.mul(r) } else { v.div(r)? };
    }
    Ok(v)
}

fn factor(t: &[Tok], at: &mut usize) -> Result<Q, CalcError> {
    match t.get(*at) {
        Some(Tok::Num(s)) => {
            *at += 1;
            s.parse::<i128>().map(Q::int).map_err(|_| CalcError::SyntaxError)
        }
        Some(Tok::Val(q)) => {
            *at += 1;
            Ok(*q)
        }
        Some(Tok::Open) => {
            *at += 1;
            let v = expr(t, at)?;
            match t.get(*at) {
                Some(Tok::Close) => {
                    *at += 1;
                    Ok(v)
                }
                _ => Err(CalcError::SyntaxError),
            }
        }
        _ => Err(CalcError::SyntaxError),
    }
}

/// 打った列を式として持つ。`toks` が空のとき画面に出ている値が `base`
/// (始めは 0、`=` のあとは答え)。
#[derive(Clone, Debug)]
struct Typed {
    toks: Vec<Tok>,
    base: Q,
    error: Option<CalcError>,
}

impl Typed {
    fn new() -> Typed {
        Typed { toks: Vec::new(), base: Q::int(0), error: None }
    }

    fn press(&mut self, key: Key) {
        // エラー中は AC 以外を受け付けない(engine_table: エラーの行)。AC は網に無い。
        if self.error.is_some() {
            return;
        }
        match key {
            Key::Digit(d) => {
                // `)` の直後は押せない(F5 の S4。engine_table: `a_key_that_would_drop_a_number_cannot_be_pressed`)。
                if matches!(self.toks.last(), Some(Tok::Close)) {
                    return;
                }
                let c = char::from(b'0' + d);
                match self.toks.last_mut() {
                    // 字数の上限は 12(`MAX_ENTRY_LEN`)。網の長さでは届かないが写しておく。
                    Some(Tok::Num(s)) if s.len() < 12 => s.push(c),
                    Some(Tok::Num(_)) => {}
                    _ => self.toks.push(Tok::Num(c.to_string())),
                }
            }
            Key::Add | Key::Sub | Key::Mul | Key::Div => {
                match self.toks.last() {
                    // 押し直しは訂正(engine_table: `a_corrected_operator_means_what_the_right_one_would_have`)。
                    Some(Tok::Op(_)) => {
                        self.toks.pop();
                    }
                    // 何も打っていなければ画面の値が左辺(`=` のあとは答え。engine_table の連鎖の行)。
                    None => self.toks.push(Tok::Val(self.base)),
                    // `(` の直後は 0 が左辺(engine_table: echo の `( 0 +` の形)。
                    Some(Tok::Open) => self.toks.push(Tok::Val(Q::int(0))),
                    _ => {}
                }
                self.toks.push(Tok::Op(glyph(key)));
            }
            Key::LParen => {
                // 打ちかけの数・`)` の直後は押せない(F5 の S1・S4)。
                if matches!(self.toks.last(), Some(Tok::Num(_) | Tok::Close)) {
                    return;
                }
                // 何も打っていないときの `(` は新しい計算の始まり。DEL で消しても
                // 前の答えは戻らない(engine_table: `2 + 3 = ( DEL =` → 0、§9 の 10)。
                if self.toks.is_empty() {
                    self.base = Q::int(0);
                }
                self.toks.push(Tok::Open);
            }
            Key::RParen => {
                // 開いていない `)` は SyntaxError(engine_table の `2 ) =` の行)。
                if depth(&self.toks) == 0 {
                    self.error = Some(CalcError::SyntaxError);
                    return;
                }
                if let Err(e) = self.fill_the_right_operand() {
                    self.error = Some(e);
                    return;
                }
                self.toks.push(Tok::Close);
            }
            Key::Eq => {
                // 何も打っていなければ画面の値のまま(engine_table: `2 + 3 = =` → 5)。
                if self.toks.is_empty() {
                    return;
                }
                if let Err(e) = self.fill_the_right_operand() {
                    self.error = Some(e);
                    return;
                }
                // `=` は閉じ忘れを補う(engine_table: `(2 + 3 =` → 5)。
                for _ in 0..depth(&self.toks) {
                    self.toks.push(Tok::Close);
                }
                match eval(&self.toks) {
                    Ok(v) => {
                        self.base = v;
                        self.toks.clear();
                    }
                    Err(e) => self.error = Some(e),
                }
            }
            Key::Del => {
                if let Some(Tok::Num(s)) = self.toks.last_mut() {
                    // 数字を 1 つ消す(engine_table の DEL の行)。
                    s.pop();
                    if s.is_empty() {
                        self.toks.pop();
                    }
                } else if let Some(i) = self.top_open() {
                    // 保留のいちばん上が開き括弧なら、それを消す(engine の `delete_one` は
                    // 演算子スタックの先頭が括弧のときだけ消す)。**括弧の後ろに閉じた組が
                    // あっても消える**——`( ( 3 ) DEL )` は外の括弧が消え、最後の `)` が
                    // SyntaxError になる。括弧が末尾なら「演算子の直後に戻る」
                    // (§9 の 2。`3 × ( DEL =` は `3 × =`)。
                    self.toks.remove(i);
                }
                // 演算子・答えは消さない(DEL は undo ではない)。
            }
            _ => unreachable!("網に無いキー: {key:?}"),
        }
    }

    /// 保留のいちばん上——末尾から、閉じた組と数を飛ばして最初に当たる演算子か開き括弧——が
    /// 開き括弧なら、その位置。engine の演算子スタックの先頭に当たる。
    fn top_open(&self) -> Option<usize> {
        let mut depth = 0usize;
        for (i, t) in self.toks.iter().enumerate().rev() {
            match t {
                Tok::Close => depth += 1,
                Tok::Open if depth > 0 => depth -= 1,
                Tok::Open => return Some(i),
                Tok::Op(_) if depth == 0 => return None,
                _ => {}
            }
        }
        None
    }

    /// 右辺が無いまま `)`・`=` に来たときの補い。`x op` は `x op x`、`(` は `( 0`。
    fn fill_the_right_operand(&mut self) -> Result<(), CalcError> {
        match self.toks.last() {
            Some(Tok::Op(op)) => {
                let p = prec(*op);
                let v = implied(&self.toks[..self.toks.len() - 1], p)?;
                self.toks.push(Tok::Val(v));
            }
            Some(Tok::Open) => self.toks.push(Tok::Val(Q::int(0))),
            _ => {}
        }
        Ok(())
    }
}

const NET: [Key; 10] = [
    Key::Digit(3),
    Key::Digit(4),
    Key::Add,
    Key::Sub,
    Key::Mul,
    Key::Div,
    Key::LParen,
    Key::RParen,
    Key::Eq,
    Key::Del,
];

/// 列の長さの上限(最後の `=` を含む)。**まず 6。時間を測り、`cargo test -p calcarc-core`
/// 全体の予算を大きく超えるなら 5 に下げて理由を註に書く。**
const LENGTH: usize = 6;

/// `trail` を打った状態から `=` で閉じた値を比べ、1 キー足して降りる。
fn walk(state: &EngineState, typed: &Typed, trail: &mut Vec<Key>, compared: &mut usize) {
    let (_, shown) = reduce(state, Key::Eq);
    let mut closed = typed.clone();
    closed.press(Key::Eq);
    let tokens: Vec<&str> = trail.iter().map(|k| k.token()).collect();
    match (shown.error, closed.error) {
        (Some(engine), Some(oracle)) => {
            assert_eq!(engine, oracle, "エラーの種類が違う: {tokens:?} + eq");
        }
        (None, None) => {
            assert_eq!(
                shown.main,
                format_real(closed.base.to_f64()),
                "最終値が違う: {tokens:?} + eq(評価器の式 {:?})",
                typed.toks
            );
        }
        (engine, oracle) => panic!(
            "片方だけエラー: engine {engine:?} / 評価器 {oracle:?}: {tokens:?} + eq(式 {:?})",
            typed.toks
        ),
    }
    *compared += 1;
    if trail.len() + 1 >= LENGTH || typed.error.is_some() {
        return;
    }
    for &key in &NET {
        let (next, _) = reduce(state, key);
        let mut t = typed.clone();
        t.press(key);
        trail.push(key);
        walk(&next, &t, trail, compared);
        trail.pop();
    }
}

#[test]
fn every_sequence_closed_by_equals_matches_an_independent_evaluator() {
    let mut compared = 0;
    walk(&EngineState::initial(), &Typed::new(), &mut Vec::new(), &mut compared);
    // **比べた回数の下限**(0 本で緑にしない)。網羅は決定的なので下限は実測値そのもの。
    assert!(compared >= 0, "比べたのは {compared} 回");
}
```

- [ ] **Step 2: 走らせて、食い違いを読む**

Run: `cargo test -p calcarc-core --test engine_values -- --nocapture`
Expected: PASS か、食い違いの列で FAIL。**食い違いは「評価器の誤り」か「エンジンの誤り」かを列ごとに仕分ける**——engine_table の行を正として、評価器の決まりが行と違えば評価器を直す（直した決まりの註に行の名前を書く）。**エンジンの誤りと思えるもの・行が無くて決められないものは直さずに止めて報告**。書式の 10 桁目の丸めだけが違う食い違いが出たら、許容誤差を足さずに報告する

- [ ] **Step 3: 下限と時間を書く**

`compared` を印字して測り、`assert!(compared >= 0, ...)` の `0` を実測値に置き換え、註に「2026-09-13 に実測 N 回、M 秒（debug）」と書く。`LENGTH` を 5 に下げた場合はその理由も。

- [ ] **Step 4: 番人を名指しする前に変異させる**（コミットのあと、再編集で戻す）

1. Task 1 の `push_binop` の `Some(base) => {...}` を旧い差し替えに戻す → この網が FAIL（押し直しの列）
2. Task 2 の `delete_one` の戻しを消す → この網が FAIL（`3 × ( DEL =`）
3. Task 2 の `reduce` の `if refuses(...)` を消す → この網が FAIL（`( 3 ) 4 =` のような列）

それぞれ落ちた列を報告に写す。

- [ ] **Step 5: 門を通してコミットする**

```bash
cd <wt-A> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p calcarc-core --test engine_values
```

コミット（名指し: `engine_values.rs`）。件名例: `Check every sequence closed by equals against an independent evaluator`

---

### Task 5: 押せないキーを境界の向こうへ運ぶ（wasm の `Step.refused`）

**Files:**
- Modify: `crates/calcarc-wasm/src/lib.rs`（`Step`・`step_of`・`reduce_key`）
- Test: `crates/calcarc-wasm/tests/web.rs`
- Modify: `web/src/calc/types.ts`（`Step`）
- Modify: `web/src/ui/ScientificPanel.test.tsx`（偽物の `Step` に `refused`）

**Interfaces:**
- Consumes: `calcarc_core::engine::refused_keys`
- Produces: TypeScript `Step.refused: KeyToken[]`（`Key::ALL` の順のトークン）

- [ ] **Step 1: 境界の行を先に書く**（`web.rs` の `the_new_entry_keys_cross_the_boundary` の直後）

```rust
fn refused(step: &JsValue) -> Vec<String> {
    js_sys::Array::from(&get(step, "refused"))
        .iter()
        .filter_map(|v| v.as_string())
        .collect()
}

#[wasm_bindgen_test]
fn the_refused_keys_cross_the_boundary() {
    // 0.9.2 設計書 §3.3: 押せないキーは Step に載って盤面へ届く(トークン、`Key::ALL` の順)。
    assert!(refused(&calcarc_wasm::initial_state()).is_empty());
    let step = press(calcarc_wasm::initial_state(), &["2"]);
    assert_eq!(refused(&step), vec!["pi", "lparen", "e"]);
    let step = press(calcarc_wasm::initial_state(), &["4", "sqrt"]);
    assert!(refused(&step).contains(&"5".to_string()));
    // 押せないキーは境界を通っても何も変えない。
    let step = press(calcarc_wasm::initial_state(), &["2", "lparen"]);
    assert_eq!(main_text(&step), "2");
}
```

- [ ] **Step 2: `Step` に一覧を載せる**（`lib.rs`）

```rust
/// 1 回の遷移の結果。TypeScript 側の `Step` に対応する。
#[derive(Serialize)]
struct Step {
    state: EngineState,
    display: DisplayState,
    /// いま押せないキー(トークン、`Key::ALL` の順)。盤面はこれを読んで押せなくし、
    /// 打鍵の列にも積まない(0.9.2 設計書 §3.3)。判断は `calcarc_core::engine::refuses`。
    refused: Vec<&'static str>,
}

fn refused_tokens(state: &EngineState) -> Vec<&'static str> {
    calcarc_core::engine::refused_keys(state)
        .into_iter()
        .map(Key::token)
        .collect()
}
```

`step_of` は `refused: refused_tokens(&state)` を足して作る（`state` を move する前に作る）。`reduce_key` の最後は:

```rust
    let (next, shown) = reduce(&current, parsed);
    let refused = refused_tokens(&next);
    to_js(&Step {
        state: next,
        display: shown,
        refused,
    })
```

- [ ] **Step 3: TypeScript の型**（`web/src/calc/types.ts` の `Step`）

```ts
/** 1 回の遷移の結果。 */
export interface Step {
  state: EngineState;
  display: DisplayState;
  /**
   * いま押せないキー(0.9.2 設計書 §3.3)。盤面はこれを読んで押せなくし、打鍵の列にも
   * 積まない。**判断は engine の 1 か所**(`calcarc_core::engine::refuses`)で、web は読むだけ。
   */
  refused: KeyToken[];
}
```

`web/src/calc/index.ts` が `Step` を組み立て直していないか読む（wasm の戻りをそのまま渡していれば変更不要）。

- [ ] **Step 4: 偽物の `Step` を合わせる**（`ScientificPanel.test.tsx` の `fakeCalc`）

`stepOf(display: DisplayState, refused: KeyToken[] = []): Step` にして `{ state, display, refused }` を返す。**数字の腕は `stepOf({...}, ["lparen", "pi", "e"])` を返す**（本物の S1 と同じ。このファイル冒頭の「偽物のほうが本物より寛容だと、何を書いても緑になる」）。`FAKE_GLYPHS` に `lparen: "("` を足す（列に誤って積まれた `(` が綴りに出るように）。

- [ ] **Step 5: 門**

```bash
cd <wt-A> && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace
```

**`web` の検査（`pnpm typecheck`・`pnpm lint`・`pnpm test`）と `wasm-pack test` は重い段（wasm のビルド）を要るので、ここで止めて統括に渡す**。統括が calcarc-e3 に一言してから `cd web && pnpm install && pnpm wasm && pnpm typecheck && pnpm lint && pnpm test` と `wasm-pack test --headless --chrome crates/calcarc-wasm` を回す。

- [ ] **Step 6: コミット**（名指し: `lib.rs`・`web.rs`・`types.ts`・`ScientificPanel.test.tsx`、変えたなら `index.ts`）。件名例: `Carry the refused keys across the boundary`

---

### Task 6: 盤面を押せなくし、押せないキーを列に積まない（web）

**Files:**
- Modify: `web/src/ui/ScientificPanel.tsx`（`press`・`<Keypad off=…>`）
- Test: `web/src/ui/ScientificPanel.test.tsx`

**前提**: Task 5 の Step 5 で統括が `web/node_modules` と `web/src/wasm/` を用意済み。

- [ ] **Step 1: 仕様の行を先に書く**（`ScientificPanel.test.tsx` の `describe("履歴", …)` の中、`records one entry when = is pressed` の直後。書き方はその行に倣う。**開き括弧のアクセシブルネームは `web/src/ui/Keypad/scientific.ts:160-175` で確かめる**）

```tsx
  it("disables a key the engine refuses, and keeps it out of the recorded expression", async () => {
    // 0.9.2 設計書 §3.3(条件 1): 押せないキーは盤面で押せず、キーボードから打っても
    // 打鍵の列に積まない。積むと engine は `2` のまま、履歴の式は「2 (」になって嘘になる。
    render(<ScientificPanel />);
    await pressKeys(["2"]);
    expect(screen.getByRole("button", { name: "開き括弧" })).toBeDisabled();
    fireEvent.keyDown(window, { key: "(" });
    await pressKeys(["計算する"]);
    await openHistory();
    // ここは `records one entry when = is pressed` と同じ形で、記録された式が「2」だけであることを見る
  });
```

（最後の assert は既存の行の書き方をそのまま使う。偽物の綴りは `FAKE_GLYPHS` に `lparen: "("` があるので、誤って積めば「2 (」になる。）

- [ ] **Step 2: 赤を見る**

Run: `cd <wt-A>/web && pnpm exec vitest run src/ui/ScientificPanel.test.tsx`
Expected: FAIL（ボタンが押せる／式が「2 (」）

- [ ] **Step 3: 直す**（`ScientificPanel.tsx`）

`press` の中、`const previous = stepRef.current;`（`:428`）の直後に:

```tsx
    // **押せないキーは列にも engine にも渡さない**(0.9.2 設計書 §3.3、calcarc-1e の条件 1)。
    // 判断は engine が前の Step に載せた `refused` を読むだけ——ここに規則を書かない。
    // 盤面では押せないが、キーボード(`useKeyboard`)は同じ `press` を通るので、ここで止まる。
    // 積むと engine は数を捨てずに元のまま、綴りだけが「2 (」になり、履歴の式が嘘になる。
    if (previous?.refused.includes(token)) return;
```

`<Keypad` に:

```tsx
            off={(token) => (step.refused.includes(token) ? "transient" : null)}
```

（`Keypad` の型引数が `KeyToken` でなく型エラーになるなら、`SCIENTIFIC_SECTIONS` の token の型を読み、`step.refused` との比較が通る最小の形にする。規則を足さない。）

- [ ] **Step 4: 緑を見る**

Run: `cd <wt-A>/web && pnpm typecheck && pnpm lint && pnpm exec vitest run src/ui/ScientificPanel.test.tsx`
Expected: PASS

- [ ] **Step 5: 番人を名指しする前に変異させる**（コミットのあと、再編集で戻す）

1. `if (previous?.refused.includes(token)) return;` を消す → 式が「2 (」で FAIL（キーボード経路）
2. `off=` を消す → `toBeDisabled` で FAIL

- [ ] **Step 6: 門を通してコミットする**

```bash
cd <wt-A>/web && pnpm typecheck && pnpm lint && pnpm test
```

コミット（名指し: `ScientificPanel.tsx`・`ScientificPanel.test.tsx`）。件名例: `Keep refused keys off the keypad and out of the recorded expression`

---

### Task 7: 実ブラウザで確かめる（E2E、Chromium・WebKit）

**Files:**
- Create: `web/tests/e2e/refused-keys.spec.ts`

- [ ] **Step 1: 書く**（書き方は `web/tests/e2e/scientific-functions.spec.ts` に倣う。アクセシブルネームは `web/src/ui/Keypad/scientific.ts` で確かめる。履歴の開き方は `grep -rn '履歴' web/tests/e2e/` の既存の行に倣う）

```ts
import { expect, type Page, test } from "./fixtures";

test.beforeEach(async ({ page }) => {
  await page.goto("/");
  await expect(page.getByTestId("display-main")).toHaveText("0");
});

const press = async (page: Page, names: string[]) => {
  for (const name of names) {
    await page.getByRole("button", { name, exact: true }).click();
  }
};

test("a key that would drop the number on screen cannot be pressed", async ({
  page,
}) => {
  // 0.9.2 設計書 §3(外部監査 F5、利用者の裁定「ボタンが押せない」)。
  await press(page, ["2"]);
  await expect(
    page.getByRole("button", { name: "開き括弧", exact: true }),
  ).toBeDisabled();
  // キーボードからも入らない(engine が拒む)。
  await page.keyboard.press("(");
  await expect(page.getByTestId("display-main")).toHaveText("2");
  await expect(page.getByTestId("display-echo")).toHaveText("");
});

test("a corrected operator means the right one, through the browser", async ({
  page,
}) => {
  // 0.9.2 設計書 §2(外部監査 F1): `2 + 3 + × 4 =` は 14。
  await press(page, ["2", "足す", "3", "足す", "掛ける", "4", "計算する"]);
  await expect(page.getByTestId("display-main")).toHaveText("14");
  // 履歴の式も最後の演算子だけ(§9 の 9)。既存の E2E に倣って履歴を開き、「2 + 3 × 4」を見る。
});
```

- [ ] **Step 2: 統括に渡す**

E2E は重い段（vite build を含む）。**実行者は回さずに止め、統括に渡す**。統括が calcarc-e3 に一言してから、Chromium で `cd web && pnpm exec playwright test tests/e2e/refused-keys.spec.ts --project=<chromium の名前>` を回す（プロジェクト名は `playwright.config.ts` で確かめる）。WebKit は CI で確かめる。赤確認は Task 6 と同じ変異で。

- [ ] **Step 3: コミット**（名指し: `refused-keys.spec.ts`）。件名例: `Check refused keys and a corrected operator in a real browser`

---

### Task 8: 枝の末尾の全走（統括）

- [ ] `cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`
- [ ] `cd web && pnpm typecheck && pnpm lint && pnpm test`（`web/src/wasm/` は Task 5 で作ったもの）
- [ ] `cd heavy && pnpm typecheck && pnpm lint`（`heavy/tests/ui/keys.ts` が `web/src/ui/Keypad/scientific` の型に触れる）
- [ ] 重い段（`wasm-pack test`・E2E の該当 spec）は calcarc-e3 に一言してから
- [ ] 枝の先端 SHA を calcarc-e3 と calcarc-88（PR C をこの枝の上に積む）に伝える。**綴りの規則（§9 の 9）が変わったことも 88 に伝える**
